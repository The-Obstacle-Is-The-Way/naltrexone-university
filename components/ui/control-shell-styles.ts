/**
 * Shared visual shell for compact inline controls.
 *
 * Used when distinct control primitives should read as the same bordered muted
 * surface without duplicating class strings.
 */
export const compactControlShellClasses =
  'inline-flex rounded-lg border border-border bg-muted p-1';
