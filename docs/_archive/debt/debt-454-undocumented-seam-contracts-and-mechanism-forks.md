# DEBT-454: Undocumented Per-Context Seam Contracts and Mechanism Forks — `end()` Dead/Live Recovery Split, Convention-Only Lock Preconditions, Forked DB Retry Loop

**Status:** Deferred / resolved in part
**Priority:** P4
**Date:** 2026-07-09
**2026-07-18 staleness audit:** Stale but real against `ddad8eee`. BUG-294 added deletion's transaction-bound subscription lock, bringing the inventory to five lock methods and three advisory-lock implementations; two adapters intentionally share the canonical subscription-domain key. Discard is also a pinned third consumer of the private DB retry runner.
**FW-1 disposition:** 2026-07-21 — Parts 1, 2a, and 3 are resolved: the per-context `end()` contract, all five transaction-bound lock preconditions, callback-transaction wiring/shared subscription-lock structure, and the deliberate fresh-transaction retry fork are now documented and pinned by focused tests. Part 2b remains **PARKED**; revive only when a real base-DB lock call is found or a sixth lock-capable consumer is introduced, using the Part 2a wiring/structural tests as the detection instrument. No factory split, hash change, or retry consolidation shipped.

---

## Direction (2026-07-20 forest review)

| Part | Verdict | Chosen option | Rejected as disproportionate | One-line rationale |
| --- | --- | --- | --- | --- |
| 1. `end()` dead/live recovery split | **FIX (Option A, minimal form)** | Add the per-context contract comment above the 0-row recovery and the one-line nested-SAVEPOINT/snapshot note on `inRepeatableRead`, exactly following DEBT-441; change no behavior. | Option B's tx-handle detection/error remapping and Option C's standalone/tx structural split for a dead branch. | (a) Correct documentation closes the seam without another path; (b) the standalone branch is live and the current tx-bound branch is dead; (c) Blast radius: a future reroute can turn an ended-session CONFLICT into INTERNAL_ERROR. Fix cost: two local comments; (d) makes the hidden context contract visible where it is edited; (e) preserves the existing retry owner. |
| 2a. Lock contracts and wiring | **FIX (Option B, minimal form)** | Add implementation-local tx-precondition comments to all five lock methods; add webhook-wiring tests proving callback-transaction construction and a structural pin that subscription upsert and user deletion both use the shared `acquireSubscriptionWriteLock`/`hashtext(userId)` helper. Keep the unrelated tombstone hash documented as a separate domain. | Immediate factory/type/interface segregation; hash standardization; a one-commit hash switch. | (a) Reuses visible comments and existing wiring seams; (b) all current callers are tx-bound and no misuse exists; (c) Blast radius: a future base-db lock silently releases before protected work. Fix cost: five comments and focused structural/wiring tests; (d) pins the real contract and shared logical key without inventing sameness across domains; (e) leaves factory ownership unchanged until evidence warrants it. |
| 2b. Type-level lock enforcement | **PARK** | Do not split factories/interfaces now. Revive Option A only when review/test evidence finds one real base-db lock call or a sixth lock-capable consumer is introduced; the source scan/wiring tests from Part 2a are the detection instrument. | Option A's factory split, transaction-scoped locker factory, or user-repository interface segregation before the trigger. | (a) Defers a boundary that all current callers already honor; (b) misuse is possible but absent; (c) Blast radius: one future autocommit call would provide no multi-statement exclusion. Cure cost: factory/type churn across mixed read/write repositories is larger; (d) comments/tests provide the proportionate partial boundary; (e) names one trigger for every lock method instead of duplicating enforcement. |
| 3. DB retry fork | **FIX (Option 1, minimal form)** | Keep the local runner; document fresh top-level RR transaction per attempt, SQLSTATE allowlist, base-plus-uniform jitter, and typed exhaustion. Add a nonzero deterministic RNG test, a safe best-effort retry log carrying only attempt/max/code/delay, and structural coverage for finalize, session-backed submit, and discard. | Option 2's generic `retry()` delay/RNG extension and consolidation; changing the updater's separate immediate CAS loop. | (a) Documents a legitimate fork and adds only missing observation/test seams; (b) the current zero-RNG test is vacuous and retries are silent; (c) Blast radius: a refactor can drop jitter/fresh-transaction/typed-exhaustion semantics or elevated retries can remain invisible. Fix cost: comments, one deterministic test, and one non-sensitive log hook; (d) tests real semantics instead of shared-mechanism aesthetics; (e) keeps transaction retry and external-service retry as intentionally distinct owners. |

Per-context documentation and focused structural tests are the proportionate boundary for seams whose current callers are correct; factory/type enforcement remains parked until misuse or a new consumer proves the stronger boundary worthwhile. Subscription upsert and deletion must keep one canonical advisory key, while tombstone locking remains a different domain; any future hash migration requires the documented expand/contract dual-lock transition. The DB retry fork is retained because each attempt must open a fresh transaction and exhaustion has a domain-specific mapping.

## Description

Three adapter/composition-root seams carry a load-bearing per-context contract or deliberate mechanism divergence that is only partially documented. The transaction requirement for repository `lock()` methods exists at the ports, but is neither enforced by types/factories nor repeated at the implementations; the `end()` recovery split and DB-retry fork have no equivalent contract comment. Current paths behave correctly with respect to these three findings. The risk is that a plausible refactor removes a property whose ownership is not visible at the edited seam. This is the same comprehension class DEBT-441 resolved for `practice-session-question-state-updater.ts` with a per-context contract comment.

### 1. `end()` 0-row recovery branch: undocumented dead/live split with latent CONFLICT→INTERNAL_ERROR misclassification

[`DrizzlePracticeSessionRepository.end()`](../../../src/adapters/repositories/drizzle-practice-session-repository.ts#L569) runs in two calling contexts with opposite liveness for its 0-row recovery branch ([lines 593-607](../../../src/adapters/repositories/drizzle-practice-session-repository.ts#L593)), and neither `end()` nor [`inRepeatableRead`](../../../src/adapters/repositories/drizzle-practice-session-repository.ts#L157) documents the split:

- **Standalone** ([`EndPracticeSessionUseCase`](../../../src/application/use-cases/end-practice-session.ts#L38), autocommit READ COMMITTED): the guarded UPDATE (`isNull(endedAt)`) can genuinely return 0 rows after a concurrent committed end, and the recovery re-read at [line 594](../../../src/adapters/repositories/drizzle-practice-session-repository.ts#L594) (`findByIdAndUserId` → fresh top-level REPEATABLE READ transaction) correctly classifies CONFLICT. **Live and correct.**
- **Tx-bound** ([`FinalizeExamAnswersUseCase` calls `tx.sessions.end(...)`](../../../src/application/use-cases/finalize-exam-answers.ts#L278) inside the composition-root RR transaction from [`use-cases.ts`](../../../lib/container/use-cases.ts#L117)): a concurrent committed end raises `40001` at the UPDATE instead, so the branch is dead for the current finalize flow. If a future tx-bound path made a 0-row result reachable, `findByIdAndUserId` would call `inRepeatableRead` on an existing `PostgresJsTransaction`; installed drizzle-orm 0.45.2 implements that nested call as `client.savepoint(callback)` and accepts no isolation config, so the re-read inherits the outer snapshot. It could still see `endedAt = null` and fall through to [`INTERNAL_ERROR 'Failed to end practice session'`](../../../src/adapters/repositories/drizzle-practice-session-repository.ts#L604) instead of the standalone branch's ended-session CONFLICT.

The armed scenario is a future reroute/copy, not a current defect. Moving standalone `end()` under the RR runner would change the live zero-row classification into transaction-level `40001` ownership; copying the re-read into another tx-bound flow could activate the stale-snapshot misclassification. Losing the CONFLICT code would bypass the reason-aware recovery work shipped under DEBT-438; absent/unknown **reasons** still fail safe, but `INTERNAL_ERROR` is a different status entirely. DEBT-441 documents the corresponding split one file over ([practice-session-question-state-updater.ts#L128-L135](../../../src/adapters/repositories/practice-session-question-state-updater.ts#L128)). BUG-293 concerns a different `end()` seam — its two standalone pre-reads can observe different autocommit snapshots — and its proposed read-only RR wrapper does not move the later UPDATE under the composition-root runner.

### 2. Five row/advisory lock methods rely on tx-bound construction; three advisory call paths span two key domains

Five repository methods provide exclusion only while their database handle is inside a surrounding transaction:

- [`DrizzleDeletedClerkUserRepository.lock`](../../../src/adapters/repositories/drizzle-deleted-clerk-user-repository.ts#L11) uses `pg_advisory_xact_lock`.
- [`DrizzleClerkEventRepository.lock`](../../../src/adapters/repositories/drizzle-clerk-event-repository.ts#L40), [`DrizzleStripeEventRepository.lock`](../../../src/adapters/repositories/drizzle-stripe-event-repository.ts#L40), and [`DrizzleUserRepository.lockByClerkId`](../../../src/adapters/repositories/drizzle-user-repository.ts#L56) use `SELECT ... FOR UPDATE`.
- [`DrizzleUserRepository.acquireSubscriptionWriteLock`](../../../src/adapters/repositories/drizzle-user-repository.ts) uses the shared subscription-domain `pg_advisory_xact_lock(hashtext(userId))` helper before the user-deletion cascade.

Their port contracts state that the lock must be called inside a transaction ([deleted-Clerk-user port](../../../src/application/ports/deleted-clerk-user-repository.ts#L1), [Clerk-event port](../../../src/application/ports/clerk-event-repository.ts#L17), [Stripe-event port](../../../src/application/ports/stripe-event-repository.ts#L17), [user port](../../../src/application/ports/user-repository.ts#L19)). The gap is enforcement and implementation-local visibility: the relevant factories accept an omitted `dbOverride` and can construct these repositories over `primitives.db` ([repositories.ts](../../../lib/container/repositories.ts)). In autocommit, the transaction-level advisory lock or row lock is released when that one statement's implicit transaction ends, before subsequent read/write statements can use the exclusion.

Current production callers are correctly bound. The Clerk route constructs Clerk-event, deleted-user, and user repositories from the callback's `tx` ([app/api/webhooks/clerk/route.ts](../../../app/api/webhooks/clerk/route.ts)); both user-deletion call sites acquire the subscription-domain lock before their cascade. Stripe webhook dependencies construct the Stripe-event repository from their transaction callback ([lib/container/controllers.ts](../../../lib/container/controllers.ts)). The subscription repository opens its own transaction before acquiring the same subscription-domain lock. Thus no reachable misuse was found, but a future caller can compile a base-db lock that has no cross-statement effect.

Repository-wide tracing finds three advisory-lock call paths. Deleted-Clerk-user locking hashes an external Clerk ID with [`hashtextextended(clerkUserId, 0)`](../../../src/adapters/repositories/drizzle-deleted-clerk-user-repository.ts#L13). Subscription upsert and user deletion both call [`acquireSubscriptionWriteLock`](../../../src/adapters/repositories/subscription-write-lock.ts), which uses the exact canonical `pg_advisory_xact_lock(hashtext(userId))` expression over the internal user UUID. Those two adapters intentionally share one logical lock domain: Stripe webhook, checkout success, reconcile, and the inverse-order deletion cascade must serialize on the same key. The tombstone domain remains unrelated and uses its distinct hash idiom; a rare cross-domain numeric collision can cause spurious blocking, not lost exclusion.

### 3. Composition-root DB write-retry loop forks the shared `retry()` mechanism

[`runPracticeSessionStateWriteTransaction`](../../../lib/container/use-cases.ts#L79), introduced by BUG-268, is wired around `FinalizeExamAnswersUseCase` ([lines 117-132](../../../lib/container/use-cases.ts#L117)), **session-backed** `SubmitAnswerUseCase`, and — since [BUG-292's fix (PR #627)](../bugs/bug-292-discard-unowned-serialization-failure-internal-error.md) — `DiscardPracticeSessionUseCase` ([lines 157-162](../../../lib/container/use-cases.ts#L157)). Draft save and mark-for-review use the updater's standalone READ COMMITTED/CAS retry path; standalone end runs its pre-reads in one repeatable-read snapshot with an autocommit guarded UPDATE ([BUG-293's fix, PR #627](../bugs/bug-293-end-split-prereads-phantom-corruption-error.md)). The original `submit/finalize/draft-save` scope claim was false; the wave-1 fixes then added discard as the third runner consumer.

The runner hand-rolls exponential backoff with private constants ([use-cases.ts#L41-L50](../../../lib/container/use-cases.ts#L41): 3 attempts / 25ms base / 250ms cap), base-plus-uniform jitter ([lines 63-73](../../../lib/container/use-cases.ts#L63)), and local sleep. Only attempts 0 and 1 sleep under the current three-attempt loop, producing integer delays of 25-49 ms and 50-99 ms; the 250 ms cap is not reached. This is not “full jitter” in `[0, cappedDelay)`. [`src/adapters/shared/retry.ts`](../../../src/adapters/shared/retry.ts#L69) already supplies bounded deterministic exponential retry with `shouldRetry`, `onRetry`, a delay cap, and injectable sleep; [`retry-defaults.ts`](../../../src/adapters/shared/retry-defaults.ts#L8) holds the external-call defaults (3/100ms/x2/1000ms). The DB loop also has distinct exhaustion semantics: it maps exhausted `40001`/`40P01` failures to `practiceSessionStateChangedConcurrentlyError({ cause })`, whereas generic `retry()` rethrows the last error. A separate DB policy is therefore legitimate, but its jitter/fresh-transaction/exhaustion rationale is not documented at the implementation.

Consequences:

- The DB-retry knobs are invisible to anyone tuning `retry-defaults.ts`.
- The loop has no retry-observation hook ([use-cases.ts#L95-L103](../../../lib/container/use-cases.ts#L95)), so `40001`/`40P01` retries on these two transaction-bound workflows are silent, while Stripe retry logs each attempt ([stripe-retry.ts#L43-L59](../../../src/adapters/gateways/stripe/stripe-retry.ts#L43)).
- The existing test named "waits with jittered backoff" forces `Math.random()` to `0` and proves only the 25ms base delay ([container-practice-session-state-transactions.test.ts#L196-L227](../../../lib/container-practice-session-state-transactions.test.ts#L196)); removing the random term would still pass it. A maintainer consolidating onto `retry()` as-is could therefore drop jitter without a red test.

The verifier explicitly excluded [`UPDATE_QUESTION_STATE_MAX_RETRIES`](../../../src/adapters/repositories/practice-session-question-state-updater.ts#L12) from this finding: it is an intentionally immediate-retry, backoff-free CAS loop already ruled correct and documented via DEBT-441 — context, not part of the defect.

## Impact

Today: zero wrong behavior on any live path — all three parts are documentation/mechanism-hygiene debt, hence P4. What each part contributes:

1. **`end()` split** — a refactor that reroutes or copies the recovery branch turns a concurrent double-end/finalize into a 500-class INTERNAL_ERROR instead of CONFLICT, silently defeating the DEBT-438 exam-client recovery UX. The failure is quiet (only a concurrency race exposes it) and would pass typecheck and most tests.
2. **Lock preconditions** — a future caller can construct any of five locking methods without a tx override; the port comments are the primary defense. The subscription-domain sharing is correct; the difference between that domain and the tombstone hash remains naming/keyspace comprehension debt, not a proven correctness problem.
3. **Retry fork** — tuning external-call `DEFAULT_RETRY_OPTIONS` correctly has no effect on the separate DB policy, but that separation is not explained. Elevated `40001`/`40P01` retry churn is invisible until exhaustion surfaces as typed CONFLICT with cause preserved. Cost: change comprehension, an unpinned jitter property, and missing retry observability — not current wrong behavior.

## Proposed Resolution

**Part 1 — `end()` recovery branch:**
- **Option A (CHOSEN, minimal form; DEBT-441 precedent):** add a per-context contract comment above the 0-row recovery branch in `end()` and a one-line note on `inRepeatableRead` that nested calls on a tx-bound `this.db` become SAVEPOINTs inheriting the outer snapshot while isolation config is ignored. State that the standalone guarded UPDATE can genuinely return 0 rows and the fresh top-level re-read classifies CONFLICT, while tx-bound RR callers raise `40001` and are owned by `runPracticeSessionStateWriteTransaction`, so the branch is dead and MUST NOT be relied on there. No behavior changes.
- **Options B and C are REJECTED BY DIRECTION REVIEW:** tx-handle detection/error remapping or a structural path split is disproportionate for a dead current branch.

**Part 2 — lock preconditions and idiom divergence:**
- **Option B (CHOSEN, minimal form):** add implementation-local tx-precondition comments to all five lock methods; add webhook-wiring tests proving Clerk/Stripe callback-transaction construction; and add a structural pin proving subscription upsert and user deletion both call the same shared `acquireSubscriptionWriteLock` helper, whose canonical expression remains `hashtext(userId)`. Keep the tombstone's `hashtextextended(clerkUserId, 0)` documented as an unrelated lock domain.
- **Option A's factory split/interface segregation is PARKED:** revive only when a real base-db lock call is found by review/tests or a sixth lock-capable consumer is added. Until then, the comments and wiring/source-scan tests are the detection instrument and proportionate partial boundary.
- **Hash standardization is REJECTED BY DIRECTION REVIEW.** If a future concrete cross-domain requirement ever changes that ruling, it must use an expand/contract transition: acquire old and new keys in deterministic order during a compatibility deploy, then remove the old key only after old instances are gone. A one-commit hash switch is forbidden under rolling deployment.

**Part 3 — retry fork:**
- **Option 1 (CHOSEN, minimal form):** keep the local DB loop and document why it is separate: every attempt opens a fresh top-level RR transaction, only `40001`/`40P01` retry, delay is base-plus-uniform jitter, and exhaustion maps to typed `StateChangedConcurrently`. Add a best-effort retry log carrying only attempt/max/code/delay, a nonzero deterministic RNG test so removing jitter fails, and a structural pin for finalize, session-backed submit, and discard. Logger failure must not change retry behavior. Keep DB policy constants local/DB-named.
- **Option 2 is REJECTED BY DIRECTION REVIEW:** do not extend generic `retry()` with delay/RNG machinery merely to erase a legitimate semantic fork.
- Leave the updater's `UPDATE_QUESTION_STATE_MAX_RETRIES` loop unchanged; it is the separately documented, deliberately backoff-free CAS pattern from DEBT-441.

## Verification

1. **Part 1:** the two comments state the standalone-vs-tx-bound contract; existing standalone-CONFLICT and tx-bound-40001 behavior remains unchanged, with no assertion or production-branch change.
2. **Part 2a only:** all five implementation methods state the transaction requirement; Clerk and Stripe wiring tests prove callback-tx construction; a structural test pins subscription upsert and user deletion to the same shared helper/expression; the tombstone domain remains distinct. No factory/type split or hash change is allowed while Part 2b is parked.
3. **Part 3:** a composition-root structural test pins the runner to finalize, session-backed submit, and discard; a nonzero deterministic RNG pins the 25–49 ms and 50–99 ms base-plus-uniform ranges; best-effort observation is asserted for both `40001` and `40P01`; logger failure is harmless; exhausted retries still map to typed CONFLICT with cause; and non-retryable errors run once.

## Related

- [DEBT-441 (archived)](./debt-441-updater-dead-stale-retry-paths-under-rr.md) — the precedent: per-context dead/live retry contract documented for the question-state updater; its resolution is scoped solely to that file and does not cover `end()`.
- [BUG-267 (archived)](../bugs/bug-267-nested-repeatable-read-silently-drops-isolation.md) — verified that nested `PostgresJsTransaction.transaction()` never receives/applies an isolation config; fixed by opening the outer tx at RR, leaving caller discipline as a documented residual hazard.
- [BUG-268 (archived)](../bugs/bug-268-cas-retry-loop-ignores-repeatable-read-serialization-failure.md) — introduced `runPracticeSessionStateWriteTransaction`; its archive records no reuse-vs-fork decision.
- [BUG-209 (archived)](../bugs/bug-209-clerk-webhook-lacks-idempotency.md) — the tombstone-resurrection race the Clerk lock seam closes; records the lock mechanism as fix narrative, files no ruling on the enforcement gap.
- [DEBT-438 (archived)](./debt-438-conflict-reason-client-coverage-gaps.md) — the exam clients' CONFLICT-driven recovery UX that Part 1's latent misclassification would bypass; [DEBT-435 (archived)](./debt-435-practice-session-conflict-and-test-hygiene-follow-ups.md) hardened the retry-loop test with a non-retryable negative path.
- [BUG-292](../bugs/bug-292-discard-unowned-serialization-failure-internal-error.md) and [BUG-293](../bugs/bug-293-end-split-prereads-phantom-corruption-error.md) — archived records of the pre-fix seams. Post-wave: BUG-292's fix routes discard through this composition-root retry helper (its third consumer), while BUG-293's standalone-end pre-reads run in a read-only repeatable-read snapshot outside it.
- [PostgreSQL advisory-lock functions](https://www.postgresql.org/docs/16/functions-admin.html#FUNCTIONS-ADVISORY-LOCKS) and [explicit locking](https://www.postgresql.org/docs/17/explicit-locking.html#ADVISORY-LOCKS) — transaction-level advisory and row locks are released at transaction end; a one-statement autocommit call cannot protect later statements.
- Found during the 2026-07-09 DDIA-lens adversarial database-seam sweep (12 finder lenses, per-candidate adversarial verification, dedup against the full archived register).
