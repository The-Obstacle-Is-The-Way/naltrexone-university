# DEBT-475: Toolchain Coherence — Scars, Scaffolding, and the Ruling That Went Stale

**Status:** Resolved — 2026-09-21; archived after implementation and production verification
**Priority:** P2
**Date:** 2026-08-24

## Closeout — 2026-09-21

All authorized DELETE/REPLACE work is shipped and production-verified. This dated closeout supersedes the earlier execution paragraphs' “pending” labels without rewriting those historical receipts. The current disposition table below records the final decisions; KEEP mechanisms remain, the optional shared helper was not selected without demonstrated net deletion, and cron-secret operations remain owned by the still-open DEBT-474.

Final implementation: #960/#961 removed helper-level error recovery; #962/#963 replaced the general raw-JSX-button scan; #964/#965 moved ordinary architecture specifiers and filenames to Biome while retaining unsupported literal-call and semantic checks. Latest main is `66ee3da2`, with dev/main tree `d2488a1e289de455988bf64bb990717f1c911a3e`. Main CI `35559030529` passed 4,495 unit / 411 browser / 349 integration plus six intentional provider skips / 44 E2E. Production and health returned 200; Vercel held the release until the main test check passed. [Complete closeout ledger](../../debt/assets/debt-475-closeout-2026-09-21/verification.md).

Corrections: the retained seed-choice reference guard is `scripts/seed-helpers.ts`'s refusal to delete choices referenced by attempt/session history, not explanation-reference validation. The workflow-pin row now reflects four workflows. The source-helper row is a deliberate no-extraction decision, not an unimplemented mandatory replacement. No content-import code, protected debt record, size rule, floor, or skip changed in this closeout.

**Source:** Owner question while [DEBT-473](./debt-473-green-without-evidence.md) was under review: is the dev toolchain "battle scars" that fix real incidents, or "ad hoc patchwork" that an agent fleet built around problems instead of removing them — and how would we tell the difference before deleting something load-bearing? Answered by a four-slice read-only census at `dev` `5a095f12` that traced 66 mechanisms (scripts, wrappers, policy scans, ratchets, hooks, configs) to their introducing commit and the debt or bug document that motivated each, then asked of every one: is that problem still real, what depends on it, and what simpler primitive would do the same job. Every number below that the census produced was re-executed by the filer at `5a095f12` before filing; four census file paths were wrong and are corrected here.

**Corrections (2026-08-24, PR #831 rounds 2–3).** Round 2 withdrew the proposed deletion of the actual-schema drift probe and cross-clone URL guard, restored the exact-semver Stripe CLI policy and hard `security.txt` expiry assertion, corrected the DEBT-472 LOC arithmetic, and moved the AUDIT-012 supersession note to live ADR-001. Round 3 confirmed that DEBT-244 §1 records the schema probe as implemented under DEBT-243, but found three residual defects: the Biome step retained only hook placement despite F1 naming four scanner-only semantics; DEBT-472 step 4 did not yet own the three `.setup.ts` mocks; and DEBT-473's wrapper deletion set still removed live process contracts. The resolution now preserves the full semantic scanner remainder, adds the DEBT-472 pointer, classifies the provider wrapper as 35 keep / 9 preflight cases relocated / 0 deleted, and removes literal test-database host/port values from this planning record. The residue pass corrected the semantic scanner's source anchors, removed the remaining impact claim that Biome replaces all ~1,400 scanner lines, removed the false claim that deletions carry no design risk, and made replacement-contract proofs—not a zero-importer grep—the prerequisite for removing the bookmark and Docker helpers. The first exact-head review repaired the template-literal code span and replaced the shell-redirection placeholder with a concrete, archive-excluding `rg` command; the second widened that command from a hand-picked extension list to every tracked file outside the archive.

**Correction (2026-08-25, PR #831 final-head gate).** An interrupted required-E2E run exposed one more toolchain gap: the local orchestrator does not clean up its spawned process tree on `SIGINT` or `SIGTERM`. F9 records the observed orphan and adds process-tree cleanup to this debt rather than treating the occupied-port symptom as operator error.

**Correction (2026-08-25, promotion review).** The copyable deleted-file proof now invokes `ls-files` through `git --no-pager`, matching the repository-wide non-interactive Git-read rule without changing the proof's tracked-file or archive-exclusion scope.

**Correction (2026-08-26, DEBT-473 implementation review).** The skip-policy scan was added after this census. Its policy remains load-bearing: unapproved test controls must fail the required lane. A review-driven attempt to model JavaScript alias data flow was removed in PR #838 after the scanner adopted fail-closed unknown receivers, reducing that scanner family from 1,826 to 901 lines before this debt begins. The promotion reviews also caught two residual inventory mismatches in the former source-walker plan: it omitted `src` despite naming the controller datetime contract, and its shorthand named only hook placement instead of the retained architecture-boundary scanner's four semantic properties. The 2026-08-27 table supersedes that consolidation plan: policy scopes stay separate and only value-free read/sort/fail-closed acquisition may be shared.

**Correction (2026-08-27, Phase C disposition ruling).** The percentage judgment and eight-step consolidation plan were too coarse to authorize deletion safely. The disposition table in **Resolution** is now the sole execution authority: it names the property, current-tree receipt, replacement proof, and risk for each mechanism. F1–F9 remain discovery context, not implementation instructions. Where the evidence does not choose a safe direction, the table says **OWNER RULING NEEDED** and work stops instead of extending the excavation. The exact-head review found one table-coverage omission: F6's completed workflow-action SHA-pinning control. Its explicit `KEEP — COMPLETE under DEBT-474 step 2` row now preserves that supply-chain property without reopening implementation.

**Execution (2026-08-27, Phase C mechanism 1).** The production 350-line warning is deleted without replacement. Before the change, a package-config assertion that lint-staged did not invoke `check-file-size` failed red. After removing that lint-staged entry, `scripts/check-file-size.sh`, and its self-test, the same assertion and an archive-independent source-reference check passed. The unit lane moved from 465 files / 4,235 tests to 464 files / 4,234 tests, exactly the deleted self-test, with every remaining case green. No Biome file-size rule or other metric gate was added; ADR-019 still requires a new ADR before a metric can gate work.

**Execution (2026-08-27, Phase C mechanism 2).** The subscription-repository contract-coverage self-test is deleted. Before deletion, removing `findByExternalSubscriptionId` from `SUBSCRIPTION_REPOSITORY_CONTRACT_METHODS` made `pnpm typecheck` fail at the exhaustive assignment with `Type 'true' is not assignable to type 'false'`. Restoring the method made typecheck green, and the fake contract's eight scenarios remained green. The removed runtime test only compared the same exported list with a second copy of its literal; the compile-time `Exclude<keyof SubscriptionRepository, listed method>` proof remains the authority.

**Execution (2026-08-27, Phase C mechanism 3).** The unused `lockPath` field is deleted from `LocalTestTarget`. Before deletion, changing the explicit-override contract from partial matching to exact equality failed 1/14 because `lockPath` was the sole extra property; all app, database, Compose, and instance identities already matched. Removing the field and its unused path imports made the same contract pass 14/14, typecheck passed, and a non-documentation tree search found no remaining `lockPath` consumer.

**Execution (2026-08-27, Phase C mechanism 4).** The `seed-all-environments.sh` shell trampoline is deleted while both TypeScript seed entry points remain separate. Before deletion, the package-script contract failed 1/7 because `db:seed:all` still targeted the shell wrapper. Pointing it directly at `seed-all-environments.ts` made the suite pass 7/7. A process-level case also passes an unsupported option through the package command and proves that the TypeScript entry's argument-specific usage error and exit status 1 reach the caller before any seed runtime is created.

**Execution (2026-08-27, Phase C mechanism 5).** The `@stripe/cli` version guard no longer duplicates the current package version. The replacement contract first failed six range, wildcard, tag, and workspace-protocol cases while the literal assertion remained. A local exact-SemVer recognizer then made all 22 cases green; after removing only the `1.50.1` comparison, the focused policy suite remains green 21/21. The manifest still must use exact SemVer, while the six-case provider contract remains the behavioral proof required for an actual CLI bump.

**Execution (2026-08-28, Phase C mechanism 6).** The bookmark-only reset helper and its two self-tests are deleted after their narrower target-specific postcondition moved into the general E2E reset. The new case holds the total bookmark count at one while setting the `placeholder01Id` bookmark count to zero; it failed red because the general verifier resolved, then passed in the 24/24 focused suite after verification received the already-resolved fixture IDs, counted the placeholder target separately, and required both counts to equal one. A live-code reference scan found only the orphan's own tests and one multi-dot filename allowlist entry before deletion; after deleting all three files and that allowlist entry, no non-documentation reference remains.

**Execution (2026-08-29, Phase C mechanism 7).** `export:feedback` now enters through the existing human database-command boundary instead of loading `.env.local` / `.env` and reading `DATABASE_URL` directly. Three focused cases failed red on the former path: a mocked dotenv fallback was accepted, a remote target without `DB_TARGET_ACK` opened the SQL client, and an approved local target emitted no redacted identity. Removing the dotenv loads and delegating to `runHumanDatabaseCommand` made the export, command-boundary, and target-boundary suites pass 43/43. The package entry point with both authority variables absent exits 1 before opening SQL with the value-free explicit-URL refusal; approved targets write their redacted identity to stderr so CSV/JSON stdout remains machine-readable.

**Execution (2026-08-29, Phase C mechanism 8).** The supported `db:generate` package entry now uses a thin wrapper around the existing human database-command boundary before Drizzle Kit loads its fallback-capable configuration. The initial red run proved both the wrapper module and package routing were absent; a direct-execution mutation then made all three behavior cases fail: missing explicit URL and missing remote acknowledgement each started the process, while an approved local target emitted no redacted receipt. Delegation to `runHumanDatabaseCommand` made the generate/command/target suites pass 25/25. Promotion review then found that the wrapper dropped caller-supplied Drizzle Kit options: the new `--name` case failed 1/4 before the executable entry forwarded `process.argv.slice(2)`, after which the three boundary suites passed 26/26. With `DATABASE_URL` and `DB_TARGET_ACK` absent, the real package entry exits 1 before Drizzle Kit starts even when ignored dotenv files exist. Raw dependency-CLI invocation is not a supported repository entry point and remains outside this package-command contract.

**Execution (2026-08-29, Phase C mechanism 9).** Test-double fidelity remains blocking but no longer runs as Vitest `globalSetup`. The first focused run failed all four intended boundaries: a forbidden own-code factory fixture did not reach a fail-closed CLI, `lint:doubles` was absent from the package entry points, Vitest still named the setup module, and required CI had no fidelity step. The existing scanner now throws on any live ratchet issue; `pnpm lint` and required CI invoke `pnpm lint:doubles`, and CI does so before unit coverage. A temporary forbidden own-code factory fixture made the real package command exit 1 with the file and ratchet detail, while the clean tree reports `PASS test-double-fidelity issues=0`. The 53-line setup provider, its 70-line self-test, and three injected unit assertions are deleted; the scanner, all three policy families, and every floor stay. Across three equivalent local runs, median `pnpm test --run` wall time moved from 38.07 seconds (38.07 / 37.99 / 38.22) to 29.43 seconds (29.43 / 29.43 / 29.66), an 8.64-second reduction; the explicit scan measured 8.93 seconds once per lint invocation.

**The answer, in one line:** decide one named property at a time; origin stories and line counts are evidence, not permission to keep or delete code.

**Owner rulings and implementation re-audit (2026-09-20).** The complete 2026-09-19 owner reply supersedes the three `OWNER RULING NEEDED` dispositions: replace the span scanner with a typed wrapper and a Sentry import boundary while retaining runtime filtering; pin the Vercel CLI and use `pnpm exec`; add a monthly single-issue warning inside 90 days while keeping the hard security.txt test. These are separate mechanism PRs. The current tracing surface is still six `Sentry.startSpan` sites: four action spans, the webhook parent and subscription retrieval. The existing 102-line projector permits only the registered identity strings, finite nonnegative duration, nonnegative safe-integer count and known application error codes, including protection from throwing accessors; both initial and later-set fields must retain that runtime protection. The replacement hides the SDK span and derives fixed metadata from the registered family. It replaces the old exact-file/site census with the owner-approved typed family/field and import boundary, not a new source scanner. Biome is now 2.5.12; [its import restriction](https://biomejs.dev/linter/rules/no-restricted-imports/javascript/) supports the named module ban. The old scanner stays until the real-lint, typecheck, runtime-filter and emitted-metadata proofs pass. No Stripe API, SDK pin, sampling configuration or application behavior change is authorized by this row.

## Description

**Execution (2026-09-20, security.txt renewal; review/promotion pending).** The monthly `Security contact renewal` workflow checks the committed file on the first day of each month at 07:11 UTC and supports manual dispatch. It opens or reuses one titled issue only when fewer than 90 days remain, including an already-expired file; repeated checks do not create duplicates, and a later renewal cycle reopens the same issue. It does not extend Expires automatically. The issue requires contact/policy re-verification and an expiry less than one year after that verification date. The hard expiry test is unchanged. SHA-pinned actions, job-scoped `issues: write`, read-only checkout, serialized runs, and value-free failure reporting bound the automation. [Red proofs and operational limits](../../debt/assets/security-txt-renewal-2026-09-20/verification.md). This implements only the renewal row; the debt stays Open.

**Implementation correction (2026-09-20, E2E infrastructure).** The owner restored one-concern PRs: readiness and interrupted-run cleanup are the first E2E-infrastructure PR; renewal automation and the CLI boundary follow separately. Bounded readiness and normal interruption are implemented, with promotion receipts still required. The promotion review below reopens cleanup after an OS signal error; it does not undo the verified normal-interruption proofs. The old 575-line Docker layer is removed only after the surviving Compose command proves bounded readiness and failure propagation. The signal fixture also reproduced a detached-descendant leak in a group-only prototype: Playwright creates a separate web-server group, so cleanup snapshots only the active command's descendant groups before signalling and retains escalation even if the parent exits first. No blanket port kill, retry change, new framework, register edit, or debt closure is included. See the [red-proof ledger](../../debt/assets/debt-475-e2e-infrastructure-2026-09-20/verification.md).

### What is earned (and must not be deleted)

These trace to incidents the current tree still permits. They are listed first because the owner's fear — deleting a scar — is the correct fear, and the census's first job was to name what stays:

| Mechanism | Incident it fixes | Still reachable today because |
| --- | --- | --- |
| DB target guard + `DB_TARGET_ACK` (`scripts/database-target.ts`, `database-command.ts`) | [DEBT-240](./debt-240-local-dev-database-url-points-to-production.md): `.env.local` pointed at production Neon; [DEBT-446](./debt-446-local-db-script-target-guards.md) | `drizzle.config.ts:6-7` still falls back to `.env.local`; drizzle-kit 0.31.8 has no target guard |
| Per-clone test target resolver (`scripts/resolve-local-test-target.ts`) | [DEBT-417](./debt-417-multi-clone-local-test-infrastructure-isolation.md): a fixed port made `lsof … kill -9` kill a foreign server; duplicate container names risked cross-clone data corruption | `.env.test` and `docker-compose.yml:13` still define a raw fixed-port fallback; the owner runs multiple clones |
| Migration ledger verifier (`scripts/migration-ledger.ts`, `verify-migration-ledger.ts`) | BUG-240/241 production outage (migrations never applied); DEBT-391, DEBT-442, DEBT-445 | `vercel.json` migrates at build; drizzle-kit has no drift command |
| Seed answer-key guard (`scripts/seed/question-syncer.ts:73-100`), wrong-answer-heading rejection (`question-parser.ts:57-61`), choice-reference guard (`seed-helpers.ts:63-67`) | BUG-281 (production-verified: re-import rewrote `is_correct` under graded history); DEBT-338 (verified live corruption); DEBT-111 | 948 imported MDX files are re-seeded on every environment |
| E2E credential health check (`tests/e2e/helpers/credential-health-check.ts`) | DEBT-243 (credential drift), DEBT-391/442 (schema drift behind `.env.local`) | one shared E2E user, one shared Stripe test account |
| Owner namespace (`E2E_STRIPE_OWNER`, `seed-test-user.ts`) | DEBT-384 (46% webhook error rate), [DEBT-386](./debt-386-e2e-stripe-customer-ownership-drift-webhook-500s.md) (cross-database rebind → 500 → retry loop) | every lane shares one Stripe test account |
| Deterministic E2E state reset (`reset-e2e-user-state.ts`, `e2e-reset-shared.ts`) | DEBT-244, DEBT-293 (CI run `22852148955`), DEBT-411 | `workers: 1`, shared user |
| Fixture UUID integrity test (`tests/shared/fixture-uuid-integrity.test.ts`) | [DEBT-400](./debt-400-test-fixture-integrity-zod-boundary.md): Zod 4 changed UUID validation and broke boundary fixtures | ids are strings; TypeScript cannot check them |
| Theme-token **render** tests (`components/theme-token-regression.test.tsx:352-697`) | DEBT-108 (shipped unreadable dark-mode buttons), BUG-151/DEBT-279 (WCAG), DEBT-313 | tokens are strings in class attributes |
| Chromium installer bound (`scripts/ci/install-playwright-chromium.sh`, `ci.yml:121`) | DEBT-471 F2: a 56-minute silent install hang on run `32298458967` | runner-image mirror behavior is outside the repo |
| Playwright lane pins (`tests/playwright-lane-policy.test.ts:72-135`) | DEBT-471 F1 (Stripe hosted-DOM change reddened a docs PR), DEBT-205 (same, February) | Stripe documents Checkout as un-automatable |
| Vitest timeouts, `fileParallelism: false`, `optimizeDeps` list | DEBT-225 (cold-import flakes), DEBT-333, taxonomy-census race (`vitest.integration.config.mts:10-13`), DEBT-368 (Vite reloads mid-suite) | same runtime |
| Supply-chain policy (`pnpm-workspace.yaml`: `strictDepBuilds`, `allowBuilds`, overrides, cooldown) | [DEBT-394](./debt-394-supply-chain-hardening.md) | pnpm 11.3.0 |
| Node-version hook (`.husky/check-node-version.sh`) | DEBT-394 PR1: a stale global husky init loaded nvm without `nvm use` | machine-local, still true |
| Draft-question importer, feedback exporter, seed environment fence | DEBT-102, DEBT-338, BUG-250 (formula injection), DEBT-446 | live content pipeline |
| Actual-schema drift probe (`credential-health-check.ts:185-215` `verifyIdempotencySchema`) and the cross-clone URL guard (`tests/shared/resolve-integration-database-url.ts`) | DEBT-244 §1 (implemented under DEBT-243: schema drift the ledger cannot see); DEBT-417 (cross-clone contamination) | the ledger checks records and hashes, not columns; `setup.ts:61` checks loopback, not clone identity |

Everything in the rest of this document is judged against that list: a change that weakens any row above is out of scope.

### F1 — A correct June ruling became the cause of ~1,400 LOC of hand-written lint rules

[AUDIT-012](../audits/audit-012-repo-org-devx.md) (2026-06-13) locked a principle at `:57`: "the primary contributor is an AI-agent fleet, and the repo's revealed strategy is executable, no-new-dependency guardrails over convention," and at `:59` ruled for ARCH-1: "custom Vitest import-boundary test. Do NOT add `dependency-cruiser`." That was right for June. It was never re-asked when the tool the repository already depends on caught up. Biome 2.5.8 (installed; `node_modules/@biomejs/biome/configuration_schema.json`) ships `noRestrictedImports` with path patterns and per-directory overrides, `useFilenamingConvention`, `noRestrictedElements`, `noExcessiveLinesPerFile`, and `noImportCycles`. The filing compared five hand-written or tool-specific controls with those capabilities — partly, not wholly:

| Scanner | LOC | Property | Biome equivalent | Bypass the census found |
| --- | --- | --- | --- | --- |
| `tests/architecture-boundary-source-scan.ts` + test | 604 | layer import bans; kebab-case filenames; hook placement | `noRestrictedImports` overrides cover specifier-level bans and `useFilenamingConvention` the filenames; the scan's resolved-target and type-only distinctions (`:121-167`), question-route hook placement (`:211-224`), and page-model naming rule (`:226-242`) have no Biome equivalent and stay in a slimmed scanner | ``import(`zod`)`` template literal (`:287` requires `isStringLiteral`); `require()` never parsed; `lib/` and `db/` not importer-scanned (`:17-23`) |
| `tests/server-span-family-boundary.test.ts` | 469 (own glob + `createProgram` + type checker) | six `Sentry.startSpan` sites use projected attributes | a typed `startServerSpan()` wrapper + `noRestrictedImports` on `@sentry/nextjs#startSpan`; DEBT-462 recorded **zero** spans existed when the guard was written | `startInactiveSpan`, `startSpanManual`, `withActiveSpan`, `setContext/setTag/setExtra` all escape (`:384-387` pre-filters on the substring `startSpan`) |
| `components/theme-token-regression-source-scan.ts`, raw-`<button>` half | ~150 | no raw `<button>` outside `components/ui/` | `noRestrictedElements` (JSX only; `React.createElement('button')` stays uncovered by either) | `React.createElement('button')`; template-literal class strings (`:37-38` line regex) |
| Former `scripts/check-file-size.sh` + self-test | 194 before deletion; 0 now | observational 350-line production warning, 14 exemptions | **None under the current ADR.** `pnpm lint` promotes Biome warnings to failures, so a production `noExcessiveLinesPerFile` rule would create a numeric quality gate forbidden by [ADR-019](../../adr/adr-019-test-quality-practices.md) without a new ADR | **It could not fail:** its unconditional `exit 0` let production files cross 350 lines while active. Phase C mechanism 1 deleted the script, self-test, and lint-staged entry without replacement. |
| Biome `noExcessiveLinesPerFile` test override at 800 (`biome.json:41-53`) | 12 + 28 suppressions | test files ≤ 800 lines | it *is* the primitive | 28 of 28 over-limit files (34,090 LOC, 21.5% of the test estate) carry a `biome-ignore … DEBT-469`; a rule with a 100% suppression rate fails only on the 29th file |

The opacity-scale half of the theme scan (`:42-87`) has no lint equivalent and stays. Biome may replace the specifier-level, filename, and raw-JSX-element halves only after red equivalence proofs; it does not replace resolved-import semantics, type-only distinctions, question-route hook placement, page-model naming, opacity policy, or `createElement` coverage. The production line-count warning is different: it is observational, has not changed behavior, and cannot be promoted to a Biome gate without amending ADR-019, so it is deleted without replacement.

### F2 — Build-around instead of fix, with the receipts that show the primitive arrived and the workaround stayed

- **Historical census at `5a095f12`; replaced by #939 on 2026-09-20.** `scripts/local-test-db.ts` + `scripts/ensure-local-test-db.ts` + test (**575 LOC**, one caller: `e2e-local-orchestrator.ts:73`). Both branches of `ensureLocalTestDatabase` (`:50-64`, `:66-78`) run `docker compose … up -d --wait db` and then a 60×1 s `docker inspect` poll (`:100-130`). The poll predates `--wait` (`06671f7b`, DEBT-411 PR-B); `be50dda7` (DEBT-417) added `--wait` and kept the poll. `scripts/run-local-test-db.ts:41,68` already issues the identical `--wait` command with the same resolver-scoped project name. Compose v5.4.0: `--wait  Wait for services to be running|healthy`. These files are now deleted; `scripts/run-local-test-db.ts:39-56,69-90` owns `--wait --wait-timeout 60`, and `scripts/e2e-local-orchestrator.ts:72-75` delegates to the shared command. This paragraph retains the original replacement rationale, not a current duplicate-polling finding.
- At filing, `scripts/run-trial-clock-smoke.ts` had nine duplicated preflight cases assigned to DEBT-473's shared provider gate. **Completed by DEBT-473 step 4 on 2026-08-26:** the file and its two test files are renamed to `run-stripe-provider-contracts*`, the nine preflight cases moved to `tests/shared/stripe-provider-gate.test.ts`, and the other 35 cases remain. They are **not** a deletion candidate: the round-3 case audit found each reporter, scratch, exit, platform, process-group, signal-forwarding, escalation, or descendant-cleanup case protects a live branch.
- The former `ci.yml` "Validate E2E credential inputs" step (53 lines of bash) duplicated `credential-health-check.ts`'s required-variable and dummy checks. **Completed by #842:** the workflow step is gone, the Playwright `setup` project remains fail closed, and `tests/ci-workflow.test.ts` pins the duplicate name's absence.
- `credential-health-check.ts:185-215` `verifyIdempotencySchema` was listed here in the previous draft and is **withdrawn**: it probes `information_schema.columns` for the actual `idempotency_keys.completed_at` column, whereas the ledger (`migration-ledger.ts:256-275`) verifies recorded migrations and their file hashes — a manually dropped column leaves the ledger valid. It is the only actual-schema drift probe on the E2E path and joins the keep list.
- **Completed by Phase C mechanism 2 on 2026-08-27:** the former `tests/shared/subscription-repository-contract-coverage.test.ts` asserted that a five-element const equalled its own literal. It is deleted after the compile-time check in `subscription-observation-version-contract.ts` failed red on an omitted method and the focused fake contract remained green.
- **Completed by Phase C mechanism 4 on 2026-08-27:** the former `scripts/seed-all-environments.sh` only forwarded to the TypeScript entry and is deleted. `db:seed:all` now invokes `seed-all-environments.ts` directly, with package-command coverage for argument and exit-status forwarding. `scripts/seed-production.ts` and `scripts/seed-all-environments.ts` remain separate because they expose different production authority and consent paths despite their similar size.
- `scripts/crap-report.ts:588-662` validates coverage-map structure that `istanbul-lib-coverage.createCoverageMap` accepts even when counters are missing, negative, extra, or attached to reversed ranges. Focused tests prove those failures, so the validator stays. The small `--k=v` parser at `:500-517` also stays absent a demonstrated broken property.

### F3 — Orphans, and near-orphans kept alive by their own guards

- **Completed by Phase C mechanism 6 on 2026-08-28:** at filing, `tests/e2e/helpers/reset-bookmarks-for-e2e-user.ts` plus two self-tests were an 882-LOC orphan with zero non-test importers and one scanner allowlist entry. They are deleted after the general reset absorbed the missing `placeholder01Id`-specific verification under a red wrong-target case. Origin `108edadc` (DEBT-281, CI run `22782151850`); superseded when the reset helper absorbed it.
- **Completed by Phase C mechanism 3 on 2026-08-27:** the former `LocalTestTarget.lockPath` field was computed for every target but consumed only by its own test. It is deleted after an exact-shape resolver contract failed red on that sole extra property and passed with every live target identity unchanged.
- `tests/shared/resolve-integration-database-url.ts` (133 LOC with test) was listed here in the previous draft and is **withdrawn**: it enforces the exact CI shape and byte-equality with the per-clone resolver (`:16-41`) — cross-clone protection that `setup.ts:61`'s loopback check does not give. One consumer is not a reason to delete a guard; if it is ever inlined, the full policy and its tests move with it.

### F4 — Five places decide "which database," and they disagree

URL-producing sources: `scripts/resolve-local-test-target.ts:47-88` (cwd-hash → resolver-selected per-clone loopback target); `scripts/seed-environment-runtime.ts:42-64` (`.env.local` + `npx vercel env pull`); `drizzle.config.ts:6-12` (dotenv fallback used by raw Drizzle Kit configuration); `.env.test` + `docker-compose.yml:13` (the raw fixed-port fallback). Guards and deciders on top of them: `database-target.ts:20-28,104-110`, `internal/database-target-managed.ts`, `run-local-integration.ts:26-31`, `e2e-local-orchestrator.ts:36-42`, `tests/integration/setup.ts:16-20,44-65` (duplicated in `helpers.ts:15-27`), `tests/shared/resolve-integration-database-url.ts:12-13,35-39`, and the human command boundary now used by `export-question-feedback.ts:191-211` and `db-generate.ts:11-21`.

At filing they disagreed in four ways: three port conventions (CI's service port, the raw fixed-port fallback, and the resolver's hash-derived per-clone target); two definitions of loopback; a caller-supplied fallback URL could pass early setup but fail the byte-equality resolver contract; and the DEBT-446 fence was bypassed by the supported `export:feedback` and `db:generate` package entries. #842 closed the `CI`-inference and fallback-authority defects under DEBT-473; Phase C mechanisms 7 and 8 then placed both human package commands behind the existing guard. `seed-environment-runtime.ts` still shells `npx vercel env pull`; `vercel` is not in `package.json`, so the table leaves pin-versus-owner-prerequisite as a ruling instead of folding these distinct authorization policies into "one resolver."

### F5 — DEBT-472 mixes load-bearing policy with avoidable startup and data-flow complexity

Filed by the same author; recorded without softening.

- The DEBT-472 fidelity scanner — the seven `tests/test-double-fidelity-*.ts` files (1,807 LOC) plus `tests/test-double-fidelity.test.ts` (639), **2,446 LOC** at filing — originally ran as a `globalSetup` subprocess on every `vitest` invocation (`vitest.config.mts:17` at filing; `LIVE_SCAN_PROCESS_TIMEOUT_MS = 30_000` because CI run `32659858531` exceeded the former 14 s bound). Measured at `5a095f12`: 8.65 s wall / 13.55 s CPU per `pnpm test`, and again on every `pre-push` hook. Phase C mechanism 9 moved the unchanged policy engine and floors to fail-closed `pnpm lint:doubles`, invoked by local lint and required CI before unit coverage; its measured unit median fell by 8.64 seconds. The scanner's test/spec-only walk still omits `practice-session-page-model.browser.setup.ts`, which contains three own-code factory-form `vi.mock` calls and remains owned by the separate classifier-replacement row. The earlier filing treated import-expression, aliased-`vi`, and cast-syntax variants as an evasion contest; the table rejects that threat-model expansion. The later replacement recognizes the explicit allowed forms and fails closed on anything else, while the unknown-cast and maintained-port policies remain separate.
- `tests/fake-contract-register*.ts` (377 LOC) protects four current properties: every maintained fake is registered exactly once, verification and known-divergence cells are non-blank, and waiver dates are real calendar dates. Its limitations—waiver dates are not tied automatically to behavior changes and discovery is convention-based—do not make a 15-line barrel-membership check equivalent. The guard stays unless a replacement first proves all four properties.

### F6 — Guards that fight the tools, or the calendar

- **Completed by Phase C mechanism 5 on 2026-08-27:** `tests/playwright-lane-policy.test.ts` no longer duplicates the current `@stripe/cli` version literal from `package.json`. The guard accepts exact SemVer and rejects ranges, wildcards, tags, and workspace protocols. The exact-version policy remains load-bearing because `stripe-cli-checkout.ts` depends on the fixture `--override`/`--remove` parser semantics; the provider contract remains the proof required for each bump.
- `tests/security-txt.test.ts:24` asserts `Expires` is in the future; `public/.well-known/security.txt:4` is `2027-06-13`. The whole unit lane fails on that date — which is correct fail-closed behavior, since [RFC 9116](https://www.rfc-editor.org/rfc/rfc9116.html) directs consumers to treat the file as stale after `Expires`; the defect is that nothing warns ahead of it.
- **Completed by #842:** all `uses:` entries in the three workflows are SHA-pinned, and the YAML-aware workflow contract covers workflow, job, step, and reusable-workflow forms (DEBT-474 F2).
- `.husky/pre-push` now pays the F5 subprocess on every push.

### F7 — Six TypeScript-compiler walkers, four different definitions of "production source"

`tests/architecture-boundary-source-scan.ts`, `tests/server-span-family-boundary.test.ts`, `tests/test-double-fidelity-source-scan.ts`, `tests/test-double-fidelity-port-double-scan.ts`, `tests/fake-contract-register-source-scan.ts`, `scripts/crap-report.ts` (plus `src/adapters/controllers/controller-output-datetime-contract.test.ts`) each build a `createSourceFile`/`createProgram` walk with policy-specific glob and ignore lists (`tests/architecture-boundary-source-scan.ts:17-37`, `tests/server-span-family-boundary.test.ts:8-20`, `components/theme-token-regression-source-scan.ts:29-35`, `tests/test-double-fidelity-source-scan.ts:49-62`). Those file sets are not one concept: production boundaries, test controls, test doubles, UI tokens, and report inputs legitimately have different scope. A shared helper is allowed only for value-free acquisition—read requested paths, sort deterministically, and fail closed on unreadable or empty input. Each policy keeps ownership of its own globs, exclusions, parser mode, and semantics; type-aware `createProgram` consumers may remain independent.

### F8 — Misplaced and redundant checks on the CI path

- `ci.yml:117-119` validates the GitHub Actions copy of `CRON_SECRET` for header safety; no cron reads that copy. [BUG-244](../bugs/bug-244-reconciliation-cron-never-scheduled.md)'s incident lived in Vercel scopes; application consumption still does not prove the Actions and Vercel values are the same. Owned by DEBT-474 step 3.
- Four "is the database up" checks on the local E2E path (Compose `--wait`, the F2 inspect poll, `setup.ts` `SELECT 1`, the health check's connectivity probe); the ledger is verified at Vercel build and again at every E2E start (intentional — different databases).

### F9 — An interrupted local E2E run orphans the app server

At 2026-08-25 03:13Z, interrupting the required-E2E gate left Playwright's `next-server` listening on this clone's resolver-selected app port. `spawnCommand()` starts every child with inherited stdio but no detached process-group ownership, parent-signal handlers, or descendant cleanup (`scripts/e2e-local-orchestrator.ts:132-153`). Playwright correctly sets `reuseExistingServer: false` (`playwright.config.ts:45-49`), so the following run refused to start while the orphan held the port instead of silently testing an unknown server. The occupied port is therefore evidence of an orchestrator lifecycle defect, not a reason to weaken Playwright's isolation check.

### What the census did not find

- No mechanism on the earned list has a simpler primitive that covers its incident.
- Every `package.json` script is referenced somewhere, but four — `db:seed:prod`, `quality:crap`, `local:test:target`, `export:feedback` — have no workflow, hook, or script caller, only documentation.
- The seed format migration is complete: zero imported MDX files contain the old "Why other answers are wrong" form; the parser's rejection is now a regression fence and cheap to keep.
- The tj-actions-class exposure (job-scoped secrets, mutable tags) is DEBT-474, not this debt.

## Impact

1. **Trust tax on every run:** 8.65 s of scanner per `pnpm test` and per push, for a scan with a live blind spot (F5).
2. **Dead and redundant code kept alive by guards** (F2, F3): an 882-LOC orphan whose only dependents are its own tests and a scanner allowlist line, and a 575-LOC layer that duplicates a Compose flag.
3. **A fence repaired at the supported entries** (F4): #842 removed ambient-`CI` target authority, and Phase C mechanisms 7–8 put `export:feedback` and `db:generate` behind the existing human-command guard; the seed-time Vercel CLI choice remains an owner ruling.
4. **Lint rules written by hand** (F1): syntactic specifier, filename, and raw-JSX-element portions may be replaceable after red equivalence proofs; resolved-target, type-only, hook-placement, page-model, opacity, and typed-span contracts are semantic. The observational production line-count warning is deleted, not converted into a gate.
5. **Guards that red the lane for the wrong reason** (F6): a duplicated version literal, and an expiry with no advance warning.
6. **Interrupted-run contamination** (F9): an aborted local E2E run can leave this clone's app port occupied, blocking the next honest run and forcing a manual ownership check.

## Resolution

**Tracing replacement implemented (2026-09-20; review/promotion pending).** The owner-approved wrapper now owns the SDK call and filters both initial and later-set attributes with the unchanged runtime projector. All six callers retain their family, count/error annotations, return/error behavior, and business callback. Real-file Biome fixtures, typecheck mutations, and runtime-filter mutations supplied the red proofs before deleting the 469-line scanner; the focused restored run passed 112 cases. The former exact-file/site census is deliberately replaced by the typed boundary and import restriction, not recreated elsewhere. The original F1/F7 census above describes the pre-replacement tree. See [the proof ledger](../../debt/assets/server-tracing-2026-09-20/verification.md). This does not close the other table rows.

**Vercel CLI ruling and implementation (2026-09-20; review/promotion pending).** The owner's later ruling supersedes the September 19 devDependency/`pnpm exec` proposal: keep the load-bearing production-identity lookup, but pin the owner-run CLI outside the application dependency graph. The earlier installation experiment added 19 advisories absent from the application lockfile, including critical `tar` via `@vercel/fun`; this is dependency presence, not proof of exploitability in `env pull`. Those tooling advisories are explicitly accepted, not relabeled as fixed or tracked as application alerts. Do not override the vendor's internals. Revisit if the CLI enters the application's runtime graph or CI. The permitted `pnpm dlx vercel@59.16.0` option was tested and refused by the unchanged no-downgrade trust policy (`undici@5.29.0`); no exception was added. The alternative owner-approved implementation requires a preinstalled **exact 59.16.0**, verified before any environment pull, and refuses missing, mismatched, or failed commands. npm still reports this as the newest stable release past the seven-day gate. This clone's installed 59.23.2 is correctly refused; an operator must provision the reviewed version before using either environment-seeding command. No global installation, remote environment pull, or database mutation was performed. See [the proof and accepted-risk record](../../debt/assets/vercel-cli-pin-2026-09-20/verification.md).

This table is the sole implementation authority for Phase C. A `KEEP` row forbids deletion in this program. A `DELETE` or `REPLACE` row licenses only the named change after its replacement proof is first observed red. `OWNER RULING NEEDED` means stop; it is not permission to investigate indefinitely or choose a direction. Risks describe the consequence of a wrong disposition, not the amount of code involved.

| Mechanism | Ruling | Property it protects | Current-tree evidence | Replacement proof required before deletion or replacement | Risk |
| --- | --- | --- | --- | --- | --- |
| Human database target guard + `DB_TARGET_ACK` | **KEEP** | A human database command cannot silently target the wrong environment | `scripts/database-target.ts` and `scripts/database-command.ts` still fence commands while `drizzle.config.ts` can read developer configuration | N/A; any future replacement must red-test local, preview, production, missing-ack, and implicit-fallback cases | Critical: production mutation |
| Per-clone local test-target resolver | **KEEP** | Concurrent clones receive distinct app, database, and Compose identities | `scripts/resolve-local-test-target.ts` remains the source of truth used by integration, database, and E2E wrappers | N/A; an alternative must prove deterministic same-clone identity and different-clone isolation | High: cross-clone contamination |
| Migration ledger verifier | **KEEP** | Recorded migrations exist, are ordered, and retain their committed hashes | `scripts/migration-ledger.ts` and `scripts/verify-migration-ledger.ts` cover a property drizzle-kit does not expose | N/A; a replacement must fail on a missing, reordered, or modified migration | Critical: unapplied or altered schema |
| Actual-schema `idempotency_keys.completed_at` probe | **KEEP** | E2E refuses a database whose live column shape drifted despite a valid ledger | `verifyIdempotencySchema` queries the catalog; a complete hashed ledger does not prove the column still exists | N/A; replacement must red-test valid ledger plus missing live column | High: false-ready E2E target |
| Cross-clone integration URL guard | **KEEP** | Integration connects to the resolver-selected clone, not merely any loopback database | `tests/shared/resolve-integration-database-url.ts` enforces exact resolver equality beyond the setup host check | N/A; if inlined, its byte-equality and foreign-clone tests move intact | High: wrong database evidence |
| Seed answer-key preservation guard | **KEEP** | Re-import cannot rewrite the correct choice beneath graded history | `scripts/seed/question-syncer.ts` still synchronizes the full MDX corpus into persisted questions | N/A; replacement must fail when a re-import changes an established answer key | Critical: graded-history corruption |
| Wrong-answer-heading rejection | **KEEP** | Legacy or ambiguous explanation structure cannot be silently accepted | `scripts/seed/question-parser.ts` rejects the retired heading even though the current corpus is clean | N/A; replacement must red-test the retired heading | High: malformed explanations re-enter content |
| Seed choice-reference guard | **KEEP** | Seeding cannot delete a choice referenced by an attempt or practice-session state | `scripts/seed-helpers.ts` computes referenced IDs and refuses their deletion in `computeChoiceSyncPlan`; `scripts/seed/question-syncer.ts` calls it before choice deletion | N/A; replacement must retain the existing referenced-choice refusal contract | High: broken graded-history references |
| E2E credential health check | **KEEP** | Authenticated billing E2E fails before running against unusable or mismatched prerequisites | The setup project still calls the health check against shared Clerk/Stripe TEST-mode state | N/A; any replacement must preserve every fail-closed prerequisite and actual-schema check | High: green without evidence or shared-state damage |
| Stripe provider gate | **KEEP** | Flag-off skips, while explicit live-provider intent throws on any unusable prerequisite | Both provider suites, E2E seed path, and runner preflight consume `resolveStripeProviderGate` | N/A; future changes must preserve the full flag/key/price truth table | High: opt-in suite goes green by skipping |
| Bounded Stripe provider runner | **KEEP** | The dedicated command runs all six provider cases, enforces process bounds, and reports executed/passed/skipped counts | Hosted evidence reports `PASS executed=6 passed=6 skipped=0`; runner tests cover cancellation and reporting | N/A; future replacement must preserve the process bound and executed-count receipt | High: partial provider evidence reported complete |
| `E2E_STRIPE_OWNER` namespace | **KEEP** | Shared Stripe TEST objects cannot be rebound across unrelated local databases | Seed and health-check paths require the owner marker before mutating the shared test customer | N/A; replacement needs isolated provider ownership with migration and cleanup proof | Critical: webhook retry/cross-database corruption |
| Deterministic general E2E state reset | **KEEP** | Every serial run starts from the same application-owned state | Required E2E still uses one worker and one shared user, and specs call the general reset | N/A; replacement must prove all reset postconditions against the shared user | High: order-dependent E2E |
| Fixture UUID integrity test | **KEEP** | Boundary fixtures remain valid under runtime UUID validation | IDs are TypeScript strings and Zod validates them only at runtime | N/A; replacement must fail on an invalid fixture UUID | Medium: boundary fixture failures |
| Theme-token render contracts | **KEEP** | Rendered classes retain the tokens that prevent historical light/dark contrast failures | `components/theme-token-regression.test.tsx` checks emitted markup that types and static import rules cannot prove | N/A; replacement must red-test the historical contrast cases | High: inaccessible UI regression |
| Theme opacity-policy scanner half | **KEEP** | Undocumented arbitrary opacity values cannot enter production UI | `components/theme-token-regression-source-scan.ts` enforces repository-specific allowed values with no stock-lint equivalent | N/A; replacement must red-test each forbidden opacity form and documented exception | Medium: design-token drift |
| Chromium installer time bound | **KEEP** | A runner archive/mirror failure terminates instead of wedging required CI | `scripts/ci/install-playwright-chromium.sh` bounds the same external apt phase that previously hung | N/A; replacement must prove healthy install plus bounded primary/failover failure | High: merge lane unavailable |
| Playwright required/hosted lane policy pins | **KEEP** | Required E2E never drives Stripe-owned DOM and hosted journeys remain observational | `tests/playwright-lane-policy.test.ts` enforces project/spec separation and scheduled naming | N/A; replacement must fail on either direction of lane leakage | High: third party can red merge lane or coverage disappears |
| Blanket Playwright retries | **REPLACE — runner policy promoted via #932; production closeout verified 2026-09-20** | A failed product/cleanup Playwright attempt cannot become green through a configured runner retry; helper-level recovery remains separate below | Promotion CI `35505691274` recovered a credential-preflight fetch exception; 42 product cases plus cleanup passed without retries. The former global CI-two/local-one setting also retried product failures | Five config contracts failed red, then 28/28 passed in local and CI modes. Default/product/cleanup retries are zero; setup retains CI-two/local-one as a documented bootstrap tradeoff, not an outage classifier. No scanner or metric gate. [Proof ledger](../../debt/assets/e2e-retries-2026-09-20/verification.md) and [live policy](../../dev/testing-infrastructure.md#playwright-retry-policy) | Medium: hidden intermittent regressions; setup still includes seed/reset |
| Session first-question and bookmarks-page implicit recovery | **REPLACED — COMPLETE 2026-09-21, #960/#961** | The ordinary success-path helpers reject their first visible question-load or bookmarks-page error instead of recovering inside a passing attempt | `startSession` no longer clicks Try again; bookmark setup no longer revisits an error page. Explicit page-state detection and legitimate empty-bookmark creation remain | [Red proof](../../debt/assets/helper-recovery-2026-09-21/verification.md): both error-then-success fixtures resolved under the old helpers, and the persistent-error case retried; three assertions failed before the fix, then all 35 focused cases passed | High: zero runner retries alone cannot prevent a helper from hiding a product failure |
| Vitest timeouts, serialization, and dependency optimization | **KEEP** | Cold imports and shared integration state execute within measured deterministic bounds | Current configs still encode the remedies for the recorded cold-import, reload, and database races | N/A; change only with lane-specific measurements and red reproduction | Medium: nondeterministic lanes |
| pnpm supply-chain policy | **KEEP** | Unexpected lifecycle scripts and unaged dependency releases are refused | `pnpm-workspace.yaml` still owns `strictDepBuilds`, `allowBuilds`, overrides, and cooldown | N/A; replacement must preserve install-script and release-age refusal | Critical: supply-chain execution |
| Workflow action and reusable-workflow SHA pins | **KEEP — COMPLETE under DEBT-474 step 2** | CI executes reviewed third-party and reusable-workflow commits rather than mutable references | Every `uses:` entry in all four current workflows is SHA-pinned with a version comment, the CI YAML-aware contract covers workflow/job/step/reusable forms, and the renewal workflow has its own SHA-pin contract | N/A in Phase C; any future action change must update the exact SHA and pass the existing workflow contract | Critical: CI supply-chain execution |
| Node-version hook | **KEEP** | Local hooks run on the repository's supported Node major | `.husky/check-node-version.sh` guards machine-local shell initialization outside package-manager control | N/A; replacement must red-test a stale active Node version | Medium: local/CI divergence |
| Draft-question importer | **KEEP** | Draft content is parsed and validated before entering the canonical question corpus | The command remains an active content entry point with historical input-safety contracts | N/A; replacement must preserve parser and destination-safety failures | High: content corruption |
| Feedback export content-safety checks | **KEEP** | Exported user feedback cannot become formula-executable or lose required fields | The command still produces owner-consumed tabular data from user-authored content | N/A; database target routing is a separate `REPLACE` row below | High: personal-data or spreadsheet injection |
| Seed-environment fence | **KEEP** | Seed commands require the intended environment and production consent | Production and all-environment entry points still rely on the fence before mutation | N/A; replacement must independently prove local, preview, and production authority | Critical: wrong-environment seed |
| Skip-policy scanner | **KEEP** | Unapproved framework skip/only/todo controls fail the required lane | The current scanner recognizes explicit allowed forms and fails closed on an unknown receiver; its floor is zero | N/A; do not expand its cooperating-author threat model without a reachable red receipt | High: green without executing tests |
| Architecture semantic checks: resolved target, type-only exception, hook placement, page-model naming | **KEEP** | Imports and presentation modules obey semantics that raw specifier lint cannot infer | The architecture scan resolves targets and inspects import kind and placement/naming conventions | N/A; each semantic red case must survive any refactor independently | High: boundary erosion |
| Architecture syntactic import-specifier and filename checks | **REPLACED — COMPLETE 2026-09-21, #964/#965** | Direct forbidden specifiers and invalid filenames fail locally and in CI | Biome 2.5.12 handles ordinary import specifiers and filenames. Installed lint misses the two documented literal-call forms, so the existing scanner retains a bounded fallback plus resolved-target/type-only/placement/naming semantics; no new scanner | [Red proof](../../debt/assets/biome-architecture-2026-09-21/verification.md): initial real-lint contracts failed 23 cases; four fallback cases failed against the old scan. Each retained semantic mutation failed independently; restored focused suites pass 68/68 | Medium: installed-rule gaps must not become an unguarded deletion |
| Raw JSX `<button>` scanner half | **REPLACED — COMPLETE 2026-09-21, #962/#963** | Production JSX uses the existing component-system button exception policy | Biome `noRestrictedElements` replaces the general raw-JSX scan with the same source scope and exclusions. The mobile-nav exact-one count, opacity policy, and render tests stay; neither old nor new general guard covers `createElement` | [Red proof](../../debt/assets/biome-raw-buttons-2026-09-21/verification.md): both forbidden real-lint fixtures fail before the rule; disabling the retained count contract fails its zero/two fixtures. After the missing-source coverage follow-up, 35 focused cases pass | High: preserved exceptions must not become broad bypasses |
| Typed server-span family guard | **REPLACE — COMPLETE 2026-09-20, #929/#930** | Named spans use registered families and runtime-filtered initial/later attributes | Six callers now use `startServerSpan`; only the wrapper and four approved setup/reporting files may import the SDK. The unchanged projector remains runtime authority; the 469-line scanner is removed | Red: real-lint restriction removed → 1 failure; widened family/field types → 3 typecheck failures; initial/later runtime filtering removed → 3 failures. Restored focused suites: 112 passed. Six literal metadata cases and a callback/family comparison prove the migration's unchanged telemetry. [Proof ledger](../../debt/assets/server-tracing-2026-09-20/verification.md). | High: sensitive telemetry or observability drift |
| Production 350-line warning (former `check-file-size.sh`, self-test, and lint-staged entry) | **DELETE — COMPLETE 2026-08-27** | It attempted to provide an observational maintainability signal | The script, self-test, and lint-staged entry are absent; the post-delete source-reference check is empty | Completed: the pre-delete lint-staged absence assertion failed red, the post-delete assertion passed, and unit stayed green while moving from 465 / 4,235 to 464 / 4,234 exactly as the removed self-test predicts. No replacement or metric gate was added. | Low: non-actionable warning removed |
| Test-file Biome warning at 800 and DEBT-469 suppressions | **KEEP** | New oversized test files are stopped at the existing ratchet while named legacy splits stay visible | `biome.json` and the DEBT-469 comments implement the repository's current warn-at-800 policy | N/A in Phase C; changing the threshold or severity is a separate approved policy decision | Medium: unbounded test growth |
| Duplicate local-E2E Docker readiness layer | **REPLACED — COMPLETE, #939; production ancestry verified 2026-09-21** | E2E starts only after its resolver-scoped database is healthy and fails with a bounded, value-free error | E2E delegates to `run-local-test-db.ts up`, which uses Compose `--wait --wait-timeout 60`; the duplicate helpers and their self-test are removed | [Red-proof ledger](../../debt/assets/debt-475-e2e-infrastructure-2026-09-20/verification.md): timeout/delegation contracts failed against the old command; real disposable Compose fixtures distinguish healthy and timed-out outcomes; mutations prove failure propagation and stop-before-migration | High: protected by the surviving readiness contracts |
| Integration/E2E database connectivity probes | **KEEP** | Each lane proves that the selected target accepts a real query after orchestration | Integration setup and E2E health check run against their resolved database; the migration ledger is a different property and may refer to a different target | N/A; removing either probe requires a red case showing equivalent same-target connectivity at the same boundary | High: orchestration succeeds while the lane target is unusable |
| Required-CI bash credential validator | **DELETE — COMPLETE in #842** | It duplicated preflight detection before E2E | Current `ci.yml` no longer contains the step; the Playwright setup remains fail closed | Existing workflow test must keep the duplicate step absent and E2E preflight required | Low: completed disposition |
| Subscription-repository contract-coverage self-test | **DELETE — COMPLETE 2026-08-27** | It attempted to prove the five-method contract table was exhaustive | The self-test is absent; the remaining compile-time `Exclude` assignment is exhaustive over the port keys | Completed: omitting `findByExternalSubscriptionId` failed typecheck at the exhaustive assignment, restoration passed, and the focused fake contract passed 8/8 before only the self-equality test was removed | Low: redundant test only |
| `seed-all-environments.sh` trampoline | **DELETE — COMPLETE 2026-08-27** | It forwarded arguments and exit status to the TypeScript entry point | The shell file is absent; `db:seed:all` invokes `seed-all-environments.ts` directly, and both TypeScript entry points remain | Completed: the direct-entry assertion failed 1/7 on the shell command before deletion, then passed 7/7; an unsupported-option process case proves argument-specific output and exit status 1 propagate through the package command | Low: command breakage |
| `seed-production.ts` entry point | **KEEP** | Production seeding requires its explicit production runner and consent path | It imports the production-specific runner rather than selecting authority through a runtime flag | N/A; do not merge entry points in Phase C | Critical: production seed authority |
| `seed-all-environments.ts` entry point | **KEEP** | The deliberate all-environments workflow remains separately named and testable | It imports the all-environment runner and is the direct target that will replace the shell trampoline | N/A; do not merge entry points in Phase C | High: environment fan-out |
| CRAP coverage-map validator | **KEEP** | Malformed Istanbul maps fail as infrastructure errors instead of producing authoritative-looking rankings | Focused cases reject negative, missing, extra, and reversed-range data that `createCoverageMap` accepts | N/A; replacement must reproduce every malformed-map red case | High: false quality report |
| CRAP CLI argument parser | **KEEP** | Invalid observational-report configuration fails explicitly | The parser is small, local, and has no demonstrated defect or replacement benefit | N/A; revisit only with a failing receipt | Low |
| Bookmark-only reset helper | **DELETE — COMPLETE 2026-08-28** | The surviving general reset proves exactly one bookmark overall and exactly one for `placeholder01Id` | The orphan helper, both self-tests, and its scanner allowlist entry are absent; no non-documentation reference remains | Completed: a total-count-one / placeholder-count-zero case failed 1/24 before the general verifier received fixture IDs and checked both counts; the focused suite then passed 24/24 | High: protected by the surviving target-specific postcondition |
| Unused `lockPath` target field | **DELETE — COMPLETE 2026-08-27** | None; the rejected locking design never consumed it | The field and path-building imports are absent, and no non-documentation consumer remains | Completed: the exact target-shape test failed 1/14 on `lockPath` as the sole extra property, then passed 14/14 after deletion with typecheck green | Low |
| `export:feedback` database target path | **REPLACE — COMPLETE 2026-08-29** | Feedback export accepts only an explicitly supplied, human-authorized database target | The command has no dotenv load and delegates target refusal, remote acknowledgement, and redacted target logging to `runHumanDatabaseCommand` before it creates SQL | Completed: three former-path cases failed red for accepted dotenv fallback, accepted unacknowledged remote access, and a missing local target receipt; the three boundary suites then passed 43/43, and the package entry with authority variables absent exited 1 before SQL creation | Critical: personal-data export from wrong database |
| `db:generate` database target path | **REPLACE — COMPLETE 2026-08-29** | The supported migration-generation entry must use an explicitly supplied, human-authorized database target | `pnpm db:generate` routes through `db-generate.ts` and the existing human database-command guard before Drizzle Kit loads its fallback-capable config, then forwards caller-supplied Drizzle Kit options | Completed: the direct-execution mutation failed all three missing-URL, unacknowledged-remote, and redacted-approved-target cases; a dropped-`--name` case failed 1/4 before option forwarding, the focused boundary suites passed 26/26, and the real package entry with authority variables absent exited 1 before Drizzle Kit started | High: migration generated from wrong schema |
| Runtime `npx vercel env pull` in seed setup | **REPLACED — COMPLETE, #948/#959; production ancestry verified 2026-09-21** | Seed setup obtains the intended remote environment only through an explicitly reviewed CLI version | Runtime requires preinstalled exact 59.16.0; no npx/dlx fallback, manifest dependency, lockfile change, build allowance, trust exception, or vendor override. The production caller-URL refusal remains intact | [Red proof](../../debt/assets/vercel-cli-pin-2026-09-20/verification.md): ten assertions failed for the old command/missing check; all 30 runtime/target cases pass after the fix. A real version probe refuses this clone's 59.23.2 before env pull. Operator provisioning of the reviewed CLI is an explicit prerequisite | High: owner-run tooling still carries accepted transitive risk; re-evaluate if used in CI/runtime |
| Fidelity scan placement in Vitest `globalSetup` | **REPLACE — COMPLETE 2026-08-29** | Test-double drift remains blocking without taxing every unit invocation | `pnpm lint` and required CI now run the existing live scanner through `pnpm lint:doubles`; Vitest has no fidelity `globalSetup`, while all three policies and floors remain | Completed: four focused boundaries failed red before the command/CI/config replacement; the real package command exited 1 on a temporary forbidden fixture and passed on the clean tree. Three-run unit medians moved from 38.07 to 29.43 seconds; the explicit scan measured 8.93 seconds | Medium: protected by package, CI-order, config-absence, and forbidden-fixture contracts |
| Own-code module-mock classifier | **REPLACE — COMPLETE 2026-09-19** | Own-code `vi.mock`/`vi.doMock` permits only one argument or a second argument that is exactly the object literal `{ spy: true }` | Replaced alias/data-flow tracing with a bounded syntax check; unknown, indirect, computed, asserted, extra-property, and extra-argument forms fail closed. Scan globs and all floors are unchanged; DEBT-472 still owns setup-file migration before widening | Red proof: 27 existing/new assertions failed against the old classifier; mutation probes independently rejected the four allowed API/form pairs and admitted the two forbidden factory forms before the unchanged tests passed against the replacement. This dated completion supersedes only this row's earlier REPLACE status | High: mock-policy bypass |
| Unknown-cast per-file ratchet | **KEEP** | Existing `as unknown as` debt cannot grow while DEBT-472 migration is incomplete | Current floors identify the accepted legacy sites and fail on per-file growth | N/A until DEBT-472 step 4 disposes every recorded site; never raise a floor | Medium: type-boundary erosion |
| Maintained-port hand-rolled-double scan | **KEEP** | Tests cannot bypass a maintained fake with an untracked object literal | The type-aware scan keys off maintained fake exports and protects behavior seams | N/A; removal requires completed DEBT-472 dispositions plus equivalent red contracts | High: fake/adapter divergence |
| Fake-contract register guard | **KEEP** | Every maintained fake appears once with verification, divergences, and a valid waiver date where used | The current scanner enforces all four properties; a barrel-only membership test would not | N/A; any replacement must first fail on duplicate, missing, blank-cell, and invalid-date fixtures | High: undocumented divergence |
| Shared source acquisition helper | **KEEP separate — conditional extraction not selected, 2026-09-21** | Requested sources retain their existing acquisition contracts and policy-owned scopes | Architecture/theme readers have short read/sort mappings but different return shapes; the skip reader also owns a named error contract. No shared primitive with demonstrated net deletion was produced | No deletion is licensed: the table's net-deletion prerequisite is unmet. Preserve the existing readers and tests; do not introduce a generalized walker to satisfy a checkbox | Medium: speculative abstraction would create shared blind spots |
| `@stripe/cli` version guard | **REPLACE — COMPLETE 2026-08-27** | The CLI dependency remains exact SemVer and every bump is exercised against the live fixture behavior | The manifest value is tested against a local exact-SemVer recognizer; no test duplicates the current version, and the provider contract remains the behavior proof | Completed: six non-exact forms failed red while the old literal guard remained; the replacement passed 22/22, then stayed green 21/21 after only the duplicated literal assertion was removed | Medium: false CI red or parser drift |
| Hard `security.txt` future-expiry assertion | **KEEP** | Expired security contact metadata fails closed | `tests/security-txt.test.ts` checks the RFC-required future `Expires` value | N/A; never replace with warning-only behavior | High: stale vulnerability contact |
| Ninety-day `security.txt` warning | **REPLACED — COMPLETE, #944/#959; hosted proof 2026-09-21** | It provides renewal lead time before the hard expiry without weakening that test | `.github/workflows/security-txt-renewal.yml` invokes the dependency-free script monthly; one titled issue is created, updated, or reopened as needed | [Red-proof receipt](../../debt/assets/security-txt-renewal-2026-09-20/verification.md): five assertions fail when the threshold, issue identity, failure exit, or write permission is broken; current renewal plus unchanged hard-expiry suites pass 42/42 (41 + 1), including the review additions. The initial 22/22 result remains historical in the linked receipt. Manual main workflow `35552383175` succeeded with `security.txt renewal: not-due`; no issue was due. Actions are SHA-pinned and only the renewal job has issue-write permission. | Low: monthly polling is not notification on the exact threshold date; issue delivery is not proof a human read it |
| Local E2E orchestrator child lifecycle | **REPLACED — COMPLETE, #939/#942; production ancestry verified 2026-09-21** | On macOS/Linux, `SIGINT`/`SIGTERM` terminate the active command and its existing descendant process groups when OS signaling succeeds | The runner attempts every owned group, retains escalation after partial initial failure, and preserves the first error; Windows retains the documented direct-child fallback | [Red-proof ledger](../../debt/assets/debt-475-e2e-infrastructure-2026-09-20/verification.md): both signals failed on the old runner and detached-server fixture failed on group-only cleanup; final fixtures prove termination, escalation, unrelated-process survival and listener cleanup. `reuseExistingServer: false` stays | High: #942 repaired the [mixed-group signal errors](../../debt/assets/debt-475-e2e-infrastructure-2026-09-20/signal-error-review.md); permanently unsignalable groups and forced runner termination still require the documented orphan check |
| Reviewer/guard implementation standard | **KEEP in `AGENTS.md`; do not duplicate** | Review findings expand a guard only when a failing or reachable property receipt justifies the change | `AGENTS.md` already says a finding is a claim, unknown inputs fail closed, and reviewer severity is not authority | N/A; debt-index prose must not become a competing source of truth | Medium: recurring complexity arms race |
| GitHub `CRON_SECRET` header check | **TRACKED UNDER DEBT-474 — owner ruling implemented; external receipt pending** | Cron secrets are validated where consumed, not by an unused Actions copy | #919 removed the CI step; package A deleted the Actions secret and rotated all three Vercel scopes. DEBT-474 still requires the post-rotation scheduled 200 receipts | No remaining DEBT-475 implementation; do not close DEBT-474 without its own external proof | High if operational evidence is inferred rather than observed |

### Promotion review: mixed-group signal errors (2026-09-20)

Forward pointer: the historical findings below were repaired in #942; the current disposition and closeout ledger above record the verified release.

**CONFIRMED:** at `c1463bd6`, the first non-`ESRCH` error aborts the group loop (`scripts/e2e-local-orchestrator.ts:216-230`), and an initial-signal error clears the escalation timer (`:264-269`, `:206-208`). A disposable real parent plus detached descendant, with `EPERM` injected only for the root group, leaves the descendant alive after either initial or escalated signal failure. The existing error tests (`scripts/e2e-local-orchestrator-failure.test.ts:136-155`) check error identity and listener removal, not cleanup of a later independently signalable group. [Exact probe, output, limitations, and required red cases](../../debt/assets/debt-475-e2e-infrastructure-2026-09-20/signal-error-review.md).

**UNPROVEN:** a real permission denial in ordinary same-user local/CI execution; the receipt injects that OS fault. **OVERSTATED:** the unqualified claim that a detached server cannot survive interruption, and the suggestion that collecting errors inside the loop alone repairs the initial-error path. That path must also retain bounded escalation. The future fix belongs to this existing lifecycle row, not a new debt ID: attempt every owned group, retain escalation after a partial initial failure, and preserve the original failure after cleanup. Never claim termination of a group whose OS permissions continue to reject every signal. Normal success, `ESRCH`, early-parent-exit, unrelated-process survival, Windows scope, and listener cleanup must remain covered. Runtime files are unchanged in this documentation correction; promotion remains blocked pending the owner-authorized implementation and review.

### Execution protocol

1. This disposition-table PR changes documentation only. It must pass the full gate and exact-head CodeRabbit review before any mechanism is touched.
2. After it lands, execute one `DELETE` or `REPLACE` row per PR, smallest and lowest-risk first. Replacement contracts land red before the old mechanism is removed.
3. No Phase C PR adds a new scanner, generalized walker, resolver, or lifecycle framework. The optional source helper is limited to requested-path read/sort/fail-closed acquisition; policies retain their own globs.
4. No threshold or severity changes, and no new metric gate, without an ADR-019 amendment. The test-file 800 rule is unchanged.
5. `OWNER RULING NEEDED`, `KEEP`, completed, and deferred rows do not enter the implementation queue. A new direction requires a dated table amendment reviewed before code.
6. Every mechanism PR states the protected property, attaches the focused red receipt, records the before/after surface, runs the full gate, and promotes only after exact-head approval and tree identity.

## Verification

**Architecture replacement (2026-09-21).** The table's Biome-only assumption did not hold for `require()` and template-literal import calls under installed 2.5.12. Those two documented forms stay in the existing semantic scanner after four red regressions; ordinary specifier checks and filename policy move to Biome, with the ten current filename exceptions unchanged. The scanner shrinks by 79 lines net; no generalized resolver or walker is added. [Executable proof and release status](../../debt/assets/biome-architecture-2026-09-21/verification.md). The raw-button focused count below is corrected from the initial 34 to final 35 after the coverage follow-up; #962/#963 are merged and [production-verified](https://github.com/The-Obstacle-Is-The-Way/naltrexone-university/pull/963#issuecomment-5754884564).

**Raw-JSX replacement (2026-09-21).** The existing Biome 2.5.12 [element restriction](https://biomejs.dev/linter/rules/no-restricted-elements/javascript/) now owns the syntactic ban. The scanner has a 53-line net reduction while retaining the exact-one mobile-nav exception; no new scanner, exception, opacity policy, UI code, or metric gate is added. Actual-lint contract tests replace the deleted synthetic scanner bypass case and cover both source roots, primitives, the existing app-shell exception, and all four existing excluded test suffixes. Review, hosted CI, and production receipts remain separate from the [focused proof](../../debt/assets/biome-raw-buttons-2026-09-21/verification.md).

- This docs-only table covers every mechanism named in the earned list and F1–F9 with an explicit disposition; no implementation claim is derived from a percentage or line count.
- Every relative link resolves, every cited current-tree path exists, and this record contains no literal local test-database host/port.
- `pnpm typecheck && pnpm lint && pnpm test --run && pnpm test:browser && pnpm db:test:up && pnpm test:integration && pnpm build && pnpm test:e2e` is green on the table head; record actual lane counts rather than carrying filing-era counts forward.
- CodeRabbit approves the exact table head with zero unresolved threads; the promotion produces identical `origin/dev` and `origin/main` trees and green post-merge CI/deploy.
- For each later mechanism PR, verification is the focused red proof named in its row plus the full gate. A deleted-file zero-reference grep is supporting evidence only; it never substitutes for behavioral equivalence.

## Related

- Parents and siblings: [DEBT-473](./debt-473-green-without-evidence.md) (F9 convention, F10 local-run fail-opens, step 4 wrapper), [DEBT-474](./debt-474-ci-secret-scope-and-action-immutability.md) (secret scope; owns F8's `CRON_SECRET` step), [DEBT-472](./debt-472-test-double-fidelity-and-contract-discipline.md) (owns the doubles the F5 scan counts), [DEBT-469](../../debt/debt-469-toolchain-warning-debt.md) (owns the remaining legacy size suppressions), [DEBT-465](../../debt/debt-465-test-quality-practices-adoption.md) (owns `crap-report.ts`).
- The ruling: [AUDIT-012](../audits/audit-012-repo-org-devx.md) §Resolution Decisions (2026-06-13).
- Origins of the earned mechanisms are linked in the table above; origins of the over-builds: [DEBT-411](./debt-411-local-e2e-flakiness-and-error-masking.md) PR-B (`06671f7b`), [DEBT-417](./debt-417-multi-clone-local-test-infrastructure-isolation.md) (`be50dda7`), DEBT-281 (`108edadc`), [DEBT-224](./debt-224-file-size-audit-production-and-test.md)/[DEBT-234](./debt-234-add-max-lines-lint-rule.md) (`a26c45cb`), [DEBT-370](./debt-370-oversized-test-files-without-enforced-size-rule.md), [DEBT-462](./debt-462-observability-instrument-gap-parked-triggers.md) (`2fa6e1a0`).
- Shape of the repository a reviewer sees first (measured at `5a095f12`): 49,628 production LOC; 168,014 test LOC (3.4×); 144,617 lines of `docs/*.md` in 968 files (2.9×); 479 debt documents in seven months. The ratio of process artifacts to product is the tell that an agent fleet wrote this repository; the code itself is not. This debt is about making the tooling match the code's standard.

## 2026-09-20 adversarial retry follow-through

**CONFIRMED:** #932 enforces setup-only **Playwright attempt** retries; the [production closeout receipt](../../debt/assets/e2e-retries-2026-09-20/verification.md#production-closeout--2026-09-20-1453z) confirms its promotion. The helper receipts below were rechecked against `dev` `fc0049f7` after #934: that PR retains local failure traces but does not remove helper recovery. **OVERSTATED:** the former claim that this prevents every product failure from becoming green. `tests/e2e/helpers/session.ts:340-367` clicks a visible `Try again` up to twice; `tests/e2e/helpers/bookmark.ts:181-215` revisits visible errors up to three times. `bookmark.test.ts:598` explicitly accepts `['error', 'populated']` and asserts two visits; the focused review run passed 69/69 tests. Recovery inside one attempt is invisible to Playwright flaky counts. The session helper's dev-compilation rationale is stale against `playwright.config.ts:52-53` (build/start). This is a reachable counterexample, not an observed production flake. [Full receipt and #932 gate review](../../debt/assets/adversarial-2026-09-20/review.md#pr-932).

**Separate next mechanism:** ordinary product success-path helpers must expose the first visible unexpected product error. Keep deliberate recovery-behavior tests separate and explicit. Before implementation, change the error-then-success fixture's expectation to rejection and add the equivalent first-question-error case; both must fail against today's helpers. Then remove implicit recovery from those ordinary paths. Reintroducing either loop must fail the corresponding boundary test. Do not add `failOnFlakyTests` as a purported fix for same-attempt recovery, broaden setup retries, or delete the error witness without replacement. Keep #932's narrow runner improvement; this follow-through does not alter it in the docs pass.

**Implementation follow-through (2026-09-21; review/promotion pending).** The two named recovery loops above are removed after the required red proof. First-question failure now throws with redacted rendered-alert text rather than clicking Try again; bookmark-page failure throws on the first error rather than navigating again. The existing state detector still exposes `error` for explicit error-behavior tests. All 35 focused helper cases pass, including normal session progress, navigation diagnostics, and creating a bookmark from an empty page. No application code, Playwright retry policy, skip, allowlist, or ratchet floor changed. This is a scoped helper fix, not a claim that every possible product error is globally observed. The record stays Open for the remaining disposition rows; [the proof ledger](../../debt/assets/helper-recovery-2026-09-21/verification.md) distinguishes local proof from pending review and release evidence.
