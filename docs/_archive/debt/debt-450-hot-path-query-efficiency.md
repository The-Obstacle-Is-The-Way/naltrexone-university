# DEBT-450: Hot-Path Query Efficiency — Finalize Statement Amplification, History/Bookmark Over-Hydration, Lifetime Counts, and Duplicate Window Ranking

**Status:** Deferred / resolved in part
**Priority:** P3
**Date:** 2026-07-09
**Re-verified accurate against `ddad8eee` on 2026-07-18.**
**FW-4 disposition:** 2026-07-22 — Parts 2 and 3a are resolved with the consumer-shaped history summary and relation-free/IDs-only/existence bookmark reads. Parts 1, 3b, 4, and 5 remain **PARKED** behind their named finalize latency/statement, bookmark census/latency, dashboard aggregate, and attempted-question rank/plan triggers; no parked query, pagination, or index work shipped. Re-verified against `456023e3b9c07edf774d48f10c361e7556e70ec6` before this disposition.

---

## Direction (2026-07-20 forest review)

| Part | Verdict | Chosen option | Rejected as disproportionate | One-line rationale |
| --- | --- | --- | --- | --- |
| 1. Exam-finalize `4F` loop | **PARK** | Option 3: make no query rewrite until sampled Sentry server tracing names a `finalizeExamAnswers`/outer-transaction span and either records a 30-second timeout or, over at least 100 production finalizations (or 30 days, whichever comes first), shows p95 at or above 3 seconds with the loop as the dominant span. Neon Query Analytics or slow-query logging must supply the companion statement evidence before choosing a SQL shape. | Option 1's set-based rewrite and Option 2's transaction-bound bulk method before that evidence. | (a) Avoids replacing the app's most invariant-dense transaction with bulk machinery; (b) `4F` is reachable but duration is unmeasurable while tracing is `0`; (c) Blast radius: a large finalize may hold locks or approach the request cap. Cure cost: re-proving BUG-254 flush, omitted outcomes, conflict mapping, row-count/version checks, end semantics, and whole-transaction retry is larger; (d) no dead code or duplication is removed; (e) applies DEBT-349's named-instrument gate. |
| 2. History hydration | **FIX (Option A, minimal form)** | Add one history-summary read port returning at most `S` rows; aggregate ordered state question IDs and answered/correct counts per session, compare the ordered IDs with parsed `params_json`, preserve DEBT-433 skip/log behavior, and join/project only the position-0 published slug. The fake must implement the same summary semantics. | Option B's column-only projection, which still transfers `ΣQᵢ` rows; Option C's wait-for-metrics acceptance. | (a) Replaces entity over-hydration plus a relation-heavy follow-up with the consumer's actual read model; (b) 20,000 state rows for 100 summaries is arithmetically reachable; (c) Blast radius: one bounded history request can transfer/map 20,000 fourteen-column rows. Fix cost: one client-owned port method, one adapter query, and fake parity; (d) keeps the corrupt-row invariant instead of weakening it; (e) does not create a general query abstraction. |
| 3a. Bookmark read width | **FIX (Option B, width-first minimal form)** | Keep current ordering/cardinality but give the page a relation-free bookmark/question summary read, the practice hook an IDs-only read, and the single-question review hook the existing `exists`-shaped read; no path may hydrate choices/tags or unused question fields for membership/summary output. | Option A's pagination/cursor/product change bundled into the width fix; retaining one wide DTO for all three consumers. | (a) Deletes demonstrably discarded relation work with consumer-shaped reads; (b) all three over-wide consumers are live; (c) Blast radius: every bookmark load, including retries, transfers/maps content it cannot use. Fix cost: three narrow reads at existing boundaries, with no UI state machine; (d) one port per actual contract is cleaner than a misleading shared DTO; (e) pagination stays a separately measured decision. |
| 3b. Bookmark pagination | **PARK** | Do not add page/cursor state now. Revive when the monthly [read-only Neon bookmark-cardinality census](../../dev/bookmark-cardinality-census.md) shows any user at 500 bookmarks, or after sampled Sentry tracing of `getBookmarks` records p95 at or above 300 ms for 7 consecutive days after Part 3a lands; the census and Sentry span are the named instruments. | Option A's cursor/order/UI pagination machinery before either trigger; permanent no-pagination acceptance without a measurement boundary. | (a) Defers new client/server state; (b) the catalog bound is real but current `B` and latency are unknown; (c) Blast radius: a very large collection can make one page slow and hard to scan. Cure cost: cursor contracts, URL/UI state, and product decisions exceed the unmeasured harm; (d) width waste is fixed independently; (e) follows the same DEBT-349 boundary as Parts 1/4/5. |
| 4. Dashboard lifetime aggregates | **PARK** | Measure first. Revive Option 2's consolidation-only form when sampled Sentry `getUserStats` DB spans plus Neon Query Analytics show either p95 at or above 300 ms for 7 consecutive days or the two lifetime aggregates consuming at least 20% of dashboard DB time; then use one conditional aggregate and add no index without evidence from the [sanitized `EXPLAIN (ANALYZE, BUFFERS)` capture](../../dev/sanitized-explain-capture.md). | Option 1's broader four-count/visibility/index redesign; Option 3's rollup/cache state; any speculative index. | (a) Avoids a new stats port/index/cache absent pain; (b) `A + C` growth is arithmetic, not production evidence; (c) Blast radius: a long-lived user's dashboard counts may slow as surviving attempts grow. Cure cost: query/port/index semantics and visibility parity are larger today; (d) no current correctness or dead-code issue exists; (e) names both the latency and plan instruments required by DEBT-349. |
| 5. Duplicate latest-attempt ranking | **PARK** | Measure first. Revive only when sampled Sentry `getAttemptedQuestions` DB spans plus Neon Query Analytics show p95 at or above 300 ms for 7 consecutive days or the two rank statements consume at least 20% of that route's DB time, and the production-like [sanitized `EXPLAIN (ANALYZE, BUFFERS)` capture](../../dev/sanitized-explain-capture.md) confirms duplicate ranking/sort work is dominant. | Option A's single-statement materialized/lateral result shape before evidence; Option B's wider index or `DISTINCT ON` rewrite without a plan; Option C's keyset/product-total redesign. | (a) Avoids a complex result shape and speculative index; (b) `2Aᵥ` is proven but plan/latency are not; (c) Blast radius: deep history can spend twice on ranking before paging. Cure cost: out-of-range-total semantics, every filter, planner behavior, and pagination policy must be re-proved; (d) query-construction duplication was already removed by DEBT-214; (e) one measurement law governs all scale-only query work. |

Pure waste removal is allowed without telemetry only when the replacement is bounded and consumer-shaped: Part 2 removes row cardinality and Part 3a removes unused width. Finalize, pagination, lifetime aggregation, ranking, and all index work remain parked because Sentry tracing is disabled and no query analytics currently supplies the missing production evidence. DEBT-433's corrupt-row skip/log contract and Wave A's no-speculative-maintenance ruling remain unchanged.

## Description

Five source-verifiable query-efficiency gaps exist on authenticated exam-finalize, history, bookmark, and dashboard paths. The repository proves statement/row cardinalities and query shapes; it does not contain production query plans, byte counts, p95 latency, or Neon compute measurements. The direction review fixes Part 2's bounded 20,000-to-100 hydration mismatch and Part 3a's demonstrably discarded width, while Parts 1, 3b, 4, and 5 remain parked behind named measurements. P3 remains justified by the two surviving bounded hot-path fixes; the parked work is unmeasured P4 scale/product scope.

### 1. Exam finalize executes up to `4F` sequential loop statements (`800` when all 200 questions need finalization)

Let `F` be the number of session states that still need an attempt and state finalization. [`FinalizeExamAnswersUseCase`](../../src/application/use-cases/finalize-exam-answers.ts#L212) processes those states sequentially; a state with no draft and an already-finalized `latestSelectedChoiceId` skips at [lines 219–220](../../src/application/use-cases/finalize-exam-answers.ts#L219). Each of the remaining states awaits two repository calls:

1. `tx.attempts.insert` ([lines 223–241 and 252–270](../../src/application/use-cases/finalize-exam-answers.ts#L223)) emits one `INSERT … RETURNING` statement ([`drizzle-attempt-repository.ts:170-188`](../../src/adapters/repositories/drizzle-attempt-repository.ts#L170)).
2. `tx.sessions.finalizeDraftAnswer` delegates to [`updatePracticeSessionQuestionState`](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L494). Because the repository is bound to the outer transaction, Drizzle 0.45.2 implements its nested `transaction()` with postgres.js `savepoint()` ([Drizzle's installed-version source](https://github.com/drizzle-team/drizzle-orm/blob/0.45.2/drizzle-orm/src/postgres-js/session.ts#L203-L215)). The updater then performs one snapshot `SELECT` and one version-checked `UPDATE … RETURNING` ([`practice-session-question-state-updater.ts:137-177`](../../src/adapters/repositories/practice-session-question-state-updater.ts#L137)). postgres.js 3.4.9 emits the opening `SAVEPOINT` but, on success, does not emit `RELEASE SAVEPOINT`; only the outer scope commits ([postgres.js v3.4.9 source](https://github.com/porsager/postgres/blob/v3.4.9/src/index.js#L251-L289); exact Drizzle and postgres.js versions are pinned at [`pnpm-lock.yaml:3531`](../../pnpm-lock.yaml#L3531) and [`pnpm-lock.yaml:4821`](../../pnpm-lock.yaml#L4821)). This leg is therefore three successful statements, not four.

The successful loop cost is consequently `F × (1 INSERT + 1 SAVEPOINT + 1 SELECT + 1 UPDATE) = 4F` sequential driver statements. With [`MAX_PRACTICE_SESSION_QUESTIONS = 200`](../../src/adapters/shared/validation-limits.ts#L34), `F ≤ 200`, so the loop maximum is `800`, not the previously claimed `~1,000`. That count excludes fixed transaction/session reads, the set-based drafted-question fetch at [lines 204–210](../../src/application/use-cases/finalize-exam-answers.ts#L204), `sessions.end`, `BEGIN`/isolation/`COMMIT`, and the optional single-question BUG-254 flush. An error can add rollback statements. A retryable `40001` or `40P01` restarts the whole outer REPEATABLE READ transaction up to three attempts with 25–250 ms capped jitter ([`lib/container/use-cases.ts:41-108`](../../lib/container/use-cases.ts#L41)), so completed loop work in the failed attempt is discarded and repeated.

The row locks acquired by repeated state updates and the uncommitted attempt inserts remain until the outer transaction completes, widening the conflict window as `F` grows. The app segment has [`maxDuration = 30`](<../../app/(app)/app/layout.tsx#L20>), but the repository contains no production per-statement latency or finalize-duration distribution; it cannot establish the prior “3–6+ seconds” estimate or that cap proximity occurs in production. The direction and `4F` count are source facts. Archived [DEBT-441](../_archive/debt/debt-441-updater-dead-stale-retry-paths-under-rr.md) documents why the inner stale retry is dead in this transaction context, not this statement cost. The 2026-06-23 “no counted N+1” audit concerned set-based question reads and predates PR #537's normalized per-question write shape.

### 2. A history page can hydrate 20,000 fourteen-column state rows to return at most 100 summaries

[`findCompletedByUserId`](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L300) first fetches at most `S` session rows, then calls [`loadQuestionStateRowsBySessionIds`](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L113), whose unqualified `select()` returns all 14 columns from every matching `practice_session_question_states` row. If session `i` has `Qᵢ` questions, that statement returns `ΣQᵢ` rows. The production action caps `S` at [`MAX_PAGINATION_LIMIT = 100`](../../src/adapters/controllers/practice-schemas.ts#L112), and persisted session `questionIds` cap each `Qᵢ` at [`MAX_PRACTICE_SESSION_QUESTIONS = 200`](../../src/adapters/repositories/practice-session-params.ts#L31), so the current application-contract maximum is `100 × 200 = 20,000` state rows. The default page limit is 20, hence at most 4,000 under the same contract ([`history-search-params.ts:47-50`](<../../app/(app)/app/history/history-search-params.ts#L47>)). The database has no cross-table constraint enforcing the 200 count by itself; the strict persisted-params parser and read mapper enforce the application contract.

[`GetSessionHistoryUseCase`](../../src/application/use-cases/get-session-history.ts#L48) consumes that full entity graph only to derive `questionCount`, the first question ID, and answered/correct counts; [`computeSessionStats`](../../src/domain/services/session-stats.ts#L12) reads only `latestSelectedChoiceId` and `latestIsCorrect`. The state predicate can use the unique index whose leading column is `practice_session_id` ([`db/schema.ts:509-518`](../../db/schema.ts#L509)); the actual live plan remains unverified without `EXPLAIN`. The proven issue is row/column transfer and mapping amplification, not an N+1 query.

A smaller second amplification follows: up to `S` first-question IDs go through `findPublishedByIds`, which hydrates all question choices/tags, merely to populate a slug ([`get-session-history.ts:55-68`](../../src/application/use-cases/get-session-history.ts#L55); [`drizzle-question-repository.ts:125-139`](../../src/adapters/repositories/drizzle-question-repository.ts#L125)). The dashboard's `S = 3` call pays the same shapes at lower cardinality ([`dashboard/page.tsx:260-268`](<../../app/(app)/app/dashboard/page.tsx#L260>)). Any summary projection must preserve the current list behavior around corrupt normalized state: exact count/order drift throws inside mapping and that row is then skipped and logged ([`drizzle-practice-session-repository.ts:80-110`](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L80), [lines 336–355](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L336)); archived [DEBT-433](../_archive/debt/debt-433-question-order-invariant-unenforced-at-db-level.md) accepted this read-time invariant.

### 3. Bookmark loading has no request pagination and hydrates relation data unused by every consumer

[`DrizzleBookmarkRepository.listByUserId`](../../src/adapters/repositories/drizzle-bookmark-repository.ts#L52) returns all `B` bookmark rows for a user with no `LIMIT`. `B` is not literally unbounded: the `(user_id, question_id)` primary key and question FK bound it by the number of extant question rows ([`db/schema.ts:648-670`](../../db/schema.ts#L648)). It is, however, unpaginated at the request boundary and can grow with the question catalog.

[`GetBookmarksUseCase`](../../src/application/use-cases/get-bookmarks.ts#L29) passes up to `B` IDs to `findPublishedByIds`. That repository selects all nine question columns plus all choice and question-tag/tag relations ([`drizzle-question-repository.ts:40-47`](../../src/adapters/repositories/drizzle-question-repository.ts#L40), [lines 125–139](../../src/adapters/repositories/drizzle-question-repository.ts#L125)); each choice includes `textMd` and `explanationMd`, and each question includes its own `explanationMd` and `referenceMd` ([`db/schema.ts:320-365`](../../db/schema.ts#L320)). The output projection retains only question ID, slug, stem, difficulty, and bookmark timestamp for available rows ([`get-bookmarks.ts:39-55`](../../src/application/use-cases/get-bookmarks.ts#L39)). Thus the server hydrates up to `B` questions and all of their `Σchoices + Σtags` relation objects while discarding every relation and five of the nine question fields. Exact bytes and discard percentage depend on content and are not derivable from source; the prior “~3,000 rows, multiple MB, ~95%” example was unsupported.

Three production consumption paths—not two—share this DTO:

- The [bookmarks server page](<../../app/(app)/app/bookmarks/page.tsx#L235>) renders all rows with no pagination. It uses stem/difficulty/date but not choice/tag relations; discarded relation data travels database→function, not function→browser.
- [`usePracticeQuestionBookmarks`](<../../app/(app)/app/practice/hooks/use-practice-question-bookmarks.ts#L46>) is reused by practice surfaces and converts the response to a `Set` of all IDs. Its timeout retry policy permits the initial request plus two retries ([`practice-page-bookmarks.ts:6-8`](<../../app/(app)/app/practice/practice-page-bookmarks.ts#L6>), [lines 60–82](<../../app/(app)/app/practice/practice-page-bookmarks.ts#L60>)), so an ID-only consumer can request the same full stem-bearing DTO up to three times.
- The standalone review-question hook calls the same action and scans the full returned array merely to test whether one question ID is present ([`use-question-page-bookmarks.ts:97-120`](<../../app/(app)/app/questions/[slug]/hooks/use-question-page-bookmarks.ts#L97>)).

The two client paths receive the projected stems/difficulty/timestamps but not the already-discarded choice/tag objects; their over-fetch is narrower than the server's relation hydration.

### 4. Dashboard lifetime counts perform two unbounded aggregates through the session-visibility join (P4)

Every dashboard request starts `getUserStats` ([`dashboard/page.tsx:260-268`](<../../app/(app)/app/dashboard/page.tsx#L260>)). [`GetUserStatsUseCase`](../../src/application/use-cases/get-user-stats.ts#L76) concurrently requests total lifetime attempts and total lifetime correct attempts alongside bounded recent metrics ([lines 81–95](../../src/application/use-cases/get-user-stats.ts#L81)). Both lifetime methods delegate to [`countWhere`](../../src/adapters/repositories/drizzle-attempt-repository.ts#L317), which has no time predicate and must `LEFT JOIN practice_sessions` because the active-exam visibility condition reads session ID/mode/end state ([`active-exam-visibility.ts:5-20`](../../src/adapters/repositories/shared/active-exam-visibility.ts#L5)). The 7-day count methods add `answered_at >= since`; they are not part of this unbounded-lifetime claim.

Let `A` be the user's stored attempt rows and `C ≤ A` the correct subset. Before the active-exam visibility predicate removes anything, the two lifetime aggregates have candidate cardinalities `A` and `C`, for total work scaling with `A + C` (worst case `2A`), not an unspecified “large scan.” A purely illustrative 50-attempt/day user creates 18,250 attempts/year, so the two predicates can consider at most 36,500 attempt candidates after one year if every attempt is correct. This is arithmetic, not a measured workload.

The three user-leading indexes are `(user_id, answered_at)`, `(user_id, question_id, answered_at)`, and `(user_id, is_correct, answered_at)`; none contains `practice_session_id` ([`db/schema.ts:599-608`](../../db/schema.ts#L599)). Therefore none covers the visibility join's required attempt columns, and an attempt-side index-only plan is not available from those indexes alone. The actual planner choice and buffer cost depend on production statistics and visibility maps and are unverifiable from git. There is no application-level attempt prune/delete caller, but attempts can still disappear through the user/question `ON DELETE CASCADE` FKs and session deletion sets `practice_session_id` null ([`db/schema.ts:571-586`](../../db/schema.ts#L571)); “append-only” and “monotonically growing” were therefore overclaims. The accurate statement is that surviving-user history has no time bound or rollup on this request path.

### 5. History questions executes the per-user latest-attempt rank twice; an existing index is a near-match, not absent (P4)

[`listAttemptedQuestionsByUserId`](../../src/adapters/repositories/drizzle-attempt-repository.ts#L419) and [`countAttemptedQuestionsByUserId`](../../src/adapters/repositories/drizzle-attempt-repository.ts#L475) each build and execute [`latestAttemptRowsSubquery`](../../src/adapters/repositories/drizzle-attempt-repository.ts#L59). That subquery left-joins sessions for visibility and computes [`row_number() OVER (PARTITION BY question_id ORDER BY answered_at DESC, id DESC)`](../../src/adapters/repositories/shared/latest-attempt-rank-sql.ts#L4) across the user's visible attempt candidates before the outer filters and page limit. [`GetAttemptedQuestionsUseCase`](../../src/application/use-cases/get-attempted-questions.ts#L65) runs the count and list statements concurrently with `Promise.all` ([lines 76–84](../../src/application/use-cases/get-attempted-questions.ts#L76)). For `Aᵥ` visible attempt candidates, the two statements each rank `Aᵥ` rows: total window input is `2Aᵥ`, although concurrency means wall-clock latency does not simply double.

The prior statement that no index serves the partition order was false. [`attempts_user_question_answered_at_idx`](../../db/schema.ts#L603) is `(user_id, question_id, answered_at DESC)`, so equality on `user_id` matches the partition key and the leading order. It lacks the final `id DESC` tie-breaker and does not include `practice_session_id`/`is_correct`, so it is not a full covering/order match; only an actual `EXPLAIN` can establish whether PostgreSQL uses it and whether an incremental/full sort remains. The list query applies `LIMIT/OFFSET` only after ranking/filtering ([`drizzle-attempt-repository.ts:454-458`](../../src/adapters/repositories/drizzle-attempt-repository.ts#L454)); the controller permits [`MAX_PAGINATION_OFFSET = 10,000`](../../src/adapters/controllers/review-controller.ts#L18), so a deep list page can additionally discard up to 10,000 filtered latest-question rows after the rank work. Archived [DEBT-214](../_archive/debt/debt-214-drizzle-query-duplication-attempt-repository.md) removed duplicated query-construction branches; its scope did not merge these two runtime statements.

## Impact

No wrong result or demonstrated production outage is attributed to these five parts. Part 1 proves up to 800 sequential success-path loop statements plus fixed transaction work and full-transaction retry, but production duration/cap proximity is unmeasured. Part 2 proves up to 20,000 fourteen-column state rows under current input/persisted-session limits, plus relation-heavy first-question reads, to emit at most 100 summaries. Part 3 proves an unpaginated `B`-row bookmark read, relation hydration proportional to all referenced choices/tags, and ID-only client consumers receiving the wider DTO; exact bytes are unmeasured. Parts 4–5 scale with `A + C` and `2Aᵥ` attempt candidates respectively; production plans and latency are unverified. Severity remains P3 overall because Parts 2 and 3a are bounded, source-proven waste with proportionate fixes; Parts 1, 3b, 4, and 5 do not independently justify implementation before their measurement triggers.

## Proposed Resolution

**Part 1 — PARKED (Option 3, measurement first):** do not rewrite the finalize transaction. The revive trigger is a 30-second production timeout or at least 100 sampled production finalizations (or 30 days, whichever comes first) with Sentry `finalizeExamAnswers`/outer-transaction p95 at or above 3 seconds and the loop as the dominant span; Neon Query Analytics or slow-query logging must then identify the statement shape. **Options 1 and 2 are REJECTED BY DIRECTION REVIEW before that trigger:** both must re-prove BUG-254 flush, omitted outcomes, attempt-conflict mapping, row/version counts, terminal end semantics, and whole-transaction retry, so neither is a minimal unmeasured optimization.

**Part 2 — Option A (CHOSEN, minimal form):** add a history-specific summary read port returning at most `S` rows. Aggregate answered/correct counts plus `array_agg(question_id ORDER BY position)` per session; compare that ordered array with parsed `params_json.questionIds` so DEBT-433's exact count/order invariant and skip/log behavior remain intact. Project only the position-0 published question slug; the fake must return the same summary values, while the real adapter retains the corrupt-row skip/log behavior. **Option B is REJECTED BY DIRECTION REVIEW** because it retains `ΣQᵢ` row transfer; **Option C is REJECTED BY DIRECTION REVIEW** because the bounded 20,000-to-100 mismatch is already sufficient evidence for this contained read model.

**Part 3a — Option B (CHOSEN, width-first minimal form):** keep current bookmark ordering and cardinality but split the three read contracts: a relation-free page summary selecting bookmark timestamp plus published question ID/slug/stem/difficulty, an IDs-only membership read for practice, and the existing `exists(userId, questionId)`-shaped read for single-question review. Preserve unavailable bookmark rows. Choices, tags, and unused question columns must not be hydrated for any of these reads. **Option A's pagination/cursor portion is PARKED as Part 3b**, rather than bundled into the width fix; **Option C's permanent no-pagination acceptance and retaining one wide DTO are REJECTED BY DIRECTION REVIEW.**

**Part 3b — PARKED (pagination portion of Option A):** add no cursor, page URL state, or client pagination until the monthly [read-only Neon bookmark-cardinality census](../../dev/bookmark-cardinality-census.md) finds any user with at least 500 bookmarks or, after Part 3a, sampled Sentry `getBookmarks` spans show p95 at or above 300 ms for 7 consecutive days. Those are the named cardinality/latency instruments. Permanent no-pagination acceptance is not chosen; the product decision remains parked behind observable scale.

**Part 4 — PARKED (Option 2 only after measurement):** do not change the stats port or indexes now. Revive the consolidation-only form after sampled Sentry `getUserStats` DB spans plus Neon Query Analytics show p95 at or above 300 ms for 7 consecutive days or the two lifetime aggregates consuming at least 20% of dashboard DB time. The first landing then combines only lifetime total/correct in one conditional aggregate. **Option 1's four-count/visibility/index expansion and Option 3's rollup/cache are REJECTED BY DIRECTION REVIEW;** no index is allowed without the [sanitized `EXPLAIN (ANALYZE, BUFFERS)` procedure](../../dev/sanitized-explain-capture.md) on production-like cardinalities.

**Part 5 — PARKED (no SQL option chosen before measurement):** revive only after sampled Sentry `getAttemptedQuestions` DB spans plus Neon Query Analytics meet the 300 ms p95/7-day or 20%-of-route-DB-time trigger and the [sanitized `EXPLAIN (ANALYZE, BUFFERS)` procedure](../../dev/sanitized-explain-capture.md) shows duplicate rank/sort work is dominant. **Option A is PARKED for judgment against that measured plan; Option B's wider index/`DISTINCT ON` rewrite and Option C's keyset/total-count product change are REJECTED BY DIRECTION REVIEW absent the evidence.**

## Verification

- **Part 2 only:** unit/fake contract tests pin summary questionCount/answered/correct/first-slug values and exact ordered-ID drift skip/log behavior. A real-Postgres integration case proves the query returns one row per session, keeps `total` semantics, skips/logs a corrupt ordered-ID row, and does not issue the old relation-heavy first-question fetch.
- **Part 3a only:** repository/controller tests prove the page summary selects no choice/tag or unused question fields, practice receives IDs only, and single-question review performs the scoped existence check. Existing retry, unavailable-row, order, and membership behavior remains green; no pagination assertion is added while Part 3b is parked.
- **Parked Parts 1, 3b, 4, and 5:** no implementation verification is authorized before the stated triggers. When measurement is enabled, record the named Sentry/Neon evidence in this doc before selecting a production change; Part 3b uses the [monthly read-only bookmark census](../../dev/bookmark-cardinality-census.md), and Parts 4/5 use the [sanitized `EXPLAIN (ANALYZE, BUFFERS)` capture](../../dev/sanitized-explain-capture.md) before any index or query-shape work.

## Related

- [DEBT-441](../_archive/debt/debt-441-updater-dead-stale-retry-paths-under-rr.md) — nested-savepoint and dead inner-retry semantics used by Part 1.
- [DEBT-433](../_archive/debt/debt-433-question-order-invariant-unenforced-at-db-level.md) — exact normalized-state order/count check Part 2 must preserve.
- [DEBT-214](../_archive/debt/debt-214-drizzle-query-duplication-attempt-repository.md) — deduplicated Part 5's TypeScript query construction, not its two SQL executions; [DEBT-188](../_archive/debt/debt-188-duplicated-count-query-attempt-repository.md) similarly extracted count construction only.
- [DEBT-347](../_archive/debt/debt-347-parallel-fetch-opportunities.md) records that dashboard/history requests already use `Promise.all`; concurrency does not remove per-query work. [DEBT-206](../_archive/debt/debt-206-client-side-difficulty-tag-filters-history.md) and [DEBT-083](../_archive/debt/debt-083-unused-attempt-repository-find-by-user-id.md) are server-filtering/pagination precedents.
- [DEBT-437](../_archive/debt/debt-437-tutor-submit-vs-end-write-skew-and-debt-426-residual-wording.md) cites `findCompletedByUserId` only in its accepted write-skew read audit; it makes no query-efficiency ruling.
- Found during the 2026-07-09 DDIA-lens adversarial database-seam sweep (12 finder lenses, per-candidate adversarial verification, dedup against the full archived register); independently source-audited 2026-07-10.
