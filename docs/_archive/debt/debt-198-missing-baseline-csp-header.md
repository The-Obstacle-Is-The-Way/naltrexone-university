# DEBT-198: Missing Baseline Content-Security-Policy Header

**Status:** Invalidated (False Positive)
**Priority:** P3
**Date:** 2026-02-08
**Invalidated:** 2026-02-09

---

## Description

The `next.config.ts` configures security headers (`X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`, `Permissions-Policy`, `Strict-Transport-Security`) but does not include a `Content-Security-Policy` header.

The Clerk middleware in `proxy.ts` does configure CSP directives, but this only applies when Clerk middleware is active (not when `SKIP_CLERK=true` in development).

## Affected Files

| File | Issue |
|------|-------|
| `next.config.ts` | No CSP header in `headers()` config |
| `proxy.ts` | CSP only applied via Clerk middleware |

## Impact

- Without a baseline CSP, the app relies entirely on Clerk middleware for CSP protection.
- In development with `SKIP_CLERK=true`, no CSP is applied at all.

This finding was invalidated after verifying the CSP ownership model and prior regressions:

- CSP is intentionally generated by Clerk middleware in `proxy.ts` (see `docs/vendor-docs/clerk.md`).
- Static CSP via `next.config.ts` is brittle and has caused Preview deployment outages when tightened (see BUG-071).
- `next.config.test.ts` asserting CSP is absent is an intentional regression guard for this strategy.

## Resolution

No code change required. Keep CSP ownership delegated to Clerk middleware.

## Verification

- [x] Verified CSP ownership via `docs/vendor-docs/clerk.md`
- [x] Verified prior static CSP regression history via BUG-071 archive
- [x] Confirmed `next.config.test.ts` is the intended guardrail (no static CSP header)

## Related

- Security audit finding (defense-in-depth)
