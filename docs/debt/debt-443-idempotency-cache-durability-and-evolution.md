# DEBT-443: Idempotency Cache Durability and Evolution Gaps — Schema-Drift Replay Poisoning, Asymmetric Error Records, Production-Dead Lifecycle Cache Branch

**Status:** Active
**Priority:** P3
**Date:** 2026-07-09

---

## Description

The idempotency cache (`idempotency_keys` + `withIdempotency`) treats stored outcomes as durable data but has no evolution story for them: replayed results are validated against whatever schema the *current* deploy ships, cached errors can lose structured fields or replay a different public error than the first request, and one of the cache's dedicated policy branches is exercised only by fabricated test fixtures. Three constituent findings, independently re-verified against current source and tests on 2026-07-10.

### 1. Replay validates cached `result_json` against the CURRENT `.strict()` schema — no version stamp, no invalidation

[`executeIdempotent`](../../src/adapters/controllers/shared/execute-idempotent.ts#L56) unconditionally passes `parseResult: (value) => outputSchema.parse(value)` into `withIdempotency`. There are 11 production `executeIdempotent` call sites across the practice, billing, bookmark, feedback, and question controllers, and all 11 output schemas are strict objects (for example, [`FinalizeExamAnswersOutputSchema`](../../src/adapters/controllers/practice-schemas.ts#L173) and [`CreatePortalSessionOutputSchema`](../../src/adapters/controllers/billing-controller.ts#L49)). On replay of a completed row ([`with-idempotency.ts:319-333`](../../src/adapters/shared/with-idempotency.ts#L319)), the pre-deploy `result_json` is parsed with the post-deploy schema. An incompatible change such as adding a required field or removing a field fails in opposite directions (missing key vs. `.strict()` unknown key); compatible changes such as adding an optional/defaulted field need not fail. The parse-failure catch throws `ApplicationError('INTERNAL_ERROR', 'Cached idempotency result is invalid')` without deleting or aborting the row, and [`idempotency_keys`](../../db/schema.ts#L291) has no schema-version column, so the completed row remains unreadable through the current parser until `expiresAt` ([24h default TTL](../../src/adapters/shared/with-idempotency.ts#L16)).

Failure scenario: a deploy makes an incompatible change to an idempotent action's output shape; request 1 completes pre-deploy and stores `result_json` under the old shape; a duplicate/retry with the same key arrives post-deploy within the TTL — e.g. an exam-finalize retry after the "concurrent request still processing" branch (which deliberately does NOT rotate the key, [practice-session-page-logic.ts:277-283](<../../app/(app)/app/practice/[sessionId]/practice-session-page-logic.ts#L277>)), or a second submission of a billing form rendered pre-deploy. The replay parses the old payload with the new schema, parse throws, and the user gets `INTERNAL_ERROR`. Server-side the row is not invalidated; shipped practice clients rotate the key after a non-`CONFLICT` error ([practice-session-page-logic.ts:285](<../../app/(app)/app/practice/[sessionId]/practice-session-page-logic.ts#L285>)), while form-backed billing surfaces navigate or can be reloaded to render a fresh hidden key. The usual honest-client recovery therefore requires a new key and may re-execute an operation whose first outcome already committed. Existing tests do cover the loud parse-failure behavior with deliberately invalid cached payloads ([`with-idempotency.test.ts:619-666`](../../src/adapters/shared/with-idempotency.test.ts#L619), [`execute-idempotent.test.ts:193-230`](../../src/adapters/controllers/shared/execute-idempotent.test.ts#L193)); what is missing is a deploy-evolution contract that can decode a previous valid shape without converting a committed outcome into a fresh execution. [BUG-218](../_archive/bugs/bug-218-idempotency-parse-error-loses-cause.md) touched this exact throw site but only added `{ cause }` for diagnosability, explicitly leaving schema drift unresolved.

### 2. Cached error records lose structured fields and can expose raw execute-error messages on replay

`ApplicationError` exposes `code`, `message`, `fieldErrors`, and `details` to `ActionResult` callers ([`fieldErrors` at application-errors.ts:82](../../src/application/errors/application-errors.ts#L82)), but the cache persists only the first, second, and fourth: [`toErrorRecord`](../../src/adapters/shared/with-idempotency.ts#L39) and the [`IdempotencyKeyError` port type](../../src/application/ports/idempotency-key-repository.ts#L8) omit `fieldErrors`, and the [replay path](../../src/adapters/shared/with-idempotency.ts#L308) reconstructs the error with `fieldErrors` undefined. [`DrizzleIdempotencyKeyRepository.toApplicationErrorDetails`](../../src/adapters/repositories/drizzle-idempotency-key-repository.ts#L17) also returns only a recognized `ApplicationConflictReason`, discarding every other persisted key. `ApplicationErrorDetails` currently defines only `reason`, so today's recognized values survive; a future typed enrichment would be written by `storeError` but silently dropped on Drizzle read unless this decoder changed with it.

Non-`ApplicationError` failures that an action's policy elects to cache have the opposite problem: [`toErrorRecord`](../../src/adapters/shared/with-idempotency.ts#L39) stores `INTERNAL_ERROR` with the original error's truncated message. The first request rethrows the original, so [`handleError`](../../src/adapters/controllers/action-result.ts#L45) returns `VALIDATION_ERROR` plus flattened fields for a `ZodError`, or the generic `INTERNAL_ERROR` / `Internal error` response for any other unknown error. A replay instead constructs an `ApplicationError` from the cached record ([`with-idempotency.ts:308-316`](../../src/adapters/shared/with-idempotency.ts#L308)), and `handleError` forwards the stored message as public text. The existing wrapper test pins this raw-message replay (`unexpected failure`) at [`with-idempotency.test.ts:668-693`](../../src/adapters/shared/with-idempotency.test.ts#L668); it does not compare the first and replayed controller responses.

Input validation itself is outside the cache because [`createAction`'s `safeParse`](../../src/adapters/controllers/create-action.ts#L45) runs before `executeIdempotent`, and no UI currently reads `ActionResult.error.fieldErrors`. But two keyed actions parse fresh use-case output *inside* `execute`: portal creation ([`billing-controller.ts:160-172`](../../src/adapters/controllers/billing-controller.ts#L160)) and exam finalization ([`practice-controller.ts:335-342`](../../src/adapters/controllers/practice-controller.ts#L335)). An invalid output is therefore `VALIDATION_ERROR` with fields on the first response but cached as `INTERNAL_ERROR` with Zod's raw message for replay. Existing tests cover invalid fresh output only without an idempotency key ([`billing-controller.test.ts:404-424`](../../src/adapters/controllers/billing-controller.test.ts#L404), [`practice-controller-session-lifecycle.test.ts:733-759`](../../src/adapters/controllers/practice-controller-session-lifecycle.test.ts#L733)). Separately, [`CorruptPracticeSessionRowError`](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L38)'s `fieldErrors` survive the first `ActionResult` but disappear on replay. The fake also diverges from the real adapter: [`FakeIdempotencyKeyRepository.find()`](../../src/application/test-helpers/fakes/fake-idempotency-key-repository.ts#L64) returns stored `details` verbatim, while the Drizzle repository validates/narrows them. The [DEBT-435](../_archive/debt/debt-435-practice-session-conflict-and-test-hygiene-follow-ups.md) resolution added `details` persistence/replay but recorded no ruling for these other channels. P3: a same-key replay can change the public code/shape and expose raw internal error text that the first response deliberately hid.

### 3. Lifecycle idempotency cache branch is production-dead: end-path already-ended CONFLICTs are reasonless while fakes and policy tests fabricate reason-bearing errors

[`shouldCachePracticeSessionLifecycleError`](../../src/adapters/controllers/shared/practice-session-idempotency-policy.ts#L20) — the BUG-278 fix wired to `practice:endPracticeSession` and `practice:discardPracticeSession` ([practice-controller.ts:279](../../src/adapters/controllers/practice-controller.ts#L279), [:317](../../src/adapters/controllers/practice-controller.ts#L317)) — caches only CONFLICTs carrying `details.reason` `AlreadyEnded`/`ExamTimeExpired`. But no production emitter on either action ever attaches a reason: [`EndPracticeSessionUseCase` throws bare CONFLICT](../../src/application/use-cases/end-practice-session.ts#L29), [`DrizzlePracticeSessionRepository.end()` throws bare CONFLICT at line 576](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L576) and [599-602](../../src/adapters/repositories/drizzle-practice-session-repository.ts#L599), and [`DiscardPracticeSessionUseCase`](../../src/application/use-cases/discard-practice-session.ts#L34) throws no CONFLICT at all. The cache-true branch never fires in production; BUG-278's documented "terminal conflicts replay with structured reasons" semantics are certified only by tests fabricating `practiceSessionAlreadyEndedError()` at the use-case seam ([practice-controller-session-lifecycle-idempotency-policy.test.ts:73](../../src/adapters/controllers/practice-controller-session-lifecycle-idempotency-policy.test.ts#L73), [with-idempotency-practice-session-conflicts.test.ts:138](../../src/adapters/shared/with-idempotency-practice-session-conflicts.test.ts#L138)), and [`FakePracticeSessionRepository.end()`](../../src/application/test-helpers/fakes/fake-practice-session-repository.ts#L372) over-promises a reason the real repository's `end()` never emits — an LSP break in the test double's error contract. [BUG-282](../_archive/bugs/bug-282-tutor-two-tab-ended-session-dead-end-loop.md) annotated only the submit/load paths and left the end path bare.

No wrong observable behavior today: an end request made with a fresh key after the session has already ended gets a bare `CONFLICT`; the policy declines to cache it, aborts the claim, and a same-key retry re-executes to the same monotone conflict. (A retry of the key that originally ended the session replays its cached success.) The client end flow recovers any end `CONFLICT` via catch-all summary fetch. The verifier confirmed `ExamTimeExpired` cannot make the branch live on these actions (exam mode is rejected with `VALIDATION_ERROR` before `end()`, and discard emits no `CONFLICT`). The trap is latent: any future client or policy change branching on `details.reason === AlreadyEnded` for the end/discard surface can pass the reason-bearing policy/controller tests and silently take the generic path in production. P4.

## Impact

- **Part 1 (P3, drives the doc priority):** an incompatible output-schema change across a deploy turns a straddling-key replay into `INTERNAL_ERROR` for the row's remaining TTL. Honest clients can obtain a fresh key, but that recovery can re-execute an operation whose completed cache record says the first outcome committed. Tests pin the fail-loud parser branch but provide no previous-version decode/upcast contract.
- **Part 2 (P3):** first and replayed controller responses can disagree today: keyed output-validation failures change from structured `VALIDATION_ERROR` to cached `INTERNAL_ERROR`, corrupt-row fields disappear, and cached unknown execute failures replay their raw message instead of the first response's generic text. The port cannot express `fieldErrors`, and the fake over-promises arbitrary typed `details` round trips.
- **Part 3 (P4):** dead policy code certified by fabricated fixtures — every "terminal" end conflict is re-executed, not replayed — plus a fake-contract trap for the natural next step of the DEBT-426/BUG-282 reason contract.

## Proposed Resolution

**Part 1:**
- **Option 1 (RECOMMENDED):** define a per-action, versioned cached-result envelope and version-specific decoders/upcasters. Treat existing unversioned rows as the baseline version; decode the stored version and map it to the current output contract before returning it. Unknown/invalid versions stay fail-loud with the completed row intact. A schema mismatch must never delete/reclaim a completed outcome or re-run `execute`, because doing so breaks DEBT-424 outcome immutability and can duplicate side effects.
- **Option 2 (smaller operational discipline):** for each incompatible output change, ship a replay parser that accepts and maps the previous valid shape, with a fixture produced by the pre-deploy schema. Retain it until at least 24 hours after the last old-version writer is no longer serving traffic. This avoids persistence changes but relies on release discipline and handles only explicitly supported prior shapes.
- **Rejected:** deleting the completed row, treating a version mismatch as a cache miss, or falling back to raw/unvalidated JSON. The first two can re-execute a committed effect; the last returns a payload that violates the current controller contract.

**Part 2:**
- **Option 1 (recommended):** define one versioned, validated public-error envelope and a pure normalization seam shared by first-response mapping and cache serialization. Preserve all safe `ApplicationError` fields; normalize `ZodError` to the same `VALIDATION_ERROR` / `Invalid input` / flattened fields shape on first response and replay; normalize every other unknown error to the same generic `INTERNAL_ERROR` / `Internal error` public record while retaining raw diagnostics only in logs or causes. Evolve `details` through an explicit codec, treat existing rows as a legacy version, and align fake and Drizzle contract tests around identical accepted/rejected shapes.
- **Option 2 (explicit narrowing):** document and type a smaller durable public contract, but still normalize `ZodError` and unknown failures to the same safe first-response shapes before persistence. This may intentionally omit `fieldErrors`/new details only if first execution omits them too; it must not preserve today's raw-message replay.
- **Rejected:** passing arbitrary JSONB keys through as `ApplicationErrorDetails`. That would trade the current explicit allowlist for an unchecked type assertion at the persistence boundary.

**Part 3:**
- **Option 1 (recommended):** annotate the end-path already-ended conflicts with `practiceSessionAlreadyEndedError()` at both real emitters (`end-practice-session.ts:29`; `drizzle-practice-session-repository.ts:576`, `599-602`), completing the BUG-282 treatment — makes the fake's contract truthful, makes the BUG-278 cache branch live, and is behavior-compatible because the client already recovers any end CONFLICT to the summary.
- **Option 2 (if reasonless-by-design is preferred):** align `FakePracticeSessionRepository.end()` to throw the bare CONFLICT the real path emits, rewrite the lifecycle-policy/cache-replay tests to use production-shaped conflicts, and document in the policy that the cache branch is reserved for a future reason-wired end path.
- **Option 3 (either way):** add a fake-vs-real contract test pinning that both repositories' `end()` throw the same error shape (code AND `details.reason`) for an already-ended session.

## Verification

- **Part 1:** a pre-deploy cached fixture is replayed through the post-deploy decoder and mapped to the current output without calling `execute`; unknown/invalid versions still produce the existing `INTERNAL_ERROR` with `cause`, preserve the completed row, and never re-execute. For the compatibility-window option, pin both the previous and current strict shapes.
- **Part 2:** controller-level same-key tests prove first and replayed `ActionResult`s are identical for an `ApplicationError` with fields/details and for invalid keyed portal/finalize output. A raw execute error yields generic public text on both requests, and its cached public record contains no raw message. Drizzle/fake round trips accept and reject the same detail shapes; a legacy row without newer fields follows an explicitly pinned compatibility rule.
- **Part 3:** an integration or contract test where a real (or production-shaped) already-ended `end()` conflict flows through `shouldCachePracticeSessionLifecycleError` and is cached-and-replayed (Option 1) — or the rewritten tests proving the branch is declined for production-shaped bare CONFLICTs (Option 2); either way the fabricated-fixture certification gap is closed.

## Related

- [BUG-218](../_archive/bugs/bug-218-idempotency-parse-error-loses-cause.md) — added `{ cause }` at the exact replay-parse throw site; left schema drift open.
- [DEBT-342](../_archive/debt/debt-342-idempotency-backward-compat-guard.md) — the unrelated `completedAt||resultJson` fallback removal.
- [DEBT-435](../_archive/debt/debt-435-practice-session-conflict-and-test-hygiene-follow-ups.md) — added `details` persistence/replay; no ruling on `fieldErrors`.
- [BUG-278](../_archive/bugs/bug-278-end-discard-idempotency-caches-transient-errors-under-fixed-key.md) — built the lifecycle cache policy whose cache-true branch part 3 shows is production-dead.
- [BUG-282](../_archive/bugs/bug-282-tutor-two-tab-ended-session-dead-end-loop.md) — annotated submit/load conflicts with reasons; end path left bare.
- [DEBT-437](../_archive/debt/debt-437-tutor-submit-vs-end-write-skew-and-debt-426-residual-wording.md) / [DEBT-438](../_archive/debt/debt-438-conflict-reason-client-coverage-gaps.md) — adjacent idempotency/conflict-reason seams; verifier confirmed none of these rule on the gaps above.
- Found during the 2026-07-09 DDIA-lens adversarial database-seam sweep (12 finder lenses, per-candidate adversarial verification, dedup against the full archived register).
