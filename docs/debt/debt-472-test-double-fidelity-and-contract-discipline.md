# DEBT-472: The Fakes-Over-Mocks Rule Sanctions Unverified Doubles at Every External Seam

**Status:** Active
**Priority:** P2
**Date:** 2026-08-22

> **2026-09-23 UTC Practice starter pointer:** Quick Practice #1031 merged as `cd6fe03d` after formal exact-head approval **5291471054**, zero threads and CI **35865089072** (52/52 E2E, no failure/retry markers). Its milestone promotion remains pending. The two mocked Practice starter units now have named real-component browser twins; candidate floors are **13 / 210 / 44** across **5 / 43 / 17** files. See the [Practice starter disposition](#practice-starter-state-disposition-2026-09-23-utc). This increment's gate/review/promotion and the remaining steps 4–6 remain Open.

> **2026-09-23 UTC #1030 release receipt:** The pending verification below is complete. Main CI **35844589693** passed `test` at **09:55:20Z** (52/52 E2E, no failure/retry markers). Vercel was Ready at **09:44:49.658Z**, observed STAGED without a production alias at **09:54:48Z**, then assigned production at **09:55:22.722Z**. Both trees equalled `e4ca78e932f4082115067bbf9e1e04d18b77c04d`; production `/` and `/api/health` returned 200 with app/database healthy. The same serving commit and timestamps were reverified through the API at **13:03:36Z** after the restart.

> **2026-09-23 UTC Quick Practice pointer:** History increment #1029 merged as `3b9ec20e` with exact-head approval **5289124496**, zero threads and CI **35842211278** (52/52 E2E, no failure/retry markers). #1028/#1029 are promoted through #1030 (`c2139f54`) after source-provenance verification and promotion CI **35843457223**; post-merge release verification remains pending. The Quick Practice count-render unit has a named real-hook browser twin, and both its owned factory and browser hook spy retire. Candidate floors are **15 / 210 / 44** across **6 / 43 / 17** files. See the [Quick Practice disposition](#quick-practice-count-hook-disposition-2026-09-23-utc). This increment's full gate/review and the remaining steps 4–6 are not claimed complete.

> **2026-09-23 UTC history-hook pointer:** Mark-for-review increment #1028 merged as `cdcbfd65`, with formal exact-head approval and CI **35839978793** (52/52 E2E, no failure/retry markers). Two mocked expanded-state history units now retire behind their named real-hook Browser Mode twins; the other sixteen units retain all 102 assertions. Candidate floors are **16 / 210 / 44** across **7 / 43 / 17** files. See the [history disposition](#history-disclosure-disposition-2026-09-23-utc). Its gate/review and the #1028 milestone promotion remain pending; steps 4–6 stay Open.

> **2026-09-23 08:47 UTC release receipt:** The pending #1027 verification below is complete. Main CI **35837772747** passed `test` at **08:43:06Z** (52/52 E2E, no failure/retry markers). Vercel was Ready at **08:34:32.855Z**, observed STAGED without a production alias at **08:40:14.506Z**, then assigned production at **08:43:08.458Z**. Both trees equal `e3af13fbf56ca23c1b1abddd581c9dd639dc6e58`; production `/` and `/api/health` returned 200 with app/database healthy. The mark-for-review increment remains unlanded.

> **2026-09-23 UTC mark-for-review pointer:** #1025/#1026 are promoted by #1027 (`668d2bec`), after source approval proof and promotion CI **35836560668** (52/52 E2E, no failure/retry markers); post-merge release verification remains pending. The fifth reporter factory now retires through five named Browser Mode replacements plus a business-error negative control. Candidate floors are **17 / 210 / 44** across **8 / 43 / 17** files. See the [mark-for-review disposition](#mark-for-review-reporter-disposition-2026-09-23-utc). This increment and the remaining steps 4–6 are not claimed complete.

> **2026-09-23 UTC browser-support pointer:** The three previously excluded browser-setup factories are now migrated to typed spies on the real controller exports, under the existing Browser Mode exception. The existing scan now includes `.setup.ts(x)` and `*-test-helpers.ts(x)` support sources; no floor is raised. The expanded census remains **18 / 210 / 44** across **9 / 43 / 17** files. See the [browser-support disposition](#browser-support-disposition-2026-09-23-utc). Reporter increment #1025 merged as `e18cb298` after exact-head approval and CI **35832686174** (52/52 E2E, no failure/retry markers); its promotion and this increment remain pending. Steps 4–6 remain Open.
**Source:** Owner question after DEBT-471 — "we obsess over fakes over mocks in the docs, so how did we end up with ~1,100 LOC of hand-rolled Stripe mocks?" A read-only six-pass census of the test estate at `dev` `070cd567` (578 tracked test files; 4,061 unit / 398 browser / 258 integration cases) answered it. The cause is the rule text itself, not agent drift; the estate is otherwise clean.

**Part A re-audit (2026-08-23; scope clarified 2026-08-24):** Re-derived at `dev` `01fd55b8` before implementation. The live anchors remain `AGENTS.md:525-592` (including `:76`, `:544`, and the `fakeDb` example at `:565-569`), `.claude/rules/testing.md:19-32`, `.claude/rules/architecture.md:45`, `.coderabbit.yaml:59-67`, `docs/dev/react-vitest-testing.md:252-258,297-313`, and `docs/adr/adr-003-testing-strategy.md:146-179,396`. The original `*.test.*` / `*.spec.*` census contains exactly 325 `as unknown as` sites across 60 test files and 17 own-code factory-form `vi.mock` calls across 12 test files; it excludes non-test support files. Both source-scan exemplars remain at `tests/architecture-boundary-source-scan.ts` and `components/theme-token-regression-source-scan.ts`. No requested Part A anchor or census value drifted within that scope. The later review of the executable guard found the equivalent bypass through 5 own-code factory-form `vi.doMock` calls in one additional file, making the guarded floor 22 module factories across 13 files. DEBT-475 then found 3 more factory-form `vi.mock` calls in one excluded `.setup.ts` file, so the observed all-source total is 25 factories across 14 files; those 3 sites are migration work, not additions to the ratchet floor.

**Part B re-audit (2026-08-23):** Re-derived at `dev` `874406a9` before implementation. The maintained behavior-double inventory remains 24: 23 port/gateway fakes exported from `src/application/test-helpers/fakes/index.ts` (excluding the legacy-named canned `FakeUseCase` stubs) plus adapter-owned `FakeStripeCheckoutClient`. Two surfaces have fake↔real contracts: `tests/shared/subscription-observation-version-contract.ts` is invoked by `fake-subscription-repository-contract.test.ts` and `bug-regression-subscription-observation-version-fence.integration.test.ts`, while `pending-stripe-customer-cleanup-contract.integration.test.ts` runs one scenario table against both implementations. Of the remaining 22, 21 have isolation-only tests and `FakeSha256Hasher` has no isolation test. `SubscriptionRepository` still has five methods; the shared contract type names only `findByUserId`, `findObservationVersionByUserId`, and `upsert`, leaving both external-id lookup methods outside the contract. ADR-003's permissive-fake license is now at line 56, not line 55: fakes need not reproduce adapter-boundary parsing/validation. Archived DEBT-007 chose that Option C but enumerated only two method examples plus a repository-wide UUID omission, not a maintained per-fake divergence list. Archived DEBT-455 rejected a generalized harness for two demonstrated `FakeUserRepository` divergences; that ruling predates this 24-surface census and did not adjudicate a shared scenario-table shape across the estate.

## Execution re-audit (2026-09-19)

> **2026-09-23 UTC reporter-boundary pointer:** #1022 and #1023 are promoted through #1024 (`32741ef3`); main CI **35829482597** passed with 52/52 E2E and no failure/retry markers. Vercel held the Ready build until `test` at **07:09:25Z**, then assigned production domains at **07:09:27.459Z**; both trees equal `d8343c3e500ff01a67ea29da1a7ec4dcb2d67f4b` and production health is green. Four logic suites now exercise the real reporter and fake only the external Sentry SDK. The same disabled-reporter mutation that passed all 106 old cases now fails 14 preserved reporting assertions. Candidate floors are **18 / 210 / 44**. The attempted Stripe Search replacement is unproven and unlanded; its three old units and floor remain intact. See the [reporter disposition](#client-reporter-boundary-disposition-2026-09-23-utc) and [Search limitation](#customer-search-replacement-attempt-2026-09-23-utc). Steps 4–6 remain Open.

> **2026-09-23 UTC customer fault-shape pointer:** Container transaction #1022 merged as `a37a5173` after exact-head approval **5287538005**, zero unresolved threads and CI **35825473830** (52/52 E2E, no failure/retry markers); promotion remains pending. Four customer fault/request-shape units now reuse the existing typed client without changing fake or provider behavior. Each preserved case has an independent mutation red; the three Search-behavior cases remain byte-identical and undisposed. Candidate floors are **22 / 210 / 44**. See the [customer disposition](#customer-fault-and-request-shape-disposition-2026-09-23-utc). Steps 4–6 remain Open.

> **2026-09-23 UTC container transaction pointer:** #1019 and #1020 are promoted through #1021 (`97005e65`); main CI **35823266832** passed with 52/52 E2E and no failure/retry markers. Vercel held the Ready build until `test` at **05:50:30Z**, then assigned production domains at **05:50:32.275Z**; both trees equal `125c75bc33afd80772c24b5bf1d7d479881275d7` and production health is green. The container's canned transaction unit now has a real-Postgres twin, proven against four transaction-wiring mutations and a commit-before-rethrow mutation. Candidate floors are **22 / 214 / 44**; seven named F8 doubles remain (the Stripe customer cases). See the [transaction disposition](#container-transaction-disposition-2026-09-23-utc). This increment and the rest of steps 4–6 remain pending.

> **2026-09-23 UTC container wiring pointer:** #1019 merged as `29d87984` after exact-head approval **5287029022**, zero threads and CI **35819982108** (52/52 E2E, no failure/retry markers); its promotion remains pending. The three remaining named private-field casts now have public wiring replacements: both price mappings run through the real SDK and Postgres, and the injected SDK verifies a signed event. Three independent wiring mutations fail before the unchanged production source passes. All five F8 type lies are disposed; eight enumerated F8 doubles remain, including the separate container transaction stub. Candidate floors are **22 / 216 / 45**. See the [container wiring disposition](#container-wiring-disposition-2026-09-23-utc). Steps 4–6 remain Open.

> **2026-09-23 UTC private-state fixture pointer:** Feedback-export #1016 merged as `48c51e4c` after exact-head approval **5286669529** and CI **35815339547** (52/52 E2E, no failure/retry markers); promotion of that increment remains pending. The fake-session lost-persistence fixture now uses its existing public `discard` operation instead of casting through the private `sessions` field. Four independent mutations prove the unchanged conflict assertions still detect lost state; fake and adapter behavior are unchanged. Candidate floors are **22 / 224 / 45**; four of the five type lies remain. See the [private-state disposition](#private-state-fixture-disposition-2026-09-23-utc). Steps 4–6 remain Open.

> **2026-09-23 UTC feedback-export pointer:** #1013 and #1014 are merged and carried by promotion #1015 (`37f3f6ec`); main CI **35812849184** passed with 52/52 E2E and no failure/retry markers. Vercel held the ready build until `test` completed at **03:15:12Z**, then assigned production domains at **03:15:14.562Z**; production health is green and both trees equal `0ec6be6798373c8a9e9917a9028296f4ed953a0b`. The next F8 disposition replaces one canned SQL unit with two mutation-proven Postgres cases for complete projection/join results and chronological/ID ordering. The old unit stayed green with reversed chronological order. Candidate floors are **22 / 225 / 45**; the retained CLI stub is explicitly wiring-only. See the [feedback-export disposition](#feedback-export-f8-disposition-2026-09-23-utc). Steps 4–6 remain Open.

> **2026-09-23 UTC portal execution pointer:** The rate-limiter disposition below merged through #1013 as `82f4fc73` after exact-head approval and CI `35809735788` (52/52 E2E, no failures/retries); promotion remains pending. The two portal-test casts now use the existing typed `FakeStripeCheckoutClient` without changing its behavior. The lower floor first failed on both old casts; retry removal and omitted request options independently failed the migrated tests. Current candidate floors are **22 / 226 / 45**. This is typed fault/request-shape coverage, not a claim that the client's canned portal member models Stripe. See the [portal disposition](#portal-f8-disposition-2026-09-23-utc); steps 4–6 remain Open.

> **2026-09-23 UTC rate-limiter execution pointer:** Published WIP `9446b459` was rebased onto current `dev` `d711454a` on a fresh branch; the published WIP was preserved. All 14 added Postgres cases and the three existing cases pass on this base. Sixteen targeted mutation runs fail as intended, including the retained empty-driver-response unit; the production adapter is byte-identical to `dev`. The candidate cast floor is now **228 / 47 files**, down from **240 / 48**. This disposes the 12 rate-limiter casts, not any additional repository or Stripe seam. The [current deletion map and red receipts](#rate-limiter-f8-disposition-2026-09-23-utc) supersede the unlanded draft's aggregate counts. Review and promotion of this disposition remain pending; steps 4–6 remain Open.

> **2026-09-21 current-tree forward pointer:** #920/#921/#922/#923 are merged and promoted. Five real-Postgres filter twins, the 19-case Stripe-event suite (including `peek` and bounded pruning), and the attempt disposition are complete. The live floors are **22 / 240 / 45** across **13 / 48 / 18** files; textual casts are **278 / 59** files, including fixture strings, and `RepoDb` casts are **138 / 21** files. There are **27** size suppressions. The older Part-A alias-resolution description is superseded by #923's bounded explicit-form/fail-closed classifier, not current resolver behavior. The three browser setup factories remain outside the scanner's test/spec walk, all five type lies remain, and the first live-retrieve fake migration is still outstanding. Step 4 is partially complete, not a mandate to repeat already-landed deletions. Step 5's migration ordering is already recorded in DEBT-468; execution remains. Step 6's index guard convention already exists; the live DEBT-035/444 adjudication remains. No floor or allowlist changed in this audit. [Receipts](./assets/active-audit-2026-09-21/verification.md).

Baseline: `dev` `ed07103f7c40c47ccbe83a0afccee076d25cbd44`. This dated section is the forward pointer for current execution; the filing and Part A/B receipts above and below remain historical. Node 24.16.0, frozen pnpm 11.3.0 install, installed/locked Zod 4.6.0, all eight required E2E key names, and 958 parseable seed files were verified without logging credentials. Migrations and seeding used this clone's resolver-selected local Postgres. Baseline unit: 463 files / 4,272 passing cases.

| Item | Claim | Verdict | Current evidence (file:line at the baseline) |
| --- | --- | --- | --- |
| Ratchet | Floors remain 22 own-code factories / 289 casts / 45 maintained-port doubles | **CONFIRMED** | `tests/test-double-fidelity-ratchet-floors.ts:11,30,137`; live scanner equals all three maps (13 / 50 / 18 files). |
| Raw cast census | The live textual census is still 325 sites / 60 files | **REFUTED** | 327 / 61; the two additions are fixture strings at `tests/test-double-fidelity-edge-cases.test.ts:52,65`. The parsed, non-allowlisted cast count remains 289. |
| Repository doubles | 187 `as unknown as RepoDb` sites span 23 files | **CONFIRMED** | Current tracked-file census; representative constructor cast at `src/adapters/repositories/drizzle-attempt-repository.test.ts:946`. |
| Five filter tests | The five units evaluate the production predicates | **REFUTED** | `drizzle-attempt-repository.test.ts:932-1055` returns canned matching rows; production builds predicates at `drizzle-attempt-repository.ts:91-105`. |
| Replacement coverage | All five filters already have real-Postgres twins | **REFUTED** | Only `incorrect` is explicitly asserted at `tests/integration/session-attempt-repository.integration.test.ts:808-814`; the integration tree has no `correct`, `adhoc`, `tutor`, or `exam` attempted-question filter invocation. Add replacement cases before deletion. |
| Stripe-event gaps | `pruneProcessedBefore` and `peek` lack direct real-Postgres assertions | **CONFIRMED** | `drizzle-stripe-event-repository.ts:23,91`; `stripe-repositories.integration.test.ts:32-61` exercises claim/lock/mark, while existing integration `peek` calls target Clerk events. |
| Setup-file path | The step-4 browser setup path no longer exists | **REFUTED** | `app/(app)/app/practice/[sessionId]/hooks/practice-session-page-model.browser.setup.ts:32,37,44` still contains all three factories. |
| Scan scope | Those three setup factories are outside the 22-site floor | **CONFIRMED** | `tests/test-double-fidelity-source-scan.ts:49-54` walks only test/spec extensions. Migrate before widening; never raise a floor. |
| Prototype spread | The transactional session double loses its prototype | **CONFIRMED** | `tests/integration/controllers.integration.test.ts:615-619` spreads the instance before casting it back. |
| Private-field casts | Four additional type lies remain | **CONFIRMED** | `lib/container.test.ts:528,531,560`; `fake-practice-session-repository.test.ts:22`. |
| First Stripe migration | Live-retrieve can use the existing fake without a new provider behavior | **CONFIRMED** | `stripe-checkout-sessions-live-retrieve.test.ts:7-57` duplicates create/retrieve plumbing; existing fake retrieval overrides supply the two fault cases. |
| Contract terminology | DEBT-468's “colocated contract coverage” means shared fake↔real parity | **REFUTED** | `docs/dev/test-double-contract-register.md:49-73` records 24 surfaces with three shared contracts and explicit waivers for the remainder. |
| Suppression count | 28 size suppressions remain | **CONFIRMED** | Tracked test-file census: 28 `noExcessiveLinesPerFile` directives; policy remains 800 at `biome.json:47-50`. Updated physical lengths are in DEBT-469's dated pointer. |
| Step 6 convention | The debt-index deletion/guard convention is absent | **REFUTED** | `docs/debt/index.md` § Resolution Evidence Conventions (line 1037 at this head; the line moves as the index grows) (the original `:901` citation pointed at an archived-row table entry and is corrected 2026-09-24 UTC) already requires the executable guard. Preserve it; the live DEBT-035/444 adjudication remains work. |
| Classifier replacement | DEBT-475 authorizes bounded fail-closed syntax instead of alias analysis | **CONFIRMED** | Its “Own-code module-mock classifier” row requires no second argument or exact literal `{ spy: true }`, rejecting factories and indirect/unknown forms under red proof. |

Path/SHA audit: all 25 commit identifiers cited across the four records (including this baseline) resolve; numeric Actions run IDs are not commit SHAs. Removed wrapper, bookmark-helper, and self-test paths in DEBT-475 are explicitly historical; DEBT-468's proposed fixture paths are proposals, not existing files. The renamed runner is `scripts/run-stripe-provider-contracts.ts`, and the hosted annual spec is `tests/e2e/stripe-hosted-paid-checkout.spec.ts`. DEBT-475's seed-choice row has a separate stale path (`scripts/seed/seed-helpers.ts` → `scripts/seed-helpers.ts`); that row belongs to the other clone and is recorded here without editing its scope. Historical coverage percentages and line anchors describe their named audit commits and are not current measurements.

**First coverage increment (2026-09-19):** `tests/integration/attempted-question-filters.integration.test.ts` adds five real-Postgres cases for correct/incorrect and adhoc/tutor/exam. Each seeds matching and nonmatching latest attempts plus opposite older history, then checks both selected rows and the count. Removing the four result/source predicate expressions from the production adapter made all five new cases fail on extra returned rows; the five existing canned-response units stayed green under the same mutation. Restoring the unchanged adapter made all five new cases pass. No unit is deleted in this increment, no suppression changes, and floors stay 22 / 289 / 45. These cases are the deletion twins for the five units in the later repository disposition.

Execution order: land missing real-Postgres filter coverage first; then dispose each repository unit with a named twin and lower its floor. Stripe-event prune/peek coverage precedes their disposition. Keep cron-route changes blocked until PR #919 merges and the branch incorporates updated `dev`. Split suppression-bearing files when migrating them; do not transfer cast debt into a new ratchet entry. The other clone owns CI, deployment, secrets, production telemetry, orchestration, and every DEBT-475 row except the classifier replacement.

### Stripe-event repository disposition (2026-09-19)

Replacement tests were added and proved before deleting any unit. `tests/integration/stripe-event-repository.integration.test.ts` exercises the real adapter and Postgres in 17 cases; `stripe-repositories.integration.test.ts` retains the original claim/lock/lifecycle coverage. All 17 new cases failed under the targeted mutations below before passing against the restored, unchanged adapter. This disposes one of the 23 `RepoDb` files; the other 22 remain work.

| Removed unit cases (`drizzle-stripe-event-repository.test.ts`, pre-delete lines) | Real-Postgres twin |
| --- | --- |
| Claim success / duplicate (16, 43) | New `claims an event once…` verifies persisted type, boolean results, and immutable processed state on replay; existing `stripe-repositories.integration.test.ts:33` also exercises both outcomes. |
| Lock processed / pending / failed / missing (66, 85, 103, 121) | Existing `persists Stripe events with idempotency and processed tracking` checks all three stored lock states; new `rejects a missing event in lock…` checks the error class and code. The review follow-up below adds pending/failed two-connection contention cases proving the lock lasts until transaction completion. |
| Peek missing / existing without locking (141, 157) | New missing-peek case, three stored-state cases, and a second transaction holding the target row lock while `peek` must complete. |
| Mark processed / missing (179, 207) | New `marks only the requested event processed…` checks the injected timestamp, error clearing, and preservation of another event; missing-mark case checks error class/code. |
| Mark failed / missing (227, 250) | New `marks only the requested event failed…` checks timestamp clearing, error persistence, and preservation of another event; missing-mark case checks error class/code. |
| Invalid limit / empty batch (270, 295) | Three invalid-limit cases retain the no-transaction assertion against the real Drizzle object; the empty-batch case checks zero and cutoff-row preservation. |
| Bounded deletion / delete cutoff recheck (333, 373) | New batch case checks oldest-first deletion, batch count, strict cutoff, pending/failed preservation, and the remaining batch. The refresh race holds a concurrent update until PostgreSQL reports the pruner blocked, then commits a fresh timestamp and proves the selected row survives with deleted count zero. |

Red receipts: removing the peek selector fails 4 cases; adding a row lock to peek fails 1; changing missing-event codes fails 3; omitting cleared write fields fails 2; removing the invalid-limit guard fails 3; increasing the batch limit fails 1; returning a nonzero empty count fails 1; omitting the delete-side cutoff fails the refresh-race case; corrupting the claimed event type fails the replay/persistence case. The lock tests use separate real connections and bounded PostgreSQL lock waits; no production mutation remains. The epoch-era prune fixtures avoid the recent retained E2E event history.

Disposition delta: 16 call-chain unit cases and their file are removed only after the above twins pass; 17 integration cases are added. The unknown-cast floor drops **289 → 273** (50 → 49 files), raw casts **327 → 311** (61 → 60 files), and `RepoDb` casts **187 → 171** (23 → 22 files). Own-code and maintained-port floors remain **22 / 45**; size suppressions remain **28**. The `FakeStripeEventRepository` waiver remains explicit: these are real-adapter tests, not a new shared fake↔real contract.

Review correction (2026-09-19, PR #921): the original replacement suite checked `lock()` state but did not prove successful row-lock contention. The reviewer claim is **CONFIRMED**. Two additional real-Postgres cases, pending and failed events, now acquire the repository lock inside one transaction, verify PostgreSQL reports that exact backend blocking a second repository transaction, then prove the second lock completes after the first transaction ends. Replacing `.for('update')` with an ordinary select made both cases fail (`expected false to be true`); restoring the byte-identical production adapter passed all **28** focused cases. This brings the new suite to **19** cases and the integration estate to **282**, with no additional unit deletion or floor change. The first direct Vitest invocation stopped at the missing-`DATABASE_URL` setup guard and is not red evidence; the accepted red and green runs used the per-clone `pnpm test:integration` wrapper.

### Attempt repository disposition (2026-09-19)

The 28 removed cases have the following real-Postgres replacements. Five hard-to-force adapter error cases remain in the original unit file; its behavior cases move to integration, retiring the size suppression in the same change. No assertion about SQL behavior is left on a canned query result.

| Removed unit cases (`drizzle-attempt-repository.test.ts`, pre-change lines) | Real-Postgres twin / corrected premise |
| --- | --- |
| Insert mapping / explicit timestamp (274, 333) | `attempt-repository-reads-writes.integration.test.ts`: explicit timestamp plus every mapped field, persisted choice/correctness/time, and the database-default timestamp. |
| Omitted insert (370) | `session-attempt-repository.integration.test.ts:78`: omitted outcome, raw nullable choice, incorrect score, and repository readback. |
| Duplicate session-answer conflict (466) | New `maps a real duplicate session answer…`: exact conflict class/message and original-row preservation. |
| User mapping / limit guard / negative offset (549, 595, 607) | New explicit-field case, three invalid-limit cases with the real query spy untouched, and descending user-scoped pagination including negative-offset clamping. |
| Session mapping / cap / ordering (643) | Existing session scoping at `session-attempt-repository.integration.test.ts:32`; new `bounds session reads…` checks 501 persisted rows, exact 500-row bound and ID tie-breaking. The large result is deliberately seeded at the DB boundary because current session creation caps its parameter list at 200. |
| Empty question IDs (694) | New empty-ID case checks the real Drizzle select method is never called. |
| Null latest timestamps (705, 896) | **Corrected premise:** `attempts.answered_at` is `NOT NULL` (`db/schema.ts:893-895`); both projections derive from real attempt rows. New `rejects null timestamps…` checks SQLSTATE 23502 and no projection rows after the rejected write. Null-row fixtures represented impossible stored state, not filtering coverage; the schema invariant replaces them. |
| Aggregate/latest left joins (723, 745) | `bug-regression-active-exam-latest-attempt-fallback.integration.test.ts:571,621,677,733,786,829`: actual visible fallback for standalone/tutor/ended exam and hidden active-exam-only state. |
| Four counts / timestamp list (788, 877) | New inclusive-date case checks total/correct counts with and without cutoff, user isolation, and descending timestamps. Existing active-exam timestamp cases at `bug-regression-active-exam-latest-attempt-fallback.integration.test.ts:43,153` remain. |
| Active-exam count / recent / attempted-list / attempted-count joins (811, 865, 1057, 1107) | `bug-regression-active-exam-projections.integration.test.ts:130,204,304` checks actual exclusion before end and visibility afterward. |
| Recent-attempt mapping (824) | New explicit-field case checks the whole recent-attempt value including nullable session mode. |
| Five result/source filters (932, 957, 982, 1007, 1032) | All five cases in `attempted-question-filters.integration.test.ts` (PR #920), including latest-history selection and accurate counts. |
| Attempted counts / tutor / exam (1071, 1083, 1095) | Existing latest-question case at `session-attempt-repository.integration.test.ts:708`, plus PR #920's source-filter count cases. |

The retained five units cover empty insert results, corrupted answered rows on insert/read, an unrelated unique-constraint name, and an unexpected driver exception. They inject only the prepared-query execution response on an actual typed Drizzle mock database; no query-chain object is cast into `RepoDb`, and no filtering behavior is attributed to the stub. The response rows are checked against the schema's selected-row type. These fault cases cannot be produced safely through the normal schema-constrained adapter contract.

Red proofs for the 11 integration cases: omit explicit timestamps; replace the default timestamp; change the duplicate error code; remove offset clamping; remove the invalid-limit/empty-ID guards; replace the inclusive date predicate; raise the session cap; and temporarily drop the local DB's NOT NULL constraint. Every case fails under its targeted mutation and passes with the unchanged production source and restored constraint. The first timestamp/cap probes exposed fixture errors (raw PostgreSQL timestamps are strings; session creation caps at 200), which were fixed before accepting their mutation receipts.

Disposition delta: unit **33 → 5** in this file; integration **+11**. Cast floor **273 → 240**, raw casts **311 → 278**, and `RepoDb` casts **171 → 138** (22 → 21 files). Own-code/port-double floors stay **22 / 45**. Suppressions **28 → 27**. The broader step-4 inventory remains open; the fake-attempt waiver is not promoted to shared parity by these real-adapter tests.

**Review correction (2026-09-20 UTC, PR #922):** The bounded-session fixture originally gave every row the same timestamp, so it did not prove chronological selection. Confirmed by removing `answeredAt` from `findBySessionId` ordering: the entire integration lane still passed **293/293** (six opt-in cases skipped). The strengthened fixture assigns the newest timestamp to the lowest-ID row, which an ID-only 500-row bound would exclude; older equal-timestamp rows still assert the ID tie-breaker. With that mutation, the changed case fails (**1 failed / 10 passed**); restoring the byte-identical production adapter passes **11/11**. No production change, new case, floor increase, or weakened assertion is needed.

### Bounded mock classifier (2026-09-19; delegated DEBT-475 row)

The historical step-2 description of alias and assignment resolution is superseded by the DEBT-475 row's bounded rule: own-code `vi.mock` and `vi.doMock` accept one argument, or exactly two arguments whose second argument is the direct object literal `{ spy: true }` (one identifier-named property and the literal `true`). Indirect bindings, unresolved identifiers, factories, type assertions, computed/quoted property names, spreads, extra properties, and extra arguments fail closed. Semantically equivalent spellings must use the documented literal form; the scanner does not interpret arbitrary code to prove equivalence.

Before replacement, 27 assertions failed against the old classifier, including seven formerly accepted indirect forms and 13 noncanonical option forms. Separate mutations proved all four allowed API/form pairs fail when rejected and both direct factory APIs fail when admitted. The replacement removes 140 net lines from the scanner, and the focused suite passes 68 cases. Test/spec globs, cast allowlist, fake-port scan, and floors **22 / 240 / 45** are unchanged. The three setup-file factories still require migration before widening the source walk. This closes only the delegated classifier row; step 4's migration set remains open.

**Takeover verification (2026-09-20 UTC, PR #923):** Re-executing the old classifier against these tests reproduces **27 failed / 41 passed**. Seven failures are diagnostic-text changes; normalizing only that message leaves **20 behavior failures / 48 passed** (the seven indirect forms and 13 noncanonical forms above). Restoring the replacement passes **68/68**. Current `dev`, including #922's chronological-bound regression proof and #919/#924's cron/deployment work, is integrated without changing their implementation or tests. The scan still recognizes direct `vi.mock`/`vi.doMock` calls with literal own-code module paths; this replacement bounds second-argument classification, not arbitrary JavaScript receiver or module-path resolution.

### Rate-limiter F8 disposition (2026-09-23 UTC)

Recovered from the unpublished September 19 draft, published for durability as WIP `9446b459` on September 21. The draft was not a shipped receipt. Its code applies cleanly to the present base; this section records the independent current-base verification, not a September 19 release.

The next F8 audit corrects two filing claims without rewriting the historical census:

| Item | Claim | Verdict | Evidence (before this disposition) |
| --- | --- | --- | --- |
| F8 subtotal | The named hand-rolled-double set contains 25 sites | **REFUTED** | The listed subtotals are **12 + 2 + 7 + 1 + 1 = 23**, at `drizzle-rate-limiter.test.ts:31-250`, `stripe-portal.test.ts:20,45`, `stripe-customers.test.ts:28-207`, `scripts/export-question-feedback.test.ts:473` (the filing's 412 anchor moved), and `lib/container.test.ts:669`. The five type lies are separate, so the named actionable total is 28, not 30. All named sites remain in scope; no two additional sites are invented to preserve the arithmetic. |
| Rate-limiter parity | The three existing Postgres cases prove every one of the twelve unit cases | **REFUTED** | `rate-limiter.integration.test.ts:28,53,97` covers window increments/overflow, explicit bounded pruning, and skip-locked progress. It does not prove automatic 24-hour cleanup, its 100-row limit, no cleanup on increment, invalid-input guards, cleanup-failure reporting, or corrupted-counter rejection. |

Fourteen new cases in `rate-limiter-guards.integration.test.ts` replace those gaps before unit deletion. All fourteen failed under targeted production mutations and passed after byte-for-byte restoration. The existing three Postgres cases remain unchanged.

| Removed unit cases (`drizzle-rate-limiter.test.ts`, pre-change lines) | Real-Postgres twin |
| --- | --- |
| Automatic 24-hour cutoff / 100-row limit (27) | New `prunes at most 100 windows…`: 101 expired rows, the exact cutoff row, two fresh counter keys, and two cleanup passes prove both bound and strict retention cutoff. |
| No cleanup on increment (50) | New `increments an existing counter…`: an expired row seeded after counter creation survives the second increment. |
| Cleanup failure result / warning (65, 84) | New `keeps the persisted counter and logs…`: a fault injected only at `db.execute` leaves the real counter upsert intact, returns the full result, and asserts the complete warning context. |
| Invalid limit / window (109, 126) | Six invalid-input cases verify the exact nonblocking response and no call to the real DB's insert method. |
| Non-positive counter (165) | New `rejects a non-positive count…`: the schema permits a stored -1; the real upsert returns zero and the adapter must throw `INTERNAL_ERROR`. This can be forced safely in Postgres and therefore leaves the unit lane. |
| Remaining count clamp (179) | Existing `rate-limiter.integration.test.ts:28` increments beyond the limit and asserts remaining zero. |
| Atomic bounded prune SQL shape (196) | Existing batch/skip-locked cases plus new `deletes only the oldest key-window pair…`: colliding keys/windows prove both join keys and tie order. Spies observe one real statement and no wrapper transaction; no canned query result or SQL-string matching remains. |
| Invalid prune limits (235, 247) | Three invalid-limit cases verify zero and no call to the real DB's execute method. |

Only the impossible empty driver-response case (143) remains as a typed prepared-query fault unit. Its error-code mutation fails before the restored source passes. Relative to `d711454a`, this change adds **14** integration cases and removes **11** unit cases; the baseline is **349 integration / 5,720 unit**, not the draft's stale 291 / 4,243. The cast floor falls **240 → 228** (48 → 47 files); own-code/port-double floors stay **22 / 45**. Twelve of the 23 enumerated F8 doubles are disposed; the remaining eleven and five type lies remain work. `FakeRateLimiter` retains its dated shared-contract waiver; additional real-adapter tests are not fake↔real parity.

**Current-base red/green receipt (2026-09-23 UTC):** Baseline 17/17 Postgres cases passed at 02:02:26Z before deletion. The original 12-case unit file was present during the integration mutation proofs. From 02:03:13Z to 02:05:10Z, every mutation below exited 1 on its named assertion. After byte-for-byte adapter restoration, 17/17 integration and the one retained unit passed at 02:05:27Z; `pnpm lint:doubles` reported `issues=0`. Filtered mutation invocations omit unrelated cases; no skip was added. Sanitized logs are `/private/tmp/codex-archive-dispositions.c8noac/rate-red-*.log` and `rate-green.log`.

| Production mutation | Independently failing replacement |
| --- | --- |
| Retention 24 → 48 hours; automatic bound 100 → 101; strict cutoff `<` → `<=` (three runs) | `prunes at most 100 windows…` fails on the persisted survivor set in each run. |
| Cleanup on every increment rather than first insert | `increments an existing counter…` detects the deleted expired row. |
| Remove input validation | All six invalid limit/window cases fail on their result or no-write assertion. |
| Remove prune-limit validation | All three invalid prune-limit cases fail on execution or rejection. |
| Warning → info; rethrow cleanup failure (two runs) | `keeps the persisted counter and logs…` detects missing warning and lost successful result separately. |
| Accept the zero returned by incrementing corrupted stored state | `rejects a non-positive count…` fails because the call resolves. |
| Remove key join; remove window join; reverse key order; reverse window order (four runs) | `deletes only the oldest key-window pair…` catches excess deletion or the wrong survivors. |
| Remove `SKIP LOCKED` | Existing `skips locked oldest windows…` hits the bounded PostgreSQL lock timeout instead of making progress. |
| Remove remaining-count clamp | Existing `increments within a window…` observes a negative remainder instead of zero. |
| Empty-driver-response error code `INTERNAL_ERROR` → `CONFLICT` | Retained typed error unit fails on the error code. |

Fresh census: raw casts **266 / 58 files** (from 278 / 59); `RepoDb` casts remain **138 / 21**; size suppressions remain **27**. No adapter behavior, fake behavior, schema, policy floor increase, allowlist, timeout or retry policy changes in this disposition.

### Portal F8 disposition (2026-09-23 UTC)

`stripe-portal.test.ts` had two hand-rolled partial clients cast through `unknown` into the full `StripeClient` port. Both now use the existing `FakeStripeCheckoutClient`, with one typed transient-error injection and call-through request observation. The test client and production adapter are unchanged. No test case or assertion is deleted; the successful URL expectation follows the maintained client's existing canned portal response.

The distinction matters: this member is a **stub**, despite the containing class's name. These two cases prove the adapter retries a forced transport error and forwards the requested customer, return URL and idempotency option. They do not prove provider replay, Stripe validation, network availability or portal contents. The existing required `core-app-pages.spec.ts:83` case creates a real TEST-mode portal session and verifies the redirect origin; the four shared Checkout scenarios do not certify portal behavior. The contract register now states that boundary explicitly; no fake behavior is added and no new parity claim is made.

Red receipts on `82f4fc73`: at 02:30:16Z, removing the portal floor entry made `pnpm lint:doubles` exit 1 on its two existing casts. After migration, removing `callStripeWithRetry` made the transient-error case fail at 02:30:38Z; dropping request options made the forwarding case fail at 02:30:50Z. Restoring the byte-identical production adapter passed both cases at 02:30:54Z. Filtered probes omit the unrelated case without adding a skip. Sanitized receipts: `/private/tmp/codex-archive-dispositions.c8noac/portal-ratchet-red.log`, `portal-red-retry.log`, `portal-red-forwarding.log`, `portal-green.log`.

The enforced cast floor falls **228 → 226** (47 → 46 files); raw casts fall **266 → 264** (58 → 57 files). All other floors and the **138 / 21** `RepoDb` census remain unchanged. Fourteen of the enumerated 23 F8 doubles are disposed; nine plus the five type lies remain. This small migration does not close DEBT-472 or expand the fake's provider contract.

### Feedback-export F8 disposition (2026-09-23 UTC)

The unit `selects feedback rows joined to question slugs in newest-first order` in `scripts/export-question-feedback.test.ts` returned its own canned input and inspected query-method names. Its ordering claim was overstated: reversing the production date order left that unit green. Two tests in `tests/integration/question-feedback-export.integration.test.ts` execute the unchanged export query against this clone's real Postgres before that unit is removed:

| Removed unit | Named real-Postgres twins |
| --- | --- |
| `selects feedback rows joined to question slugs in newest-first order` (pre-delete lines 466–485) | `projects stored rating and report fields with their own question slugs` checks every selected field, distinct joined question slugs, and both null and populated attempt/session context. `orders by newest timestamp before descending id ties, not insertion order` gives the largest ID the oldest timestamp and inserts equal-time rows against their expected order. |

Red receipts: six independent production mutations failed from **03:05:56Z to 03:06:44Z** on 2026-09-23: omit the comment projection; substitute the session ID for attempt ID; join through feedback ID instead of question ID; reverse chronological order; reverse ID tie order; and omit date ordering. These are assertion failures, not setup or compilation errors. Both replacement cases passed at **03:06:58Z**, with production restored byte-for-byte, before the old unit was deleted. Lowering the cast floor first failed on the old cast at **03:07:28Z**. Sanitized logs: `/private/tmp/codex-archive-dispositions.c8noac/feedback-red-*.log`, `feedback-old-unit-mutation.log`, `feedback-green-before-delete.log`, and `feedback-ratchet-red.log`.

The CLI's default-adapter wiring test retains a small canned query-result **stub**, now named accordingly; it proves dependency construction/output/connection closure, not database behavior. Its unused call log and identity-check branch retire with the deleted SQL unit. Formatting, privacy and CLI assertions remain. No maintained fake or production adapter changes, so the fake↔real contract register and its waivers are unchanged.

Disposition: one unit removed, two integration cases added; one cast removed. Floors fall **226 → 225** (46 → 45 files); raw casts fall **264 → 263** (57 → 56 files). Own-code/port-double floors remain **22 / 45**, `RepoDb` casts **138 / 21**, and size suppressions **27**. Fifteen of the named 23 F8 doubles are disposed; the other eight and all five type lies remain work. This is not DEBT-472 completion.

**Review correction (2026-09-23 UTC, #1016):** Confirmed the narrow implementation-coupling finding, not an application defect. An equivalent query grouped both descending sort keys in one SQL expression: both Postgres cases passed at **03:33:11Z**, but the CLI unit failed at **03:33:01Z** solely on `orderBy` argument count (one instead of two). Removing the four retained defined-value/argument-count checks from the canned wiring stub makes that unit independent of this equivalent query shape; all 20 unit cases pass. Its CLI output, constructor and connection-closure assertions remain, and the real projection/join/order assertions stay in Postgres. Production source is restored byte-for-byte; no new case, floor change or policy exception is introduced. Receipts: `feedback-review-red.log`, `feedback-review-equivalent-sql.log`, and `feedback-review-shape-green.log` in the same sanitized receipt directory.

### Private-state fixture disposition (2026-09-23 UTC)

The `ClearingAfterReadPracticeSessionRepository` test fixture no longer invents a public `sessions` field with `as unknown as`. It calls the existing public `discard(id, userId)` after reading the one owned, incomplete session. This exercises the same missing-persisted-state condition without depending on private storage. The fake, application port and Postgres adapter remain byte-identical to `dev` `48c51e4c`; no behavior or concurrency-parity claim is added.

Red-first receipts: the removed per-file floor failed on the old private cast at **03:52:07Z**. With the replacement fixture, independently making each of `saveDraftAnswer`, `finalizeDraftAnswer`, `recordQuestionAnswer` and `setQuestionMarkedForReview` return stale state instead of throwing made the existing conflict test fail at **03:52:39Z, 03:52:53Z, 03:52:57Z and 03:53:01Z**, respectively. Every failure was “promise resolved instead of rejecting.” Restoring the source gave **28/28** fake/register cases, zero ratchet issues and a clean typecheck at **03:53:14Z**. Logs: `/private/tmp/codex-archive-dispositions.c8noac/fake-session-ratchet-red.log`, `fake-session-red-*.log` and `fake-session-restored-green.log`.

No assertion or test is removed. The deliberate legacy-input cast in the same file remains under its existing intentional-invalid policy; it is not counted as newly disposed. Enforced casts fall **225 → 224** (45 → 44 files); raw casts fall **263 → 262** (56 files unchanged). Other floors, `RepoDb` casts and size suppressions remain **22 / 45**, **138 / 21** and **27**. This resolves one of the five named type lies; the other four and eight remaining F8 doubles remain work. The contract register records fixture maintenance while retaining the dated waiver and all database limitations.

### Controller transaction fixture disposition (2026-09-23 UTC)

The rollback fixture now subclasses `DrizzlePracticeSessionRepository`, overriding only `recordQuestionAnswer` to inject the same unforceable write failure. Its inherited lookup executes against the real transaction before the fault. It no longer spreads away the repository prototype and asserts the result back to the full class. No production adapter, use case, maintained fake or application port changes.

Red-first receipts on `27ee2758`: the old eleven cases passed at **04:38:12Z**. Removing the cast floor failed at **04:38:29Z**; removing the size suppression failed at **04:38:20Z** on 1,344 unsuppressed lines. The added inherited-read assertion failed against the old spread fixture at **04:38:36Z** (`findByIdAndUserId is not a function`). After replacement, moving the attempt insert outside the transaction made the preserved zero-row assertion fail at **04:40:30Z**, with one committed row instead of zero. An earlier duplicate-insert probe failed on the wrong error at 04:39:49Z; it is not claimed as the rollback assertion proof. All mutations were restored byte-for-byte before the green run. Sanitized logs: `/private/tmp/codex-archive-dispositions.c8noac/controllers-{baseline,ratchet-red,size-red,prototype-red,rollback-leak-red,final-green}.log`.

The same eleven cases split by concern: four question-controller cases retain `controllers.integration.test.ts`; three stats/review cases move to `controllers-history.integration.test.ts`; four Stripe/Clerk cases move to `controllers-webhooks.integration.test.ts`. Existing integration database/cleanup/question helpers replace local duplicates; the auth constructor helper moves unchanged to `helpers.ts`. The webhook-specific user helper retains its required Clerk ID. All eleven case names and all ten other test bodies are mechanically identical after normalizing only the explicit shared-helper arguments and `wrongChoiceId` → `incorrectChoiceId`. The rollback case keeps its original rejection and zero-row assertions and adds one inherited-read assertion. No case is deleted or skipped.

The files are **439 / 380 / 431** lines, replacing the 1,345-line file and retiring its suppression (**27 → 26**). Enforced casts fall **224 → 223** (44 → 43 files); raw casts fall **262 → 261** (56 → 55 files). Other floors remain **22 / 45** and `RepoDb` casts **138 / 21**. Two of the five named type lies are now disposed; the three container private-field lies and eight F8 doubles remain. DEBT-469's historical ledger gets a forward pointer; DEBT-468's separate provider-fixture proposals are not claimed complete. The live integration inventory's stale ten-case controller count is corrected to the measured eleven.

**Preceding increment release receipt:** #1016 and #1017 are promoted through #1018 (`27ee2758`). Main CI **35818135479** passed at **2026-09-23 04:35:17Z** with 5,708 unit, 411 browser, 365 integration plus six provider opt-in skips, and 52/52 E2E without failure/retry markers. Vercel held the Ready build until that check, then assigned production domains at **04:35:19.879Z**. Production home/health returned 200; dev/main shared tree `ac39c92524c3fe5361695412284c425ad496d4d6`. This controller increment is not included in that prior release receipt.

### Container wiring disposition (2026-09-23 UTC)

The two private-identity units in `lib/container.test.ts` are replaced only after their named public-contract twins pass in `tests/integration/container-stripe-wiring.integration.test.ts`:

| Removed unit | Replacement and boundary |
| --- | --- |
| `shares Stripe price IDs between subscription repository and payment gateway` | Two `uses the configured monthly/annual price…` cases normalize a genuinely signed event through the real SDK, persist through the container-created real Postgres repository, inspect the stored price, and read back the expected domain plan. |
| `uses the injected getStripe override when creating the payment gateway` | `uses the injected SDK to verify the signature through the public gateway` observes the actual SDK verifier calling through and checks the public event result, without reading private fields. |

The first old assertion protected object identity, not configuration: copying the unchanged price values into a new object made that unit fail at **05:07:05Z**, while the three new cases passed at **05:07:07Z**. The replacement protects values independently in each collaborator. Its HTTP input is a canned transport **stub**, not a simulated Stripe implementation or fake↔provider contract; signature verification, SDK request handling and Postgres are real. No Stripe request leaves the process. The injected SDK case uses a non-subscription event so it needs no HTTP response. No maintained fake, adapter, API pin or contract waiver changes.

Red-first proofs on `29d87984`, 2026-09-23 UTC: swapping gateway prices failed both domain-plan assertions at **05:07:24Z**; swapping repository prices failed both persisted-price assertions at **05:07:31Z**; ignoring the injected SDK failed its public-use observation at **05:07:43Z**. Restoring byte-identical production files passed three replacement cases and all six old units at **05:07:54Z**, before deletion. Lowering the per-file cast floor from 15 to 8 first failed at **05:08:07Z**. After deleting only the two mapped units, three integration cases and four retained units, the ratchet and typecheck passed at **05:08:26Z**. Filtered mutation runs do not add skips. Sanitized logs: `/private/tmp/codex-archive-dispositions.c8noac/container-{identity-red,equivalent-green,red-gateway-prices,red-repository-prices,red-sdk-injection,green-before-delete,ratchet-red,restored-green}.log`.

This removes seven enforced casts and eleven textual casts: **223 → 216 / 43 files** enforced; **261 → 250 / 55 files** raw. `RepoDb` casts remain **138 / 21**, own-code/port-double floors **22 / 45**, and size suppressions **26**. All five named type lies are now disposed. The eight remaining F8 doubles, own-code mocks, repository dispositions, fake-extension work and legacy-pattern adjudication remain Open. This is not overall DEBT-472 completion.

### Container transaction disposition (2026-09-23 UTC)

The unit `uses repository factories inside createStripeWebhookDeps transactions` is replaced by `uses overridden repository factories in one real transaction and rolls back a failed callback` in `tests/integration/container-webhook-transaction.integration.test.ts`. Four observed factories construct the actual Drizzle repositories. The test proves that their public overrides receive the same actual transaction, that version reads use the pool outside it, and that the callback's inserted event disappears when its error is propagated. No SQL result, transaction implementation or repository behavior is supplied by a double. No production or maintained-fake behavior changes, so no fake-contract claim or waiver changes.

One old assertion overconstrained an implementation detail: explicitly passing the same pool to the outside-transaction factory, instead of relying on its default, made the old unit fail at **05:53:50Z** while the new case passed at **05:53:51Z**. That equivalent form is deliberately accepted. The four independent mutations omitting the callback transaction from event/subscription/customer/consent construction each failed the new case at **05:54:10 / 17 / 23 / 29Z**. A mutation that committed the callback's insert before rethrowing the same error failed the new zero-row assertion at **05:55:06Z**, while the old canned unit stayed green at **05:55:07Z**. Byte-identical production restoration passed the new case and all four old units at **05:55:23Z**, before deletion.

Lowering the cast floor **8 → 6** and removing the one container port-double floor first failed at **05:55:57Z**. After deleting only that mapped unit, the new integration case, three retained units, typecheck and `lint:doubles` passed at **05:56:30Z**. Sanitized logs: `/private/tmp/codex-archive-dispositions.c8noac/container-transaction-{old-shape-red,equivalent-green,red-events,red-subscriptions,red-customers,red-consents,red-rollback,old-rollback-green,green-before-delete,ratchet-red,restored-green}.log`. Identity assertion diagnostics are booleans so a failure cannot serialize a database client's connection configuration.

Measured deltas: enforced casts **216 → 214 / 43 files**; raw casts **250 → 246 / 55 files**; maintained-port doubles **45 / 18 files → 44 / 17 files**. Own-code factories remain **22**, `RepoDb` casts **138 / 21**, and size suppressions **26**. Sixteen of the 23 named F8 doubles and all five type lies are disposed; the seven Stripe-customer doubles and the broader steps 4–6 remain Open. Local mutation proofs do not substitute for the pending full gate, review and promotion.

### Customer fault and request-shape disposition (2026-09-23 UTC)

Four `stripe-customers.test.ts` cases now use the existing typed `FakeStripeCheckoutClient`: unsafe-user-ID validation before I/O, missing returned customer ID, explicit idempotency-key forwarding, and retry after an injected timeout. Their request/error assertions are preserved; retry call arguments now infer their type instead of using two extra type assertions. Each case has at most two local stubs/spies. The customer's canned create member and any test-local empty Search answer are **shape fixtures**, not Search state, replay, provider validation or fake↔real parity. Production and the maintained fake are byte-identical to the base. The contract register now makes this limited customer surface explicit; no shared-contract coverage or waiver is broadened.

Red-first on `a37a5173`, 2026-09-23 UTC: lowering the per-file cast floor **7 → 3** failed at **06:22:39Z** before migration. Independently bypassing user-ID validation failed at **06:24:04Z**; accepting an empty returned ID failed at **06:24:29Z**; ignoring the supplied idempotency key failed at **06:24:34Z**; removing the create retry failed at **06:24:39Z**. These were assertion failures, not startup failures. An intermediate patch failed to match indentation and made no source change; it is not a red-test receipt. After byte-identical production restoration, all seven customer cases plus 17 register cases, typecheck and the ratchet passed at **06:24:54Z**. Filtered mutation runs omit unrelated tests without adding skips. Sanitized receipts: `/private/tmp/codex-archive-dispositions.c8noac/customer-fault-{ratchet-red,red-validation,red-missing-id,red-idempotency,red-retry,restored-green}.log`.

**F8 claim correction, not a new Search implementation:** the first old customer stub imitates SDK receiver binding through `_makeRequest`; it does not implement pagination. The three matching/absent/duplicate Search cases remain byte-identical. The maintained Checkout fake has no Search behavior, and its four provider scenarios do not cover Search. [Stripe's Search documentation](https://docs.stripe.com/api/customers/search) explicitly disclaims immediate read-after-write consistency (normally searchable in under a minute, potentially delayed longer in an outage); removing these cases needs a genuine bounded provider proof, not an invented immediate-visibility fake. Their three casts remain in the reduced floor, not silently excused as covered.

Measured deltas: enforced casts **214 → 210 / 43 files**; raw casts **246 → 242 / 55 files**. Own-code factories **22**, maintained-port doubles **44 / 17 files**, `RepoDb` casts **138 / 21**, and size suppressions **26** are unchanged. Twenty of the 23 named F8 double sites and all five type lies are disposed; three Search-behavior sites and the rest of steps 4–6 remain Open. No test case or assertion is deleted in this increment. Full gate, review and promotion remain pending.

### Customer Search replacement attempt (2026-09-23 UTC)

This is an **unlanded, unproven candidate**, not coverage or a production fix. Three API-only Playwright cases used isolated TEST customers, never the shared E2E customer. The unmutated run at **07:08:59–07:09:52Z** failed its existing-match and duplicate-match cases: the adapter created a new customer instead of observing the fixture's metadata matches. The fixture had waited for email Search visibility, not the metadata query actually consumed. A counts-only probe observed different email/metadata visibility times; it does not establish Stripe's internal cause.

After explicitly waiting for both query preconditions, the three candidate cases passed once at **07:12:47–07:14:12Z**. This is not sufficient deletion proof: the duplicate-check mutation at **07:17:45Z** failed for the wrong reason (new-customer creation instead of reuse of either expected match), so the fixture still did not establish the claimed premise reliably. The candidate was stopped, not retried or given a longer timeout to obtain green. All production changes were restored byte-identically, all seven existing units and the three Search cast entries remain, and the candidate is outside the repository at `/private/tmp/codex-archive-dispositions.c8noac/stripe-customer-search-provider.unlanded.spec.ts`. Sanitized failing receipts and artifacts are `customer-search-green-before-delete.log`, `customer-search-restored-failure-results/`, `customer-search-validated-red-duplicate.log`, and `customer-search-inconclusive-mutant-results/` in that directory. No application root cause or provider-parity claim is made. These three step-4 sites remain Open; revisit only with a reliable bounded fixture and mutation failures for the intended assertions.

**2026-09-25 UTC forward pointer:** revisited and closed; see [the customer Search contract disposition](#customer-search-contract-disposition-2026-09-25-utc). The fake now models Search, and the contract's real half polls each read within a bound. A TEST probe found that a Search read can briefly return nothing after an earlier read returned the match. That is consistent with this candidate's wrong-reason failure, but no application root cause is claimed.

### Client reporter boundary disposition (2026-09-23 UTC)

The four pure-logic suites `fire-and-forget.test.ts`, `practice-page-incomplete-session.test.ts`, `practice-session-page-logic.test.ts` and `question-page-logic.test.ts` no longer replace the owned `reportClientError` module. They execute the real reporter and observe `captureException` at the external Sentry SDK boundary, preserving each original error and component/action pair as emitted tags. No production code, fake behavior, adapter contract or runtime filtering changes; the maintained-fake contract register is therefore unchanged. No test is deleted, skipped or weakened. An AST comparison confirms all **106** titles and **270** assertions are preserved, apart from the intentional endpoint/tag-envelope change.

Red-first on `43621d63`, **2026-09-23 UTC**: disabling the real tagged capture left the old four suites green (**106/106**, **07:22:30Z**), exposing their blind spot. The new external-boundary assertions failed **14** cases with the old own-code factories still present (**07:25:43Z**). Removing the four floor entries made `pnpm lint:doubles` reject all four sites (**07:26:02Z**). After removing the factories, all 106 cases, typecheck and the ratchet passed (**07:26:26Z**). Disabling the real tagged capture again failed the same 14 assertions (**07:26:49Z**); byte-identical production restoration passed **106/106** (**07:27:14Z**). Logs are `/private/tmp/codex-archive-dispositions.c8noac/reporter-{old-blindspot-green,external-boundary-red,floor-red,boundary-green,live-mutation-red,restored-green,census}.log`.

Measured floors: own-code factories **22 / 13 files → 18 / 9**; casts **210 / 43** and maintained-port doubles **44 / 17** are unchanged. Three additional browser-setup factories remain outside the existing test/spec scan scope; this migration does not exempt or declare them complete. The fifth reporter mock in the mark-for-review hook, other F6 mocks, repository dispositions and steps 5–6 remain work. Full gate, review and promotion of this increment remain pending.

### Browser support disposition (2026-09-23 UTC)

The three own-code factory mocks in `practice-session-page-model.browser.setup.ts` are replaced by the canonical Browser Mode `{ spy: true }` form and `vi.mocked` on the actual controller exports. This is a typed server-action boundary, not a fake implementation or provider-parity claim. The eleven exported helper spies retain their existing API. No production code, maintained fake, adapter behavior, assertion, skip, floor or allowlist changes. The contract register is unchanged because no maintained behavior double changed.

The existing source reader adds only four globs for `.setup.ts(x)` and `*-test-helpers.ts(x)`. Three new reader assertions failed at **07:50:06Z** on `e18cb298`; after widening the reader, `pnpm lint:doubles` failed at **07:50:32Z** on exactly the three previously excluded factories. They were migrated without adding floor entries. Real-export typing then rejected copied fixture drift at **07:51:16Z**: the submission fixture omitted required `referenceMd` and allowed a nullable correct choice, two wire timestamps were `Date` instead of strings, and four deferred summaries used `unknown`. Fixtures now use the canonical `PreviousSubmission`, serialized timestamps and the actual controller return type. No application type was widened and no cast was added.

Typecheck, all **71** scanner cases and the ratchet passed at **07:52:16Z**. A formatting check then rejected an unused import before any browser test ran; removing that import led to **44/44** affected browser cases across six files passing at **07:56:04–07:56:18Z**. All 44 test titles and their assertion expressions remain unchanged by AST comparison. The expanded reader includes **14** support files; measured floors remain **18 own-code factories / 9 files**, **210 casts / 43**, and **44 maintained-port doubles / 17**. No new scanner or resolver was added. Sanitized receipts are `/private/tmp/codex-archive-dispositions.c8noac/browser-support-{reader-red,factory-red,types,unit-green,runtime-green,final-census}.log`. Full gate, review and promotion of this increment remain pending.

### Mark-for-review reporter disposition (2026-09-23 UTC)

Five async jsdom cases move to `use-practice-session-mark-for-review-errors.browser.spec.tsx`, using actual React state and the real client reporter. The existing five-case browser suite is unchanged; the initial-state/no-question unit stays in jsdom with its three assertions and a `beforeAll` import. The only doubles in the new suite are the injected typed request and external Sentry capture. No production code, maintained fake or adapter changes, so no fake-contract row changes. This is hook-state/error-reporting coverage, not a provider contract.

| Deleted async unit case | Named browser twin |
| --- | --- |
| `passes an idempotency key when marking for review` | `sends an idempotency key and updates real session and review state on success` |
| `sets loadState error when mark-for-review request throws` | `reports a thrown error, preserves state and reuses the indeterminate request key` |
| `sets loadState error when mark-for-review request returns an error result` | `reports an INTERNAL_ERROR result without changing session or review state` |
| `rotates the mark key after a determinate cached failure` | `reports NOT_FOUND and rotates the key after a determinate cached failure` |
| `uses the mutation timeout tier for mark-for-review requests` | `waits past the read tier then reports the 15-second mutation timeout` |

An additional `RATE_LIMITED` case proves the real reporter suppresses an expected business outcome. The old copied filter accepted only `INTERNAL_ERROR`; the real filter also reports `NOT_FOUND`. The replacement tests this existing production policy instead of copying it.

Red proofs on `ce37a4be`, **2026-09-23 UTC**: the old owned-module factory failed four new capture assertions at **08:21:56Z**. Disabling actual tagged capture left all six old units green at **08:25:59Z**, then failed four new browser cases at **08:26:04Z**. Independent mutations failed the intended assertions for NOT_FOUND filtering (**08:26:17Z**), RATE_LIMITED suppression (**08:26:24Z**), request-key presence (**08:26:31Z**), determinate-key rotation (**08:26:38Z**), indeterminate-key reuse (**08:26:45Z**), the mutation timeout tier (**08:26:52Z**), session-state updates (**08:27:05Z**) and review-state updates (**08:27:14Z**). Source restoration was byte-identical before all **11 browser + 6 old unit** cases passed at **08:27:22Z**, before deletion. The lowered zero-site floor failed against the old factory at **08:27:48Z**; after migration, **11 browser + 1 retained unit**, typecheck and the ratchet passed at **08:28:08Z**. Filtered mutation commands' unselected cases are not new policy skips.

Sanitized receipts are `/private/tmp/codex-archive-dispositions.c8noac/mark-reporter-*.log`. Own-code factories fall **18 / 9 files → 17 / 8**; cast and maintained-port floors remain **210 / 43** and **44 / 17**. No assertion weakening, allowlist, timeout increase or production change. Full gate, review and promotion of this increment remain pending; the other F6/RepoDb dispositions, three unproven Search cases and steps 5–6 remain Open.

**08:47 UTC fixture correction:** Self-review replaced a constant mounted predicate with the actual `useIsMounted` hook, leaving only the request and Sentry doubles. A deliberate mutation preventing that hook's mount transition failed all six new cases at **08:46:54Z**; byte-identical restoration passed all **11 browser + 1 unit**, typecheck and the ratchet at **08:47:25Z**. The first local gate was intentionally stopped before E2E/push for this correction, not counted as a full pass; no E2E run was interrupted or retried. The complete gate must run again before the first push.

### History disclosure disposition (2026-09-23 UTC)

`history-sessions-tab.test.tsx` no longer replaces the owned `useHistorySessions` hook. Its sixteen initial-render cases use the real hook and retain all **102** assertions. Only two expanded-state units retire, after their named existing Browser Mode twins prove the actual click-to-state path. The browser suite keeps all nine titles and all **23** existing assertions; the flat-panel and chevron constraints move into its accessibility case without changing the UI. The unit file shrinks **737 → 592** physical lines and the browser file grows **595 → 610**, both below the unchanged size rule. No maintained fake or adapter changes; the contract register is unchanged.

| Deleted unit | Real-hook browser twin |
| --- | --- |
| `renders expanded breakdown panel as a flat disclosure region` | `wires disclosure accessibility attributes and region semantics on expand` — preserves region identity/name, expanded label, all seven panel-class constraints and the rotated chevron |
| `does not render a redundant Review session button inside breakdown content` | `does not render a redundant Review session action in the expanded breakdown panel` — first requires a loaded question link, then rejects the redundant link |

Red-first on `a32b20e8`, **2026-09-23 UTC**: omitting the real hook's selected-session transition left all **18 old units green** but failed the browser twin's `aria-expanded` assertion (**08:57:14Z**). The ported assertion then rejected independently removed `border-t` (**08:57:49Z**), added panel background (**08:58:23Z**), added rounded panel (**08:58:52Z**) and removed chevron rotation (**08:59:17Z**). Adding a redundant Review session link failed its named browser twin (**09:00:02Z**). Production restoration was byte-identical before **9 browser + 18 old units** and typecheck passed (**09:00:32Z**), before deletion. The removed floor entry failed against the still-present owned factory (**09:00:55Z**); after deletion, **9 browser + 16 units**, typecheck and the ratchet passed (**09:01:23Z**). An AST comparison preserved the other titles/assertions; filtered mutation commands did not add policy skips.

Sanitized receipts: `/private/tmp/codex-archive-dispositions.c8noac/history-*.log`. Own-code factories fall **17 / 8 files → 16 / 7**; casts **210 / 43** and maintained-port doubles **44 / 17** are unchanged. No production implementation, fake behavior, suppression, timeout or allowlist changes. Full gate, review and promotion remain pending; the remaining F6/RepoDb dispositions, unproven Search replacement and steps 5–6 remain Open.

### Quick Practice count-hook disposition (2026-09-23 UTC)

The owned count-hook factory in `quick-practice-client.test.tsx` and the hook spy in `quick-practice-client.browser.spec.tsx` both retire. Browser Mode now observes the real hook, using the existing typed real-export controller spies at the Node/browser boundary. No new fake, adapter change or parity claim is involved. The three interaction cases preserve their five assertions; their navigation locators now require the actual resolved count labels rather than fabricated count-less state. The two existing count-refresh cases retain their four assertions.

The one deleted unit, `renders status segmented control below the page heading`, has a same-named twin in `quick-practice-client-status-counts.browser.spec.tsx`: real count completion, heading, Back to Practice link, Status fieldset/legend, all three counts, selected Incorrect state, and below-heading order. The nine retained units keep all ten assertions and use a `beforeAll` component import instead of per-test imports. The initial-state unit now uses the real hook. The unit file shrinks **171 → 110** lines; affected browser cases increase **5 → 6**.

Red-first against `c33db111`, **2026-09-23 UTC**: omitting the real successful count-state transition left all **10 old units green**, while the new browser twin failed to find `Incorrect (3)` (**09:19:41Z**). Selecting the wrong status failed `aria-pressed` (**09:20:18Z**); placing the control above the heading failed the DOM-order assertion (**09:20:53Z**). Byte-identical production restoration passed **6 browser + 10 old units** and typecheck before deletion (**09:21:04Z**). Removing the floor entry while the factory still existed failed the ratchet (**09:21:28Z**). With the actual hook used in the three interaction cases, omitted navigation and submission failed all three intended assertions (**09:22:38Z**). Restored production passed **6 browser + 9 units**, typecheck and the ratchet (**09:23:11Z**); an AST census verified the retained assertions and byte-identical production sources.

Floors fall **16 / 7 files → 15 / 6** own-code factories; casts **210 / 43** and maintained-port doubles **44 / 17** are unchanged. The removed hook spy was not counted as a factory; its removal is not claimed as an extra floor decrement. No timeout, skip, allowlist, suppression, production behavior or contract-register change. Sanitized receipts: `/private/tmp/codex-archive-dispositions.c8noac/quick-*.log`. Full gate, review and promotion remain pending; other step-4 dispositions and steps 5–6 remain Open.

### Practice starter state disposition (2026-09-23 UTC)

The two `practice-page-client.test.tsx` units replaced both the controls hook and the component barrel, then asserted the status text emitted by their own substitute. Their parent-prop contract is preserved through the actual `PracticePageClient`, controls hook and `PracticeSessionStarter` in Browser Mode. Only the existing sealed-ESM server-action controller boundary uses typed real-export spies; no application hook, child, navigation or reporter is replaced.

| Deleted unit | Named real-component twin in `practice-page-client.browser.spec.tsx` |
| --- | --- |
| `passes sessionStartStatus to PracticeSessionStarter while sessionStartStatus is loading` | `disables the real session starter while session start is loading`: a click dispatches the actual hook, the pending controller result renders `Starting…` disabled, and the deliberately rejected result settles the timer and renders its alert. |
| `passes sessionStartStatus to PracticeSessionStarter while sessionStartStatus is idle` | `enables the real session starter while session start is idle`: initial loads complete and the actual `Start session` control is enabled. |

Mutation proofs against `fe214e15`, **2026-09-23 UTC**: omitting the real loading transition left **both old units green**, but the new loading twin failed to find `Starting…` (**10:02:09Z**). Forcing the parent's status prop to idle failed that loading twin (**10:02:45Z**); forcing it to loading failed the idle twin (**10:03:15Z**). Removing the real button's disabled behavior again left both old units green and failed the new disabled-state assertion (**10:03:46Z**). Production was restored byte-identically, and **2 browser + 2 old units** plus typecheck passed before deletion (**10:04:15Z**). Removing the floor entry while the two factories remained failed the ratchet (**10:04:40Z**).

The first attempted fixture incorrectly returned a tags array instead of the controller's `{ rows }` response; its runtime/type failures are not behavioral red proof. The corrected typed fixture passed before the recorded mutations. The deferred start always settles in `finally`, including a failed assertion, so the test does not leave the real mutation timer pending. Filtered mutation commands omit unselected cases only; no policy skip or timeout is added.

After deletion, both browser twins, typecheck, the ratchet and byte-identical production checks pass (**10:05:03Z**). Own-code factories fall **15 / 6 files → 13 / 5**; casts **210 / 43** and maintained-port doubles **44 / 17** are unchanged. No fake or adapter changes, so the contract register is unchanged. Sanitized receipts: `/private/tmp/codex-archive-dispositions.c8noac/practice-client-*.log`. Full gate, review and promotion remain pending; the other F6/RepoDb dispositions, unproven Search replacement and steps 5–6 remain Open.

**2026-09-23 UTC restart re-verification:** The restart removed the earlier `/private/tmp` logs; the summaries above remain historical, not a claim that those files are still available. Against `44299ad3`, both replacement browser cases and both temporarily restored old units passed before fresh mutation proofs. Omitting the actual loading transition again passed both old units and failed the loading twin (**13:10:46Z**); forcing the parent prop to idle failed that twin (**13:11:20Z**), and forcing it to loading failed the idle twin (**13:11:50Z**). Removing the actual button's disabled behavior passed both old units but failed the new disabled assertion (**13:15:02Z**). One preceding command used a nonmatching test-title filter and selected zero browser cases; it is explicitly not proof. All three production files were restored byte-identically, then **2 browser + 2 old units** and typecheck passed before deletion (**13:15:26Z**). The lower floor also failed on the two restored factories (**13:10:15Z**). New sanitized receipts are under `.git/codex-debt-resume.dnZFSM/practice-*.log` in this clone.

### Root-layout provider-boundary disposition (2026-09-23 UTC)

`app/layout.test.tsx` no longer replaces the application `Providers` and `ThemeProvider` wrappers. All **nine test titles and fourteen existing assertions** remain; two additional assertions independently check the nonce received at each external boundary, so one correct recipient cannot conceal the other's missing nonce. The existing Next headers/font stubs remain; the two replacement observation stubs are at `next/dynamic` (Clerk's provider boundary) and `next-themes`, using the installed vendor prop types. They observe forwarded props only, not authentication, theme transitions or provider parity. The existing four-substitute suite gains no additional module substitute, and no production seam is changed solely to reduce the floor.

Red proofs against `187e36fe`, **2026-09-23 UTC**: removing nonce forwarding inside the actual `Providers` left all nine old units green (**13:33:46Z**), while the revised suite failed on the Clerk-boundary nonce (**13:34:17Z**). Removing the actual theme wrapper's nonce independently failed the other recipient assertion (**13:34:46Z**). Overriding forwarded `forcedTheme` and `defaultTheme` with light independently failed the preserved dark-policy assertions (**13:34:56Z**, **13:35:11Z**). Lowering the floor while restoring the old two factories failed on both sites (**13:35:46Z**).

Both production wrappers and the root layout are restored byte-identically. The nine layout cases plus five existing provider-wrapper cases, typecheck and the ratchet pass (**13:36:29Z**); an AST comparison confirms all original titles/assertions survive. The suite snapshots and restores its explicit `NEXT_PUBLIC_SKIP_CLERK=false` fixture. Candidate own-code factories fall **13 / 5 files → 11 / 4**; cast/port floors remain **210 / 43** and **44 / 17**. No test deletion, fake/adapter change, contract-register change, policy exception or new helper framework. Sanitized receipts: `.git/codex-debt-resume.dnZFSM/root-*.log`. Full gate, review and promotion remain pending; other steps 4–6 remain Open.

### AuthNav user-button boundary disposition (2026-09-23 UTC)

`components/auth-nav.test.tsx` no longer replaces the application `AuthUserButton` wrapper. Its five `vi.doMock('./auth-user-button')` factories are replaced by one typed observation stub at Clerk's `next/dynamic` boundary (`ComponentProps<typeof import('@clerk/nextjs').UserButton>`), so the real wrapper executes and the stub reports the `appearance` it actually receives. All **nine titles and thirty-one assertions** remain; two assertions are added so the real wrapper's own 44px touch-target container is checked alongside the forwarded trigger sizing. The external `next/link` stub and the CI-fallback `@clerk/nextjs` throw stub remain. No production seam is changed.

Red proofs against `d03122c0`, **2026-09-23 UTC**: with the old suite, removing the wrapper's `appearance` forwarding (**15:10:04Z**) and removing its 44px container classes (**15:10:06Z**) each left all nine units green. With the revised suite, the same two mutations each failed the 44px case (**15:11:31Z**, **15:11:33Z**); removing the user button from `AuthNav` failed the five authenticated cases (**15:11:35Z**); dropping the appearance prop at the `AuthNav` call site failed the 44px case (**15:11:36Z**). Production was restored byte-identically after each mutation, with an empty production diff at **15:11:39Z**. Lowering the floor while the old five factories were present failed the ratchet (**15:12:02Z–15:12:21Z**); with the revised suite it passes with `issues=0`.

Candidate own-code factories fall **11 / 4 files → 6 / 3**; cast/port floors remain **210 / 43** and **44 / 17**. No test deletion, fake/adapter change, contract-register change, policy exception or new helper. **2026-09-23 UTC #1035 release receipt:** Root-layout #1034 is promoted through #1035 (`fa918a424fed64239c79d543e72483ae0a47f8b7`, merged **15:20:24Z**) after the executable provenance proof (source approval **5292735383** at 15:00:02Z preceding the 15:06:56Z merge, zero threads) and promotion CI **35879324459** (52/52 E2E, no retries). Main CI **35880818989** passed `test` at **15:30:47Z**. Vercel was Ready at **15:21:52.538Z**, observed STAGED without a production alias from **15:22:01Z** through **15:30:17Z**, then assigned production at **15:30:50.335Z**. Both trees equal `7c4305365c15d9a16ed6c9d5b750cd12b7bc79d8`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **15:32:22Z**.

Sanitized receipts: this clone's `.git/claude-debt-resume/authnav/`. **2026-09-24 UTC status:** full gate passed on `ee176555`, merged as #1036 (`2108e0b2`) and promoted through #1037; the release receipt sits in the following section. Originally: the remaining three own-code factory files (the `layout-shell` and `marketing-layout` DEBT-421 sentinels and the reconcile cron route composition root) and steps 4–6 stay Open.

### Shell toggle sentinel disposition (2026-09-23 UTC)

`app/(app)/app/layout-shell.test.tsx` and `components/marketing/marketing-layout.test.tsx` each carried one own-code `vi.mock('@/components/theme-toggle')` whose only job was to make a null-rendering client component observable in static markup, so their DEBT-421 sentinel units could assert it stayed unmounted. Nothing in either shell imports the toggle today, so the factories never executed. Each sentinel unit is replaced by a named Browser Mode twin (`layout-shell.browser.spec.tsx`, `marketing-layout.browser.spec.tsx`) that mounts the real shell and asserts no `Toggle theme` control appears once the real `ThemeToggle` would have rendered after mount. The only stub is the external `server-only` marker; no application module is replaced and no production seam changes. The remaining ten and eleven unit titles are preserved.

Red proofs against `ee176555`, **2026-09-23 UTC**: re-mounting `<ThemeToggle />` in the app shell failed the retained old sentinel unit and the new browser twin (**15:44:18Z**); the same mutation in the marketing layout failed both again (**15:44:37Z**). Detection power is equal; the twin observes the real control rather than a substitute's test id. Production was restored byte-identically after each mutation, with an empty production diff at **15:44:59Z**. With the old unit files restored, the lowered floors failed the ratchet on both files (**15:44:55Z**); with the twins in place `lint:doubles` reports `issues=0`.

Candidate own-code factories fall **6 / 3 files → 4 / 1**; cast/port floors remain **210 / 43** and **44 / 17**. The last own-code factory file is the reconcile cron route composition root, dispositioned separately. **2026-09-23 UTC #1037 release receipt:** AuthNav #1036 merged as `2108e0b277860f3678efdb6dc5579cd833fa77c8` at **15:55:05Z** (exact-head approval **5293278693** at 15:46:28Z, zero threads, CI **35883402002** with 52/52 E2E) and is promoted through #1037 (`d86406480d0de13c7763c0d224a80f928c8f7921`, merged **16:06:20Z**) after the executable provenance proof and promotion CI **35885188081** (52/52 E2E, no retries). Main CI **35886411309** passed `test` at **16:16:53Z**. Vercel was Ready at **16:07:44.575Z**, observed STAGED without a production alias from **16:08:38Z** through **16:16:52Z**, then assigned production at **16:16:55.850Z**. Both trees equal `b1e82b1dac5f0dad7ecdadd029cc62e6a03f331f`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **16:17:56Z**.

**2026-09-23 UTC hosted-CI failure, root-caused and fixed:** PR #1038's first head (`84056177`) failed the hosted **Browser tests with coverage** step twice (run **35888798953**, attempts 1 and 2) while 68 / 69 files and 420 / 420 tests passed: an untouched file failed to import with `Vitest failed to find the runner`. The cause was documented on the PR before the single permitted re-run, then reproduced locally on a cold dependency cache: the two new shell twins pull `next/server` into Browser Mode, Vite discovered it mid-run (`dependency optimized: next/server` → `optimized dependencies changed. reloading` → `[vitest] Vite unexpectedly reloaded a test`), and the reload killed whichever file was loading. The warm local cache in the full gate never re-optimized, which is why the gate was green. Fix: pre-list `next/server` in `vitest.browser.config.mts` `optimizeDeps.include`, the list the config already keeps for the other server-side modules the browser graph reaches; a cold-cache run after the fix shows no optimizer or reload line and passes 69 / 422 (**16:47:51Z**). Receipts: `.git/claude-debt-resume/sentinel/ci-1038-*.log`, `browser-cold-cache-repro.log`, `browser-cold-cache-fixed.log`.

Sanitized receipts: this clone's `.git/claude-debt-resume/sentinel/`. **2026-09-24 UTC status:** full gate passed on `b8a68c5f`, merged as #1038 (`1a1db4c1`) and promoted through #1039; the release receipt sits in the following section. Originally: steps 4–6 stay Open.

### Reconcile cron route seam disposition (2026-09-23 UTC)

`app/api/cron/reconcile-stripe-subscriptions/route.test.ts` carried the last four own-code module factories: two `importOriginal` spreads over the reconciliation jobs, one over `@/lib/container` and one over the drain job, plus a 980-line size suppression. Mirroring `app/api/cron/send-renewal-notices/route-handler.ts`, the route now composes a narrow, client-owned seam from the container (`route-handler.ts`: `cronSecret`, `logger`, `createRateLimiter`, `reconcilePage`, `reconcileAllPages`, `drainPendingStripeCustomerCleanups`); the handler owns only authorization, rate limiting, query parsing, scope routing, the BUG-262 independent drain and status mapping. `route-handler.test.ts` keeps all **26 orchestration titles** on `FakeLogger`, `FakeRateLimiter` and recording job fakes (no module mock, no cast) and adds one cutoff case. The composition assertions the old suite made through a mock container (query shape, transaction repositories, cleanup completion, customer deletion) move to `tests/integration/cron-routes.integration.test.ts`, which now also drains a stale pending customer cleanup through container wiring against real Postgres and the Stripe transport stub, and rejects unauthenticated requests through the exported `GET`/`POST`.

Red proofs against `84056177`, **2026-09-23 UTC**: the handler test failed before the handler existed (**16:27:20Z**). With the seam in place, four handler mutations each failed only their guarding cases: drain skipped after a failed reconcile failed the three BUG-262 cases (**16:29:24Z**); an unclamped limit failed the clamp case (**16:29:25Z**); ignoring partial drain failures failed the partial-failure case (**16:29:26Z**); skipping the header-safety check failed all eight value-free cases (**16:29:27Z**). Two composition mutations each failed the new integration case: the Clerk event no longer marked processed (**16:29:29Z**) and customer deletion replaced by a no-op (**16:29:31Z**). Production was restored byte-identically after each; 35 unit and 7 integration cases plus typecheck pass (**16:29:35Z**). Every old unit title survives (26 → 27).

Candidate own-code factories fall **4 / 1 file → 0 / 0**; cast/port floors remain **210 / 43** and **44 / 17**; the DEBT-469 size suppressions fall **26 → 25** with the deleted file. The production change is a behavior-preserving extraction: `GET`/`POST` and `createReconcileStripeSubscriptionsCronHandler(resolveContainer)` keep their signatures and the integration lane drives the same URL through container wiring. No fake, adapter, dependency, workflow or contract-register change. **2026-09-23 UTC #1039 release receipt:** Sentinel twins #1038 merged as `1a1db4c168b2b80c5604b61f4c4af68a095adba5` at **18:17:31Z** (exact-head approval **5294977571** at 18:15:42Z after the stale first-head approval was dismissed with a rule citation, zero threads, CI **35892215111** with 52/52 E2E) and are promoted through #1039 (`a59c9ecae2443e4f014d6cb63de4cc562cafae76`, merged **18:31:37Z**) after the executable provenance proof and promotion CI **35901559120** (52/52 E2E, no retries). Main CI **35903084432** passed `test` at **18:42:45Z**. Vercel was Ready at **18:32:53.580Z**, observed STAGED without a production alias from **18:33:55Z** through **18:42:10Z**, then assigned production at **18:42:47.736Z**. Both trees equal `551627bf865ced960baf756916f229c3e7650e5f`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **18:43:13Z**.

Sanitized receipts: this clone's `.git/claude-debt-resume/cron/`. **2026-09-24 UTC status:** full gate passed on `01a5c1cb`, merged as #1040 (`b924a7d2`) and promoted through #1041; the release receipt sits in the following section. Originally: once this lands F6 is dispositioned in full, and steps 4–6 stay Open for the remaining F8/RepoDb items.

### User repository disposition (2026-09-23 UTC)

`src/adapters/repositories/drizzle-user-repository.test.ts` replaced its 20-cast hand-rolled Drizzle chain double with **seven typed error-translation cases** at the real prepared-query boundary (`drizzle.mock({ schema })` plus `vi.spyOn(PostgresJsPreparedQuery.prototype, 'execute')`, the attempt-repository precedent). Because `drizzle.mock` has no transactional client, the suite binds `PgDatabase.prototype.transaction` to a `PgTransaction` subclass over the same mock session so faults inside `upsertByClerkId` and `updateEmailByClerkId` still reach the spy; a probe showed that without that stub two upsert cases passed only on the wrapper's `TypeError: this.client.begin is not a function`, never consuming the injected fault. Behavior moves to `tests/integration/user-repository.integration.test.ts`, which gains three twins: `findById` found and absent, a `lockByClerkId` miss, and the transaction-scoped advisory lock observed through `pg_locks` (held inside the transaction, released after commit); the existing lock case now also proves `FOR UPDATE` by the `RowShareLock` it takes and by a second session's `FOR KEY SHARE NOWAIT` on the locked row failing with `55P03` (the weaker row locks also take `RowShareLock` but would let that probe through). Fourteen chain-shape units (find, lock, upsert values, conflict lookups, delete, advisory SQL string) retire in favour of those twins. The unique-violation lookup-failure path gains a case the old suite never had.

Red proofs against `01a5c1cb`, **2026-09-23 UTC**: dropping the delete cause failed old and new suites (**19:01:13Z**); rethrowing the owner-lookup failure raw failed only the new suite while the old stayed green (**19:01:15Z**); `findById` ignoring its row failed the integration twin and the old unit (**19:01:20Z**); dropping `FOR UPDATE` failed the `RowShareLock` twin and two old units (**19:01:23Z**); making the advisory lock session-scoped failed the released-after-commit twin (**19:03:07Z**); with the old unit file restored at its path, the lowered floor failed the ratchet (`20 unknown double cast site(s), above its ratchet floor of 0`) and passes with the new suite. Production was restored byte-identically each time; 7 unit and 19 integration cases plus typecheck pass.

Casts fall **210 / 43 files → 190 / 42**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**; `RepoDb` casts fall **138 / 21 → 118 / 20**. No fake or adapter change, so the contract register is unchanged. **2026-09-23 UTC review correction (#1042):** CodeRabbit's five findings were all accepted as in-scope claims. (1) The `RowShareLock` observation alone proved only that some row-locking clause was present, so the lock twin gains a second-session `FOR KEY SHARE NOWAIT` probe that must fail with `55P03`; weakening `lockByClerkId` to `FOR NO KEY UPDATE` now fails that twin (**19:56:48Z**) where the table-lock check alone stayed green. (2) The advisory-lock twin now matches the exact key (`classid`/`objid` of `hashtext(user id)`, `objsubid` 1) on the backend PID captured inside the transaction and re-checks that PID after commit; the session-level mutation still fails it (**19:56:50Z**). (3) The found and absent `findById` lookups are separate cases. (4) The three error-translation units that asserted only `INTERNAL_ERROR` now also assert the injected cause or the boundary call count, so a wrapper failure that never reaches the prepared query cannot pass them: making the transaction stub throw the `client.begin` TypeError now fails 5 of the seven cases, including the empty-returning one (**19:58:12Z**; a first attempt whose mutation pattern did not apply is recorded in the log and not claimed). (5) The register wording is corrected to match. Receipts: `.git/claude-debt-resume/userrepo/proofs-review.log`.

**2026-09-23 UTC #1041 release receipt:** Cron seam #1040 merged as `b924a7d2f213ac955d0e1dcbce6523bde65c0736` at **19:09:18Z** (its one Minor finding adjudicated with receipts and resolved; exact-head approval **5295529639** at 19:08:14Z; CI **35905571525** with 52/52 E2E) and is promoted through #1041 (`4fc0746e20f02146343b9d30dbc9748ee9a5977b`, merged **19:23:32Z**) after the executable provenance proof and promotion CI **35907521214** (52/52 E2E, no retries). Main CI **35909005428** passed `test` at **19:34:58Z**. Vercel was Ready at **19:24:56.287Z**, observed STAGED without a production alias from **19:25:51Z** through **19:34:09Z**, then assigned production at **19:35:00.035Z**. Both trees equal `3a8411234cd00d6c531145b8cc387c5fd2d4cb1c`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **19:35:12Z**.

Sanitized receipts: this clone's `.git/claude-debt-resume/userrepo/`. **2026-09-24 UTC status:** full gate passed on `369a24f8`, merged as #1042 (`bd67dc07`) and promoted through #1043; the release receipt sits in the following section. Originally: the remaining `RepoDb` suites (idempotency-key, question, practice-session, bookmark, stripe-customer and smaller files) and steps 5–6 stay Open.

### Idempotency-key repository disposition (2026-09-23 UTC)

`src/adapters/repositories/drizzle-idempotency-key-repository.test.ts` (19 casts, 22 units) retires entirely. Its one non-chain case, the `pruneExpiredBefore` limit guard, is adapter behavior real Postgres can exercise, so it now runs against the real repository with a spy on the prepared-query boundary proving no statement is issued for a non-positive or non-integer limit (four variants). Every other case had, or now has, a real-Postgres twin. The existing 17-case `tests/integration/idempotency-key-repository.integration.test.ts` already covered claims, expired and zombie reclaims, cached results and errors, corrupt error details, stale writers, aborts and pruning; the new `idempotency-key-repository-guards.integration.test.ts` (split out rather than pushing the 803-line twin past the 800-line policy with a suppression) adds six twins: `claim` returns null while a key is active, `find` returns null for an absent key and, separately, for an expired key, a corrupt cached error with an empty code fails loudly, and `storeResult`/`storeError` throw NOT_FOUND for a missing claim, plus the four limit-guard variants. The SQL-shape prune unit retires in favour of the existing batch-limit and skip-locked twins.

Red proofs against `5c608a24`, **2026-09-23 UTC**: letting `claim` reclaim unexpired keys (`lt` → `gt`; a first attempt without the `gt` import failed both suites on a `ReferenceError` and is discarded) failed the new active-key twin and two existing reclaim twins **while the old chain suite stayed green** (**19:48:06Z**); `find` no longer treating expired rows as absent failed the new twin and one old unit (**19:46:39Z**); dropping NOT_FOUND on `storeResult` failed the new twin, two existing twins and one old unit (**19:46:42Z**); removing the prune limit guard failed all four guard variants (against real Postgres with the boundary spy, **21:02:33Z**; the earlier unit-level proof at 19:46:44Z is superseded) and one old unit; treating an empty stored error code as "no error" failed the new empty-code twin and one old unit (**19:49:11Z**); relaxing the codec's non-empty check instead was caught by the existing codec-corpus twin, not the new one, and is recorded, not claimed; with the old unit file restored at its path, the lowered floor failed the ratchet (`19 unknown double cast site(s), above its ratchet floor of 0`) and passes with the new suite. Production was restored byte-identically each time; on that first head 4 unit and 22 integration cases plus typecheck passed, and after the review correction below the replacement lane is 27 executed integration cases across 23 top-level declarations (the existing 17 plus six guard-file declarations, two of them parameterized into the four limit variants and the two store-path variants) with no unit file left.

Casts fall **190 / 42 files → 171 / 41**; `RepoDb` casts **118 / 20 → 99 / 19**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. **2026-09-23 UTC review correction (#1044):** CodeRabbit's three findings were accepted. (1) The retained limit guard was not error translation that infrastructure cannot force, so `.claude/rules/testing.md` places it in integration: the four variants now run against the real repository with a `PostgresJsPreparedQuery.execute` spy and the mocked unit file is deleted, which also removes its stale coverage pointer. (2) The absent-key and expired-key `find` cases are separate tests. The queued question and renewal-consent increments apply the same rule to their guards before review. Receipts: `.git/claude-debt-resume/idem/proofs-review.log`. **2026-09-23 UTC promotion review correction (#1045):** CodeRabbit's count finding on the promotion was accepted: parameterized inputs count as executed cases, so the guard file is ten executed cases over six declarations and the replacement lane is 27 executed / 23 declared (promotion CI `35922233763` ran 17 + 10). The promotion moved reviewed `dev` content unchanged under the reviewed-source rule; this bookmark increment carries the correction.

**2026-09-23 UTC #1043 release receipt:** User repository #1042 merged as `bd67dc07238dc5f0832d898ef490cf78a2f333d6` at **20:19:00Z** (five CodeRabbit findings fixed on its second head `369a24f8`, answered with receipts and resolved; exact-head approval **5296280840** at 20:15:05Z; CI **35913724964** with 52/52 E2E) and is promoted through #1043 (`455503b93e51a16dd678e78f4fc3a3955fe35e56`, merged **20:31:24Z**) after the executable provenance proof and promotion CI **35915226250** (52/52 E2E, no retries). Main CI **35916522586** passed `test` at **20:41:39Z**. Vercel was Ready at **20:32:47.954Z**, observed STAGED without a production alias from **20:33:44Z** through **20:40:56Z**, then assigned production at **20:41:41.206Z**. Both trees equal `725cb2151efb9ff1dd4bfc9964ee33d9caf47563`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **20:41:59Z**.

Sanitized receipts: this clone's `.git/claude-debt-resume/idem/`. **2026-09-24 UTC status:** full gate passed on `646a0f8d`, merged as #1044 (`d80129aa`) and promoted through #1045; the release receipt sits in the following section. Originally: the remaining `RepoDb` suites (question, practice-session, bookmark, stripe-customer and smaller files) and steps 5–6 stay Open.

### Bookmark repository disposition (2026-09-23 UTC)

`src/adapters/repositories/drizzle-bookmark-repository.test.ts` replaced its 9-cast hand-rolled Drizzle chain double (nine units) with one impossible-driver-response case at the real prepared-query boundary: the bookmark upsert returning no rows maps to INTERNAL_ERROR. `tests/integration/bookmark-repository.integration.test.ts` already proved membership, idempotent add/remove and the relation-free summaries; it gains three twins: `add` returns the stored row and a re-add keeps the original `bookmarkedAt` (the conflict clause writes the stored `createdAt` back to itself, a behavior the old unit only asserted as `expect.anything()`), `remove` reports whether a row was deleted, and `listByUserId` orders newest first. The eight chain-shape units (exists, add values and conflict target, remove counts, `orderBy` SQL text, column selections) retire in favour of those twins.

Red proofs against `44ed18cf`, **2026-09-23 UTC**: letting `add` return a fabricated row on an empty returning set failed the retained unit and one old unit (**19:52:42Z**); moving `bookmarkedAt` to `now()` on a re-add failed the new twin **while the old chain suite stayed green** (**19:52:44Z**); `remove` always reporting true failed the new twin and one old unit (**19:52:47Z**); ordering oldest first failed the new twin and one old unit (**19:52:50Z**); with the old unit file restored at its path, the lowered floor failed the ratchet (`9 unknown double cast site(s), above its ratchet floor of 0`) and passes with the new suite. Production was restored byte-identically each time; 1 unit and 5 integration cases plus typecheck pass.

Casts fall **171 / 41 files → 162 / 40**; `RepoDb` casts **99 / 19 → 90 / 18**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/bookmark/`. **2026-09-24 UTC status:** full gate passed on `4539cfe6`, merged as #1046 (`5a3c2157`) and promoted through #1047; the release receipt sits in the following section. Originally: the remaining `RepoDb` suites (question, practice-session, stripe-customer and smaller files) and steps 5–6 stay Open.

**2026-09-23 UTC #1045 release receipt:** Idempotency-key #1044 merged as `d80129aa3a2c2b20837d185c3ef49d98eaf9e55f` at **21:23:10Z** (three CodeRabbit findings fixed on its second head `646a0f8d`, answered with receipts and resolved; exact-head approval **5296886313** at 21:16:37Z; CI **35920922520** with 52/52 E2E) and is promoted through #1045 (`c595a0a712e36c48f85eb042f8af1e1ae4eac9c3`, merged **21:44:31Z**) after the executable provenance proof and promotion CI **35922233763** (52/52 E2E, no retries); CodeRabbit's one Minor count finding on the promotion was verified, answered and resolved, and its correction is carried by this increment. Main CI **35924297014** passed `test` at **21:56:05Z**. Vercel was Ready at **21:46:13.895Z**, observed STAGED without a production alias from **21:46:19Z** through **21:55:35Z**, then assigned production at **21:56:07.081Z**. Both trees equal `34c1ba197c9f63b58ddf4e19dc420d1010bd08ff`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **21:56:38Z**.
### Question repository disposition (2026-09-23 UTC)

`src/adapters/repositories/drizzle-question-repository.test.ts` (16 casts, sixteen units) retires entirely. Its four input guards (`listPublishedCandidateIds` and `countPublishedCandidateIds` rejecting statuses without a `userId` with VALIDATION_ERROR and an unknown status with INTERNAL_ERROR) are adapter behavior real Postgres can exercise, so they run against the real repository in the lookups integration file with a spy on the prepared-query boundary proving no statement is issued; the intentionally invalid status is a `string` variable narrowed with a plain assertion, not an `as unknown as` double cast. The existing `tests/integration/question-repository.integration.test.ts` already proved the published predicates, session-owned lookups, requested-order preservation and every candidate filter; the new `question-repository-lookups.integration.test.ts` (split out to respect the 800-line policy) adds five twins: a published question maps with choices sorted by `sortOrder` (a third choice inserted last but ordered first) and its tags, the slug lookup maps the same way, a missing id returns null, no ids return an empty list, and a stored choice with an invalid label fails loudly with the label and choice id in the message (raw insert; `choices.label` has no check constraint). Twelve chain-shape units (generated-SQL column checks for the status predicate and secrecy columns, fake-row mapping) retire in favour of those twins; with the four guards they account for all sixteen retired units.

Red proofs against `6803f1d6`, **2026-09-23 UTC**: dropping the published predicate from `findPublishedById` failed two existing twins and one old unit (**20:11:01Z**); removing the choice sort failed the new mapping twin and two old units (**20:11:04Z**); accepting invalid labels failed the corrupt-row twin and one old unit (**20:11:07Z**); removing the statuses-without-`userId` guard failed both VALIDATION_ERROR guard twins against real Postgres (**21:14:23Z**; the earlier unit-level proof at 20:11:10Z is superseded) and two old units; with the old unit file restored at its path, the lowered floor failed the ratchet (`16 unknown double cast site(s), above its ratchet floor of 0`) and passes with the new suite. Production was restored byte-identically each time; 24 executed integration cases over 22 declarations (15 existing, 5 lookup twins and the two guard cases parameterized over both candidate-id methods; parameterized inputs count as executed cases) plus typecheck pass, with no unit file left. The guards moved from a retained unit file to integration under `.claude/rules/testing.md` item 4, the same correction CodeRabbit required on #1044.

Casts fall **162 / 40 files → 146 / 39**; `RepoDb` casts **90 / 18 → 74 / 17**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/question/`. **2026-09-24 UTC status:** full gate passed on `29ef74b6`, merged as #1048 (`7a65ef0d`) and promoted through #1047; the release receipt sits in the following section. Originally: the remaining `RepoDb` suites (practice-session, stripe-customer and smaller files) and steps 5–6 stay Open.

**2026-09-23 UTC #1046 merge and promotion note:** Bookmark #1046 merged as `5a3c215752dc4a7eba2fc634cae2afbb14631e66` at **22:16:46Z** (exact-head approval **5297372733** at 22:09:23Z on `4539cfe6` with zero findings; CI **35926371305** with 52/52 E2E, no retries). Promotion #1047 (`5a3c2157` → `c595a0a7`) reported BEHIND under the strict up-to-date rule: #1046 had been rebased onto `dev` rather than `main`, so `dev` did not contain main's #1045 merge commit, and `scripts/verify-promotion.ts` requires the promotion base to be an ancestor of `dev` with every first-parent merge mapped to a reviewed source PR, which also rules out a GitHub back-merge. #1047 was closed unmerged. This branch therefore merges `origin/main` (`c6c754f4`, no tree change against `dev`) ahead of its own commits so that its merge restores the invariant; #1046 and this increment promote together, and the next increment carries that release receipt.

**2026-09-23 UTC review correction (#1048):** CodeRabbit's two findings were accepted. (1) The sixteen retired units were twelve chain-shape units plus the four guards; the wording no longer implies twenty. (2) The retired secrecy units' `mode`/`ended_at` column checks were the only question-repository-specific guard of the visibility predicate's `mode != 'exam'` branch, so the BUG-195 suite in `tests/integration/bug-regression-active-exam-projections.integration.test.ts` gains a twin proving an active tutor session's incorrect attempt stays visible to both candidate operations (integration lane 399 executed). Red proofs against `a6e8c7a7`: bypassing the shared predicate in the question repository with an `endedAt`-only condition at both call sites failed the new twin alone across the integration lane (**22:46:39Z**); removing the `mode` branch from the shared predicate failed it together with the BUG-187 tutor-count and BUG-236 fallback cases (**22:47:15Z**). Production was restored byte-identically each time and typecheck passed. Receipts: `.git/claude-debt-resume/question/proofs-review-2.log`.

### Stripe customer repository disposition (2026-09-23 UTC)

`src/adapters/repositories/drizzle-stripe-customer-repository.test.ts` replaced its 8-cast hand-rolled chain double (eight units) with **two impossible-response cases** at the real prepared-query boundary: an empty upsert result maps to INTERNAL_ERROR (asserting the boundary was called once) and an unexpected driver failure is preserved as the INTERNAL_ERROR cause. The shared `tests/integration/stripe-repositories.integration.test.ts` twin already proved the idempotent upsert and both CONFLICT paths on real Postgres; the new `stripe-customer-repository.integration.test.ts` adds three twins: `findByUserId` returns null without a mapping, the authoritative strategy replaces the stored mapping (the old unit only asserted that the mismatch check was skipped, never that the row changed), and the strict-conflict and foreign-mapping errors carry their distinct messages. Six chain-shape units (lookup rows, the single-statement assertion, the fake-row conflict and authoritative cases, the unique-violation fake) retire in favour of those twins.

Red proofs against `a50669d9`, **2026-09-23 UTC**: removing the strict mismatch check failed the shared twin, the new message twin and one old unit (**20:15:23Z**); making the authoritative strategy a no-op update failed the new replacement twin **while the old chain suite stayed green** (**20:15:27Z**); dropping the unique-violation mapping failed both twins and one old unit (**20:15:30Z**); returning silently on an empty result failed the retained unit and one old unit (**20:15:32Z**); with the old unit file restored at its path, the lowered floor failed the ratchet (`8 unknown double cast site(s), above its ratchet floor of 0`) and passes with the new suite. Production was restored byte-identically each time; 2 unit and 12 integration cases plus typecheck pass.

Casts fall **146 / 39 files → 138 / 38**; `RepoDb` casts **74 / 17 → 66 / 16**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/stripecust/`. **2026-09-24 UTC status:** full gate passed on `e9642f79`, merged as #1049 (`30191c07`) and promoted through #1050; the release receipt sits in the following section. Originally: the remaining `RepoDb` suites (practice-session and smaller files) and steps 5–6 stay Open.

**2026-09-23 UTC #1047 release receipt:** Question #1048 merged as `7a65ef0de593163e7416f4680dd61c55236004d0` at **23:16:07Z** (exact-head approval **5297815779** at 23:08:05Z on its third head `29ef74b6` after three accepted findings; CI **35931784317** with 52/52 E2E, no retries). Its branch carried a no-op merge of `origin/main`, restoring `git merge-base --is-ancestor main dev`, so promotion #1047 was reopened and merged as `e57a4a6cf6eba2ebe2b6b1eefecc10737c1bb0cb` at **23:28:32Z** covering #1046 and #1048, after the executable provenance proof and promotion CI **35932742591** (52/52 E2E, no retries); CodeRabbit's one Minor count finding on the promotion (five vs six mutation proofs) was accepted, answered and resolved, and the first commit of this increment carries the correction. Main CI **35933780940** passed `test` at **23:39:45Z**. Vercel was Ready at **23:30:18.184Z**, observed STAGED without a production alias from **23:30:49Z** through **23:39:02Z**, then assigned production at **23:39:46.815Z**. Both trees equal `31ccb91e30dcf3ddeac7e2d18148ab83bd7e4ab8`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **23:40:05Z**.

### Clerk lifecycle repositories disposition (2026-09-23 UTC)

`src/adapters/repositories/drizzle-clerk-event-repository.test.ts` (6 casts, six units) and `drizzle-deleted-clerk-user-repository.test.ts` (5 casts, five units) retire entirely: every case was a chain-shape assertion over real behavior and no error translation remained, so nothing belongs at the unit level. The new `tests/integration/clerk-lifecycle-repositories.integration.test.ts` proves the same contracts on real Postgres: an event is claimed once and later claims report a duplicate, `peek` returns null for unknown events, `lock` returns the row inside a transaction and holds `FOR UPDATE` (a second session's `FOR KEY SHARE NOWAIT` fails with `55P03`) while rejecting a missing event with NOT_FOUND, `markFailed` then `markProcessed` store the error and the injected clock and clear the error, both marks reject unknown events, a tombstone is recorded idempotently with `exists` flipping, an explicit `deletedAt` is stored while the default comes from the database, and the tombstone advisory lock is held for the transaction under the `hashtextextended(id, 0)` key on the acquiring backend and released after commit.

Red proofs against `f68acdf9`, **2026-09-23 UTC**: removing the claim's conflict clause failed the duplicate-claim twin and two old units (**20:55:13Z**); dropping `FOR UPDATE` failed the lock twin and one old unit (**20:55:16Z**); swallowing a missing event in `markProcessed` failed the NOT_FOUND twin **while the old chain suite stayed green** (**20:55:18Z**); `exists` always false failed the tombstone twin and one old unit (**20:55:20Z**); removing the tombstone conflict clause failed the twin and two old units (**20:55:23Z**); making the tombstone lock session-scoped failed the keyed `pg_locks` twin **while the old chain suite stayed green** (**20:55:25Z**); with the old unit files restored at their paths, the lowered floors failed the ratchet on both (`6` and `5 unknown double cast site(s), above its ratchet floor of 0`) and pass without them. Production was restored byte-identically each time; 8 integration cases plus typecheck pass (ten after the #1051 review corrections below).

Casts fall **138 / 38 files → 127 / 36**; `RepoDb` casts **66 / 16 → 55 / 14**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/clerk/`. **2026-09-24 UTC status:** full gate passed on `dc30e3da`, merged as #1051 (`9a1d8fdc`) and promoted through #1052; the release receipt sits in the following section. Originally: the remaining `RepoDb` suites (practice-session, question-feedback, pending-cleanup, renewal-consent) and steps 5–6 stay Open.

**2026-09-24 UTC #1050 release receipt** (times after midnight are 2026-09-24 UTC)**:** Stripe-customer #1049 merged as `30191c0798c5665f203e9a5ad9a600aa2958f2dc` at **23:59:09Z** (exact-head approval **5298095592** at 23:53:32Z on `e9642f79`, zero findings; CI **35935405418** with 52/52 E2E, no retries) and is promoted through #1050 (`d30aeb8489f60203d1d92b713c32e7b091280cff`, merged **00:10:05Z**) after the executable provenance proof and promotion CI **35936261151** (52/52 E2E, no retries). Main CI **35937158357** passed `test` at **00:21:04Z**. Vercel was Ready at **00:11:09.797Z**, observed STAGED without a production alias from **00:11:20Z** through **00:20:36Z**, then assigned production at **00:21:06.286Z**. Both trees equal `487663ab1bb8f407fd482431bff00a63c325f733`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **00:21:38Z**.

**2026-09-24 UTC review correction (#1051):** CodeRabbit's finding was accepted: the tombstone advisory-lock twin counted `pg_locks` rows regardless of mode, so a `pg_advisory_xact_lock_shared` regression would have passed. Both advisory-lock twins, this one and the user repository's subscription write lock in `user-repository.integration.test.ts`, which had the same weakness, now require a granted `ExclusiveLock`. Red proofs against `8305f5e3`: switching the tombstone lock to `pg_advisory_xact_lock_shared` failed only the tightened tombstone twin (`held` 0 inside the transaction, **00:40:19Z**); switching the subscription write lock in `subscription-write-lock.ts` to its shared form failed the tightened user twin and, independently, all five `stripe-subscription-writer-lock-order` cases with `40P01` deadlocks and a row-lock wait (**00:41:02Z**), so the writer-order suite already guarded that lock behaviorally. A second, outside-diff finding was also accepted: the retired `peeks existing events without locking` unit had been the only guard of `peek`'s lock-free shape, and the Clerk webhook controller peeks before it locks, so the twin file gains `peeks an existing event without waiting on its row lock` (a second session holds the row `FOR UPDATE` while `peek` runs under a one-second `lock_timeout` that is reset afterwards); making `peek` take `FOR UPDATE` failed only that twin with `55P03` (**01:00:43Z**). Production was restored byte-identically each time and typecheck passed; the twin file then had nine cases and the section nine mutation proofs. A third finding from the full review of `1bf27350` was accepted: the lock twin combined the existing-row `FOR UPDATE` contention with the missing-event `NOT_FOUND` path against the one-concept-per-`it()` rule, so the missing-event assertion is now its own case; making `lock` return an empty state for a missing event failed only that case (**01:22:29Z**), production was restored byte-identically and typecheck passed. The twin file has ten cases and the section ten mutation proofs. Receipts: `.git/claude-debt-resume/clerk/proofs-review.log`.

### Small repositories disposition (2026-09-23 UTC)

Three more `RepoDb` suites are dispositioned together. `src/adapters/repositories/drizzle-question-feedback-repository.test.ts` (6 casts, six units) keeps two impossible-response cases at the real prepared-query boundary: an insert that returns no rows maps to INTERNAL_ERROR (asserting one boundary call) and a failing latest-rating read maps to INTERNAL_ERROR with its cause; the existing `question-feedback-repository.integration.test.ts` twin already proved recording, replay, invalid shapes and latest-rating ordering, and gains the null case for an unrated question. `drizzle-renewal-consent-record-repository.test.ts` (5 casts, five units) retires entirely: insert, replay, cross-user rejection and termination were already proved by `renewal-consent-records.integration.test.ts`, and its prune limit guard now runs there against the real repository with a boundary spy under `.claude/rules/testing.md` item 4; the "row disappeared after a source conflict" race is not representable at the mock boundary (Drizzle's mock relational `findFirst` yields an empty, truthy array rather than `undefined`), so that unit is dropped and the race stays uncovered rather than faked. `drizzle-pending-stripe-customer-cleanup-repository.test.ts` (6 casts, six units) retires entirely behind `pending-stripe-customer-cleanup-contract.integration.test.ts`, which already proves scheduling, lookup, idempotent deletion, re-scheduling and the bounded stale listing with exclusions.

Red proofs against `dd34583f`, **2026-09-23 UTC**: a fabricated message on the empty feedback insert failed the retained unit (**21:17:37Z**); dropping the latest-rating read's cause failed the retained unit (**21:17:38Z**); removing the renewal prune guard failed all three guard variants against real Postgres (**21:17:39Z**); removing the pending-cleanup upsert clause failed the re-scheduling twin (**21:17:41Z**); ignoring excluded event ids failed the stale-listing twin (**21:17:44Z**); with the three old unit files restored at their paths, the lowered floors failed the ratchet on all three (`6`, `5` and `6 unknown double cast site(s), above its ratchet floor of 0`) and pass without them. Production was restored byte-identically each time; 2 unit and 27 executed integration cases over 21 declarations plus typecheck pass (question-feedback 9; renewal-consent 10 executed over 8 declarations with the three prune-limit variants; the pending-cleanup contract 8 executed over 4 declarations across its fake and Postgres harnesses; parameterized inputs count as executed cases).

Casts fall **127 / 36 files → 110 / 33**; `RepoDb` casts **55 / 14 → 38 / 11**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/small/`. Full gate, review and promotion remain pending; the remaining `RepoDb` suites are the three practice-session files, and steps 5–6 stay Open.

**2026-09-24 UTC #1052 release receipt** (times after midnight are 2026-09-24 UTC)**:** Clerk-lifecycle #1051 merged as `9a1d8fdc81706b9b103b21b1a9ff4645fb37bd50` at **01:41:58Z** (exact-head approval **5298689434** at 01:34:27Z on `dc30e3da`, two inline findings and one outside-diff claim accepted with receipts across its four heads; CI **35943295971** with 52/52 E2E, no retries) and is promoted through #1052 (`76120ac13c8965c55f39aeae9855d2bed007ee98`, merged **01:56:43Z**) after the executable provenance proof and promotion CI **35944078755** (52/52 E2E, no retries). Main CI **35945118878** passed `test` at **02:08:19Z**. Vercel was Ready at **01:57:44.253Z**, observed STAGED without a production alias from **01:58:12Z** through **02:07:28Z**, then assigned production at **02:08:21.661Z**. Both trees equal `33d447b32385a5683b650faccb710b8afc1b483f`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **02:08:31Z**.

### Practice-session question-state writes disposition (2026-09-24 UTC)

`src/adapters/repositories/drizzle-practice-session-repository-question-state.test.ts` (14 casts, fourteen units), `drizzle-practice-session-repository-question-state-missing-row.test.ts` (1 cast, three units) and `practice-session-question-state-updater-lock.test.ts` (1 cast, one unit) retire entirely. Their scripted-snapshot fakes asserted generated SQL (the `'+ 1'` version increment, predicate columns, the leftJoin shape) and replayed version misses from arrays, so every case was chain shape over real behavior. `drizzle-practice-session-repository-statement-cancellation.test.ts` keeps its two SQLSTATE `57014` cases, which real Postgres cannot raise deterministically inside this write, on `drizzle.mock({ schema })` with the typed `PgTransaction` stub and a `PostgresJsPreparedQuery.execute` spy, cast-free. The new `tests/integration/practice-session-question-state-writes.integration.test.ts` adds twelve twins: NOT_FOUND for a missing session and for a question outside an active session; CONFLICT for an ended session with and without a state row; INTERNAL_ERROR when a session-owned question is missing normalized state, outranking the ended-session CONFLICT; `finalizeDraftAnswer` for an answered and an omitted outcome; and the optimistic retry loop driven by a real concurrent writer through an update-interleaving proxy (a second session bumps `version` between the snapshot read and the guarded update): one retry succeeds with `version + 2`, three misses end in the changed-concurrently CONFLICT with the latest fields untouched, a session deleted after the misses ends in NOT_FOUND, and a session ended after the misses ends in the already-ended CONFLICT. Already-covered behaviors (latest-answer and mark-for-review writes in `bug-regression-historical`, draft saves and stale-draft rejection in the normalization and exam-draft-bounds suites, parent-session lock granularity in `practice-session-state-lock-granularity`) keep their existing twins. The lane runs 428 executed integration cases (416 plus twelve; the new file has no parameterized cases).

Red proofs against `636fc349`, **2026-09-24 UTC**: dropping the row-version predicate from the guarded update failed all four retry twins (**02:26:09Z**); letting a missing normalized state fall through to not-found/ended failed both missing-state twins (**02:26:50Z**); removing both already-ended checks in the updater failed the ended-session twin and the ends-after-retries twin (**02:27:29Z**; the attempt-level check alone is shadowed by the guarded update's `ended_at is null` clause plus the final snapshot check, so a single-site mutation there is not observable and is recorded, not claimed); keeping the draft snapshot in `finalizeDraftAnswer` failed the answered finalization twin (**02:28:09Z**); reporting already-ended after exhausted retries failed the exhaustion twin (**02:28:49Z**); dropping the ended-session check from the snapshot reader failed the ended-plus-foreign-question twin (**02:29:28Z**); turning off statement-cancellation classification on the mark write failed the rollback-certain unit case (**02:29:29Z**); with the four old unit files restored at their paths, the lowered floors failed the ratchet (`14`, `1`, `1` and `1` unknown double cast sites above floors of 0, **02:32:39Z**) and pass with the new suite. Production was restored byte-identically each time; 2 unit and 428 executed integration cases plus typecheck pass.

Casts fall **110 / 33 files → 93 / 29**; `RepoDb` casts **38 / 11 → 22 / 7**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/psqs/`. Full gate, review and promotion remain pending; the remaining `RepoDb` suites (practice-session session-writes, reads, corrupt-list-reads and history-summary) and steps 5–6 stay Open.

**2026-09-24 UTC #1054 release receipt:** Small repositories #1053 merged as `982b54f7b7972cdcccd4a26017dbd10710c14dcf` at **02:30:30Z** (exact-head approval **5298955832** at 02:22:26Z on `f6687a69`, zero findings; CI **35946602831** with 52/52 E2E, no retries) and is promoted through #1054 (`7046281e9f60135ab01bd1b0d754e0ffcb3ccc73`, merged **02:42:08Z**) after the executable provenance proof and promotion CI **35947559511** (52/52 E2E, no retries). Main CI **35948359342** passed `test` at **02:53:31Z**. Vercel was Ready at **02:43:50.168Z**, observed STAGED without a production alias from **02:44:25Z** through **02:52:38Z**, then assigned production at **02:53:33.589Z**. Both trees equal `626601793a4ed3a1309275d3bdb9a138a1e33ecf`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **02:53:41Z**.

### Practice-session writes disposition (2026-09-24 UTC)

`src/adapters/repositories/drizzle-practice-session-repository-session-writes.test.ts` (13 casts, twelve units plus a two-variant fallback case) keeps **two impossible-driver-response cases** on `drizzle.mock({ schema })` with the typed `PgTransaction` stub and a `PostgresJsPreparedQuery.execute` spy, cast-free: an arbitrary driver failure inside the create transaction is wrapped in INTERNAL_ERROR with its cause, and an `INSERT … RETURNING` with no row maps to INTERNAL_ERROR without attempting the state insert. Its remaining cases were chain shape over real behavior (a rendered child-delete WHERE clause checked by substring, `transaction` call arguments, scripted `findFirst` snapshots) and retire. The new `tests/integration/practice-session-writes.integration.test.ts` adds ten twins, eleven after the review correction below: the create input guard rejects invalid `paramsJson` with VALIDATION_ERROR without issuing a statement (boundary spy); `discard` deletes an incomplete session and its states, leaves another user's session untouched, and leaves an ended session untouched; `end` throws NOT_FOUND for a missing session, stamps the injected clock and returns the snapshot loaded before the update (answered state included), uses an explicit `endedAt` without consulting the clock, reports CONFLICT when a concurrent end commits between the snapshot and the guarded update and NOT_FOUND when the session disappears there (a second session interleaved through an update-deferring proxy on the root connection), and matches the fake repository's already-ended error contract. The real duplicate-incomplete-session constraint keeps its existing twin in `session-attempt-repository`; the mapped create result, which the normalization twin never asserted, gained its own twin at review. The lane runs 438 executed integration cases (428 plus ten; no parameterized cases in the new file), 439 after the review correction below.

Red proofs against `29cf850e`, **2026-09-24 UTC**: dropping `ended_at is null` from discard's parent delete failed the ended-session twin and the existing BUG-292 discard twin (**02:39:40Z**); dropping the user predicate there failed the other-user twin (**02:40:19Z**); ignoring an explicit `endedAt` failed the explicit-clock twin (**02:40:58Z**); dropping the fallback's already-ended check failed the concurrent-end twin (**02:41:37Z**); reporting invalid params as INTERNAL_ERROR failed the guard twin (**02:42:16Z**); returning the snapshot without the `endedAt` stamp failed both `end` twins, the parity twin and five existing consumers (**02:42:55Z**; a broad mutation, recorded as proof the stamp is load-bearing rather than as isolation); skipping the missing-row failure in `create` failed the no-row unit case (**02:42:57Z**); dropping the cause from the wrapped insert failure failed the cause unit case (**02:42:58Z**); with the old unit file restored at its path, the lowered floor failed the ratchet (`13` unknown double cast sites above a floor of 0, **02:43:07Z**) and passes with the new suite. Production was restored byte-identically each time; 2 unit and 438 executed integration cases plus typecheck pass.

Casts fall **93 / 29 files → 80 / 28**; `RepoDb` casts **22 / 7 → 9 / 6**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. **2026-09-24 UTC review correction (#1057):** CodeRabbit's two findings were accepted. (1) The `PgTransaction` stub and `PgDatabase.prototype.transaction` spy had been copied into a third suite, so `tests/shared/drizzle-mock-transaction.ts` now owns that cast-free wiring behind `installMockTransactionBoundary()` and the user-repository, statement-cancellation and session-writes suites call it (11/11 unchanged). (2) An outside-diff claim that no retained test asserted the mapped result of a valid `create` was verified: the normalization twin checks the stored `params_json` and state rows only, so the twin file gains `returns the mapped session and stores the initial question states` (full returned `PracticeSession` and the stored initial-state defaults); dropping the tag and difficulty filters from the mapping failed only that twin (**04:19:33Z**), and starting states marked for review failed only that twin (**04:20:12Z**). Production was restored byte-identically each time; the twin file has eleven cases and the section ten mutation proofs. Sanitized receipts: this clone's `.git/claude-debt-resume/psw/`. The local full gate passed on `0508b3e0` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. The remaining `RepoDb` suites (practice-session reads, corrupt-list-reads and history-summary) and steps 5–6 stay Open.

**2026-09-24 UTC #1056 release receipt:** Question-state writes #1055 merged as `0109afbbf4302dcd00de231d1220315228bd4934` at **03:21:11Z** (exact-head approval **5299223912** at 03:12:29Z on `f8638d9b`, zero findings; CI **35950172554** with 52/52 E2E, no retries) and is promoted through #1056 (`5f24782eedf0a8c8c40e845c7dc3110d3c2aff89`, merged **03:33:30Z**) after the executable provenance proof and promotion CI **35951097789** (52/52 E2E, no retries). Main CI **35951948414** passed `test` at **03:43:08Z**. Vercel was Ready at **03:34:45.726Z**, observed STAGED without a production alias from **03:35:47Z** through **03:43:01Z**, then assigned production at **03:43:10.465Z**. Both trees equal `da01fc2b2e8af47426ae64f8d83431586d6839df`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **03:44:04Z**.

### Practice-session reads disposition (2026-09-24 UTC)

`src/adapters/repositories/drizzle-practice-session-repository-reads.test.ts` (4 casts, ten units) and `drizzle-practice-session-repository-history-summary.test.ts` (1 cast, five units plus a two-variant case) retire entirely; their fakes replayed `findFirst`/`findMany` rows, pinned `transaction` isolation arguments, select-projection keys and `groupBy`, and asserted the domain mapping over fabricated rows. `drizzle-practice-session-repository-corrupt-list-reads.test.ts` (1 cast, four units, two of them identical) keeps **one driver-failure case** on `drizzle.mock({ schema })`, cast-free: the relational session read is answered at drizzle's `RelationalQueryBuilder.findFirst` boundary and the following state-rows read fails at the `PostgresJsPreparedQuery.execute` boundary, so the failure lands inside the corrupt-row classification and must propagate unchanged without a warn log. The shared `drizzle-practice-session-repository-test-helpers.ts` and `repository-test-helpers.ts` (with its own unit test) had no remaining importers and are deleted. The history-summary case for a completed summary missing `ended_at` is not twinned: the summaries query already requires `ended_at is not null`, so that defensive branch is unreachable on real Postgres and is recorded as such. The new `tests/integration/practice-session-reads.integration.test.ts` adds thirteen twins, fifteen after the review corrections below (fifteen and then seventeen executed cases; parameterized inputs count as executed): `findByIdAndUserId` returns null for an unknown id and for another user and maps a session with its tag and difficulty filters and ordered question states; `findLatestIncompleteByUserId` returns null when every session has ended and the latest incomplete session after an earlier one ended; completed pages come newest-ended first with the total, return no rows but the total for a limit of 0, -1 or 1.5, return an empty page past the matching rows, and filter by mode; `findByIdAndUserId` reports INTERNAL_ERROR for surplus normalized rows and for invalid persisted `paramsJson`; and history summaries skip and log a session whose positions do not start at zero, one with an extra normalized row, and one missing a row, preserving the total. Corrupt-row skip/log in list reads and the healthy summary row keep their existing twins in `practice-session-schema-hardening` and `practice-session-history-summary`. On the pre-replay base the lane ran 453 executed integration cases (438 plus fifteen); on the replayed base, which already carries the session-writes increment's valid-create twin (439), it runs 456 after the review corrections (439 plus seventeen).

Red proofs against `ec666600`, **2026-09-24 UTC**: no longer reporting surplus normalized rows failed the surplus twin (**02:48:58Z**); ignoring the offset failed the paging and past-the-end twins (**02:50:18Z**); ordering oldest-ended first failed the paging twin (**02:50:58Z**); ignoring the mode filter failed the mode twin (**02:51:38Z**); dropping the summary position check failed the positions twin (**02:52:17Z**); dropping the summary surplus-row check failed the extra-row twin (**02:52:57Z**); reporting invalid persisted params as VALIDATION_ERROR failed the invalid-params twin and three existing schema-hardening twins (**02:53:36Z**); removing the non-positive-limit early return changed nothing because the `safeLimit` guard already yields 0, so it is recorded, not claimed (**02:49:38Z**), while passing the raw limit through failed the -1 and 1.5 variants and left the 0 variant green because Postgres itself honours `LIMIT 0` (**02:57:24Z**); classifying every error as a corrupt row failed the relational-boundary unit case, which had first been written to fail before the classifying `try` and was corrected (**02:58:04Z**, against `b8ef4454`); with the three old unit files restored at their paths, the lowered floors failed the ratchet (`4`, `1` and `1` unknown double cast sites above floors of 0, **02:53:47Z**) and pass with the new suite. Production was restored byte-identically each time; 1 unit and 453 executed integration cases plus typecheck pass.

Casts fall **80 / 28 files → 74 / 25**; `RepoDb` casts **9 / 6 → 3 / 3**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. **2026-09-24 UTC review correction (#1059):** CodeRabbit's finding was accepted: the retained classification case answered the relational session read through `RelationalQueryBuilder.findFirst` with an `as never` escape, the same class of type evasion the ratchet forbids as `as unknown as`. It now spies `PgRelationalQuery.prototype.execute`, which awaiting the relational query routes through, and resolves the row there without any cast; the classification mutation still fails only that case (**06:00:39Z**, against `57e827c4`'s tree). The suite also uses the shared `tests/shared/drizzle-mock-transaction.ts` boundary landed by #1057 (re-proven at **05:33:35Z**). A census at that head finds 32 other `as never` sites in test files that `lint:doubles` does not count; that is deferred to steps 5–6 with the revive trigger "extend the ratchet to `as never` and drive the count down". Two outside-diff claims on `a4eaf21e` were then adjudicated: the first, that nothing proves the completed-page count and page come from one repeatable-read snapshot (the retired unit had only pinned the isolation option), was accepted, and `reads the count and the page from one repeatable-read snapshot` interleaves a second connection between the count and the page statements to commit a fourth completed session, asserting the page still reports three of three; dropping the `repeatable read` option from that read failed only that twin, returning four rows against a total of three (**06:20:43Z**). The second, that no Postgres test corrupts rows before the two list reads, was rejected with the receipt: `practice-session-schema-hardening.integration.test.ts` already proves both `findLatestIncompleteByUserId` and `findCompletedByUserId` skip and log a corrupt row and preserve the total. The section's mutation count is ten. A third outside-diff claim on `6a9c510f`, that no retained test persists a stale embedded `questionStates` blob (the retired reads unit had asserted the mapper ignores one), was accepted: `ignores a stale embedded questionStates blob and maps the normalized rows` writes that key into a stored `params_json` with an orphan entry and asserts both readers map only the normalized rows without a warn log; making the persisted params schema strict about unknown keys failed only that twin (**06:39:15Z**), which is exactly the compatibility regression the schema's contract comment forbids. An inline finding that the post-replay lane total should be 454 was answered with the receipt above rather than adopted: the replayed base is 439, so the measured total is 456 (455 before this twin). The section's mutation count is eleven. Sanitized receipts: this clone's `.git/claude-debt-resume/psr/`. The local full gate passed on `03ba70d7` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. the remaining `RepoDb` suites (subscription, tag and trial-payment-method-setup-operation repositories) and steps 5–6 stay Open.

**2026-09-24 UTC #1058 release receipt:** Session-writes #1057 merged as `3c24d1a8638cb8622ffa82d46d62460fb23240c1` at **05:18:18Z** (exact-head approval **5300099589** at 05:15:12Z on `f3c3ba1e`, four accepted findings and one codecov correction across five heads; CI **35958407064** with 52/52 E2E, no retries) and is promoted through #1058 (`9df80834af9a253290968b53385678eb94868ab6`, merged **05:32:06Z**) after the executable provenance proof and promotion CI **35959377050** (52/52 E2E, no retries). Main CI **35960376176** passed `test` at **05:43:34Z**. Vercel was Ready at **05:33:50.983Z**, observed STAGED without a production alias from **05:34:24Z** through **05:42:36Z**, then assigned production at **05:43:36.678Z**. Both trees equal `4ca207c3f4adb121475e4a81e79a1e1da8f5fd87`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **05:43:40Z**.

### Last repository chain doubles disposition (2026-09-24 UTC)

`src/adapters/repositories/drizzle-subscription-repository.test.ts` (1 cast, thirteen units) keeps **three error-translation cases** on `drizzle.mock({ schema })` through the shared `tests/shared/drizzle-mock-transaction.ts` boundary (the typed `PgTransaction` stub and `PostgresJsPreparedQuery.execute` spy that #1057 extracted; its local copy retires here), cast-free (the advisory lock, the `FOR UPDATE` read and the insert reach the boundary in that order; the insert carries the failure): the nested-cause `user_missing` translation, a foreign-key violation on a constraint the table does not have kept as INTERNAL_ERROR, and an arbitrary driver failure wrapped with its cause; its SQL-text and call-count assertions retire. `drizzle-tag-repository.test.ts` (1 cast, three units) and `drizzle-trial-payment-method-setup-operation-repository.test.ts` (1 cast, five units) retire entirely. The new `tests/integration/subscription-repository.integration.test.ts` adds seven twins (null lookups by user and by external id, the annual price mapping, INTERNAL_ERROR for an unknown stored price id from both lookups, the injected clock persisted as `updated_at` on insert and on conflict-update with `version` 1 → 2, and the real users foreign-key violation surfacing as `SubscriptionUserMissingError` with the driver error as its cause); `tag-repository.integration.test.ts` gains two (each tag is exactly `id`, `slug`, `name`, `kind`; tags linked only to unpublished questions are excluded); `trial-payment-method-setup-operations.integration.test.ts` gains five (the immutable pending snapshot round-trip, an identical replay resolving, a changed snapshot rejected with CONFLICT, `markTerminal` persisting its reason and rejecting a stale claim, and `pruneExpired` honouring its limit oldest-first). The remaining unit behaviors were already twinned in `stripe-repositories`, the observation-version contract, the writer lock-order suite and the post-deletion webhook regression. No repository chain double remains; fourteen executed integration cases are added, none parameterized. **Review correction (2026-09-24 UTC):** the limited-prune twin now anchors its two rows in a far-past window (`expired_at` at 1970-01-01T00:00:00Z and +1s, cutoff +2s) that no service or other test writes, and clears only that window before arranging them. The prune is table-wide by design: a first correction drained every expired row before an August 2026 cutoff, which fixed the nondeterminism (a stray older expired row owned by another user made the unchanged case fail at 07:45:31Z) but would delete foreign rows when `INTEGRATION_USE_EXISTING_DATABASE=true` passes a caller's database through; the far-past window makes foreign rows ineligible instead of deleting them. Proofs on this clone's Postgres: a realistic foreign expired row survives the case untouched (**08:02:08Z**); an in-window leftover older than the case's rows makes it fail without the drain (**08:02:10Z**) and pass with it (**08:02:37Z**); the file passes 7/7. Step 4's checklist item also gains a dated sub-status naming its remaining residue.

Red proofs against `491bde95`, **2026-09-24 UTC**: mapping an unknown price id to the monthly plan failed both unknown-price twins and the existing `stripe-repositories` mapping twin (**03:19:00Z**); stamping a fresh clock failed the clock twin (**03:19:39Z**); no longer recognising the users foreign key by constraint name failed the user-missing twin, the existing lock-order and post-deletion-webhook twins and the unit cause-chain case (**03:20:21Z**); dropping the published-question filter failed the unpublished-only tag twin (**03:21:01Z**); leaking an extra projection field failed the exact-projection twin (**03:21:41Z**); accepting a changed snapshot failed the CONFLICT twin (**03:22:21Z**); dropping the terminal reason failed the terminal twin (**03:23:01Z**); ignoring the prune limit failed the prune twin (**03:23:41Z**); treating every foreign-key violation as the users constraint failed the other-constraint unit case (**03:23:43Z**); with the three old unit files restored at their paths, the lowered floors failed the ratchet (`1`, `1` and `1` unknown double cast sites above floors of 0, **03:23:52Z**) and pass with the new suite. Production was restored byte-identically each time; 3 unit and 442 executed integration cases on the branch's base plus typecheck pass.

Casts fall **74 / 25 files → 71 / 22**; `RepoDb` casts **3 / 3 → 0 / 0**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the contract register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/last/`. The local full gate passed on `19387f5d` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 4's repository scope closes with this increment, and steps 5–6 stay Open.

**2026-09-24 UTC #1060 release receipt:** Reads #1059 merged as `db9316c2a3578ae72e0f3e497e20f083273e20fb` at **07:02:07Z** (exact-head approval **5300823491** at 06:51:43Z on `73150b2d`, one accepted inline finding, two accepted outside-diff claims and two claims answered with receipts across four heads; CI **35966347314** with 52/52 E2E, no retries) and is promoted through #1060 (`b4f01eb176e040d622f26893fe768366ee1b62eb`, merged **07:13:26Z**) after the executable provenance proof and promotion CI **35967492943** (52/52 E2E, no retries). Main CI **35968467068** passed `test` at **07:24:58Z**. Vercel was Ready at **07:14:56.990Z**, observed STAGED without a production alias from **07:16:04Z** through **07:24:20Z**, then assigned production at **07:25:00.227Z**. Both trees equal `1bd407423204dab35430f5ea27f84544420d6cff`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **07:25:23Z**.

### Prune-twin drain scoping follow-through (2026-09-24 UTC)

After #1061 merged, the reviewer replied on its resolved prune thread that the far-past window still left the setup delete window-wide: nothing enforces that only the limited-prune twin writes 1970-era `expired_at` values, so against an existing database an unrelated row in that window would be deleted rather than left alone. Accepted as a residual. The delete is now scoped by the window and by the session-id prefix that only this case creates (`cs_prune_%`), so leftovers of an aborted run are still cleared while any unrelated row in the window survives and would fail the case deterministically instead of disappearing. Proofs on this clone's Postgres: an unrelated in-window row owned by another user survives the scoped delete and the case fails on it as designed (**08:41:21Z**); a same-prefix in-window leftover is cleared and the case passes (**08:41:23Z**); the file passes 8/8. **Review correction (2026-09-24 UTC):** the prefix predicate is `LIKE`, where `_` matches any single character, so `cs_prune_%` also matched `csXpruneY…`; both underscores are now escaped and the case keeps a near-match expired row inside the window under assertion, between its two rows, so it must survive the cleanup while the limit-1 prune still takes the oldest row. With the unescaped prefix the near-match row was deleted by the cleanup (**09:11:37Z**); with the escaped prefix it survives and the case passes (**09:11:39Z**); reverting the escape makes the kept assertion fail (**09:11:40Z**). The same PR also splits the terminal-claim twin into one concept per case, as the promotion review asked: one case proves a stale claim is rejected with CONFLICT and leaves the row `processing` under the holding claim, the other proves the holding claim persists the terminal reason and time; a shared claimed-operation fixture replaces the combined case. Test-only; no floor, fake, adapter or scanner change. The local full gate passed on `4f05a936` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

**2026-09-24 UTC #1062 release receipt:** Last-doubles #1061 merged as `bd42c9a94faa93629901ee1d0084066beb1bfb67` at **08:24:05Z** (exact-head approval **5301641216** at 08:14:49Z on `9baea414`, three accepted findings across three heads; CI **35973702732** with 52/52 E2E, no retries) and is promoted through #1062 (`3509c104a21edc58cd7187c5569f473da37a0d59`, merged **08:39:49Z**) after the executable provenance proof and promotion CI **35975068163** (52/52 E2E, no retries). Main CI **35976563952** passed `test` at **08:50:58Z**. Vercel was Ready at **08:41:13.255Z**, observed STAGED without a production alias from **08:42:05Z** through **08:50:18Z**, then assigned production at **08:51:00.521Z**. Both trees equal `06c1fa7d5525d3dffa3e803d94e75e2fc712d991`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **08:51:21Z**.

### Process finding and call-chain adjudication disposition (2026-09-24 UTC)

Step 6 is documentation only. The first half was already satisfied: `docs/debt/index.md` § Resolution Evidence Conventions (line 1037 at this head; the line moves as the index grows) requires that a resolution deleting a pattern lands with the executable guard that keeps it deleted; the re-audit table's `:901` citation pointed at an archived-row table entry and is corrected. The second half had no live answer: DEBT-035 (archived, ruled the `RepoDb` call-chain object a fake) and DEBT-444 (archived, named it the anti-pattern) were both invisible to agents, and the retained `drizzle.mock` error-translation boundary that step 4 standardized on was sanctioned only in `tests/shared/README.md` and code comments, not in `.claude/rules/testing.md`, `AGENTS.md` or ADR-003. The adjudication now sits in the canonical rule: DEBT-444 governs, DEBT-035 is superseded, a call-chain object is a stub, behavior belongs on real Postgres, and the shared `drizzle.mock` boundary is the second accepted shape-only form beside the client-owned narrow seam. ADR-003's unit-adapter amendment gains the matching dated sentence and the archived DEBT-035 record carries a supersession pointer so a reader who lands there is redirected. `AGENTS.md` and `.coderabbit.yaml` already defer to the rule file and are unchanged. F5's consumer count and fake line reference are corrected in place with dated notes; step 5's order stands.

No code, fake, floor or scanner change; the ratchet, contract register and floors are untouched. The local full gate passed on `7a509099` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 6 is closed; step 4 stays Open for its named residue (the three Search-behavior cases and the unclassified `proxy.test.ts` and `db/schema.test.ts` casts) and step 5 for its remaining seven suites.

**2026-09-24 UTC #1064 release receipt:** Prune-twin scoping #1063 merged as `48b13156a5776728b6738135a2d0e322a1d4d432` at **09:35:35Z** (exact-head approval **5302552361** at 09:33:28Z on `f0eb67ea`, two accepted findings across two heads; CI **35980553850** with 52/52 E2E, no retries) and is promoted through #1064 (`a9fecd100ac35c30b0c6db8a7fbb076189145bd8`, merged **09:49:28Z**) after the executable provenance proof and promotion CI **35982229623** (52/52 E2E, no retries). Main CI **35983581042** passed `test` at **09:59:19Z**. Vercel was Ready at **09:50:59.804Z**, observed STAGED without a production alias from **09:51:44Z** through **09:58:56Z**, then assigned production at **09:59:21.756Z**. Both trees equal `bac620474d91dcfd241b4fdeb1f0afa7089e21f9`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **09:59:59Z**.

### Live-retrieve fake migration disposition (2026-09-24 UTC)

Step 5's zero-cost proof. `src/adapters/gateways/stripe/stripe-checkout-sessions-live-retrieve.test.ts` (1 cast, two units) re-implemented the whole `StripeClient` surface as a literal cast with `as unknown as` so that two cases could bend live retrieval. It now constructs `FakeStripeCheckoutClient` and bends only retrieval through the fake's `setRetrieveOverride` seam: one case throws from retrieval, the other returns the retrieved session under a different id. Creation, the idempotency key and the replay scan run through the contracted fake instead of a re-implementation, and the cases assert the created `cs_fake_1` url, the single retrieve call the fake records and the adapter's warn message. No fake extension, adapter or contract change, so the register is unchanged; the fake's unit-consumer count noted in F5 rises to six.

Mutation proofs against the production adapter, this clone, **2026-09-24 UTC**: removing the fallback after a failed live retrieval (re-throwing instead) failed the thrown-retrieval case (**09:50:42Z**); accepting a retrieved session whose id differs (dropping the id comparison) failed the mismatched-id case (**09:50:43Z**); with the old inline-stub file restored at its path, the lowered floor failed the ratchet (`1` unknown double cast site above a floor of 0, **09:50:52Z**) and passes with the migrated file. Production was restored byte-identically each time.

Casts fall **71 / 22 files → 70 / 21**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. Sanitized receipts: this clone's `.git/claude-debt-resume/live-retrieve/`. The local full gate passed on `23217cd1` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 5 continues with `-recovery`, which needs the first fake extension and its contract scenario.

**2026-09-24 UTC #1066 release receipt:** Step-6 #1065 merged as `3ab1386a22d0e5a2201567b3589fb212e416bfe5` at **11:49:34Z** (exact-head approval **5303961941** at 11:47:54Z on `2d913826`, two accepted inline findings and one accepted outside-diff claim across three heads; CI **35989758750** with 52/52 E2E, no retries) and is promoted through #1066 (`b81240651a3bbb69b86268955d85b38339af8b2f`, merged **12:03:09Z**) after the executable provenance proof and promotion CI **35995310686** (52/52 E2E, no retries). Main CI **35996649812** passed `test` at **12:14:25Z**. Vercel was Ready at **12:04:46.502Z**, observed STAGED without a production alias from **12:05:28Z** through **12:13:42Z**, then assigned production at **12:14:27.163Z**. Both trees equal `485903b3bcf56a04dbe799b880a388cbbf61b72b`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **12:14:45Z**.

### Recovery fake migration disposition (2026-09-24 UTC)

`src/adapters/gateways/stripe/stripe-checkout-sessions-recovery.test.ts` (1 cast, six units) scripted create and retrieve responses by call index and listed nothing, so the production tail scan never ran and every case was steered straight into the bounded replay traversal. It now constructs `FakeStripeCheckoutClient` under the frozen clock and seeds real state: one production run creates `cs_fake_1`, `markComplete` retires it, and completed recovery links are created through the fake's own idempotent `create` with the production recovery keys, all at the same second. The tail scan therefore runs, finds the tied newest second ambiguous, logs its fallback and walks the chain exactly as before; the six-replay case asserts that fallback warning explicitly beside the four warn-level and two error-level retries. The stale-primary-key case seeds the primary key with the same user, plan and trial but another customer, so the fake's contracted parameter-mismatch error drives the request-hash recovery. The one behavior the fake could not supply, a non-idempotency create error, comes from a new test seam, `setCreateFault`, which throws a caller-supplied error after the call is recorded and before any replay or creation, mirroring `setRetrieveOverride`; it models no Stripe fault shape, is documented in the contract register as a seam rather than provider behavior, and has its own case in the fake's unit suite. Session ids and urls in the assertions become the fake's `cs_fake_N` scheme; the recorded `createCalls`, `retrieveCalls` and idempotency keys replace the stub's spies.

Mutation proofs against the production adapter, this clone, **2026-09-24 UTC**: dropping the parameter-mismatch retry failed the stale-primary-key case (**12:28:21Z**); retrying every create error failed the non-idempotency case (**12:28:38Z**); shifting the traversal limit by one failed both limit cases (**12:27:41Z**); removing the error-level threshold failed the six-replay case (**12:27:42Z**); with the old stub file restored at its path, the lowered floor failed the ratchet (`1` unknown double cast site above a floor of 0, **12:27:51Z**) and passes with the migrated file. Production was restored byte-identically each time.

Casts fall **70 / 21 files → 69 / 20**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. The fake gains a test seam, not provider behavior, so no contract scenario is added; the register row carries the dated clarification. Sanitized receipts: this clone's `.git/claude-debt-resume/recovery/`. The local full gate passed on `a4894f48` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 5 continues with `-trials`.

**2026-09-24 UTC #1068 release receipt:** Live-retrieve #1067 merged as `c9df219850d5df720fadb9ab0c91a2c2203ebc69` at **12:50:06Z** (exact-head approval **5304548357** at 12:42:05Z on `3340b93f`, one accepted finding across two heads; CI **36000417802** with 52/52 E2E, no retries) and is promoted through #1068 (`ad7595125d61c1ff505f23b3b915f366728f78a5`, merged **13:04:22Z**) after the executable provenance proof and promotion CI **36001612955** (52/52 E2E, no retries). Main CI **36003184419** passed `test` at **13:15:40Z**. Vercel was Ready at **13:06:29.624Z**, observed STAGED without a production alias from **13:06:50Z** through **13:15:04Z**, then assigned production at **13:15:42.402Z**. Both trees equal `4e1cc7e1b72e4738fac124496b731218c4ee4c1f`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **13:16:08Z**.

### Trials fake migration disposition (2026-09-24 UTC)

`src/adapters/gateways/stripe/stripe-checkout-sessions-trials.test.ts` (2 casts: the `StripeClient` literal and a spy-call cast; nine checkout and five setup-session units, including a three-variant `it.each`) answered the preflight listing, live retrieval and setup creates from option-driven canned shapes, and its retrieval always returned `cs_existing` whatever id was asked for. It now constructs `FakeStripeCheckoutClient` and seeds real state: an existing open Session for the customer is created through the fake's own `create` with the chosen price, metadata and payment-method collection (an empty `line_items` for the price that cannot be determined), the reuse case seeds by running the trial checkout once, the setup mismatch case rotates the terms hash so the deterministic key meets different parameters and the fake raises the contracted mismatch error, the second-mismatch case arms the persistent create fault, and the replay cases use `markComplete` and the retrieve override. Two recording seams are added to the fake so the cases keep their call-shape assertions without spies: `retrieveRequests` records each retrieval's params (the `expand: ['line_items']` inspection) and `expireCalls` records each expiry's id and options (the `expire_checkout_session:<id>` key); both are documented in the register as recording seams, not provider behavior, and the fake's unit suite gains a case for them. Session ids, urls and the ids inside recovery keys become the fake's `cs_fake_N` scheme; call counts are sliced after the seed.

Mutation proofs against the production adapter, this clone, **2026-09-24 UTC**: dropping the no-card `trial_settings` failed the exact-params case (**12:44:39Z**); dropping the `:trial:N` key suffix failed the exact-params case and all three replacement-key variants (**12:44:40Z**); removing the consent-evidence comparison failed the pre-consent replacement case (**12:44:42Z**); shifting the setup traversal limit by one failed the bounded-chain case (**12:44:43Z**); with the old stub file restored at its path, the lowered floor failed the ratchet (`2` unknown double cast sites above a floor of 0, **12:44:51Z**) and passes with the migrated file. Two mutations are recorded, not claimed: forcing the same-price variant branch to keep the existing Session (**12:44:41Z**) is shadowed because the post-create reconciliation expires the superseded open Session with the same key, and forcing the variant comparison true (**12:45:38Z**) is unobservable because the consent-evidence comparison already includes the variant key, so that separate comparison cannot change the outcome for any existing Session; the adapter is unchanged here and the observation is left for step 5's remaining files. Production was restored byte-identically each time.

Casts fall **69 / 20 files → 67 / 19**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. The fake gains recording seams, not provider behavior, so no contract scenario is added; the register row carries the dated clarification. Sanitized receipts: this clone's `.git/claude-debt-resume/trials/`. The local full gate passed on `5a2e617e` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 5 continues with `-trial-recovery`.

**2026-09-24 UTC #1070 release receipt:** Recovery #1069 merged as `58a3bc1475f71106861e3a1a47e6516be4c12518` at **13:38:19Z** (exact-head approval **5305158708** at 13:34:48Z on `a1df67dc`, zero findings; CI **36005482104** with 52/52 E2E, no retries) and is promoted through #1070 (`6b79c833700cd598b8f34d681e2d19fd5813c20f`, merged **13:52:02Z**) after the executable provenance proof and promotion CI **36007046051** (52/52 E2E, no retries). Main CI **36008655589** passed `test` at **14:03:25Z**. Vercel was Ready at **13:53:36.781Z**, observed STAGED without a production alias from **13:54:19Z** through **14:02:34Z**, then assigned production at **14:03:27.643Z**. Both trees equal `4f27c1becee709e5590fbf7ecef7ea622c193d6c`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **14:03:37Z**.

### Trial-recovery fake migration disposition (2026-09-24 UTC)

`src/adapters/gateways/stripe/stripe-checkout-sessions-trial-recovery.test.ts` (1 cast, three units) listed a fixed `cs_open` Session on every call and answered retrieval from option-driven shapes. It now constructs `FakeStripeCheckoutClient` under the frozen clock, seeds the existing open trial Session through the fake's own `create` with the price each case needs, and bends only its inspection through the retrieve override: an empty `line_items` for the price that cannot be inspected, and a one-shot throw for the inspection that fails, after which the post-create retrieval of the replacement succeeds. The expiry and the replacement create are read from the fake's `expireCalls` and `createCalls`; session ids inside the trial recovery keys become the fake's `cs_fake_N` scheme. No fake extension.

Mutation proofs against the production adapter, this clone, **2026-09-24 UTC**: withholding the trial recovery key on a mismatched price failed the mismatched-price case (**13:13:34Z**); withholding it when the price cannot be inspected failed the uninspectable-price case (**13:13:35Z**); with the old stub file restored at its path, the lowered floor failed the ratchet (`1` unknown double cast site above a floor of 0, **13:13:43Z**) and passes with the migrated file. One mutation is recorded, not claimed: withholding the key inside the inspection-failure branch (**13:13:33Z**) is unobservable because a failed inspection leaves the price unknown and the missing-price branch sets the same key afterwards; the adapter is unchanged here. Production was restored byte-identically each time.

Casts fall **67 / 19 files → 66 / 18**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. No fake or adapter change, so the register is unchanged. Sanitized receipts: this clone's `.git/claude-debt-resume/trial-recovery/`. The local full gate passed on `1ec3ece8` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 5 continues with `-reconciliation`, which needs the expire-fault seam and a race between the create and the reconcile listing.

**2026-09-24 UTC #1072 release receipt:** Trials #1071 merged as `4dc88fd598e9e4c534f08342cf0fae038ffd7183` at **14:25:28Z** (exact-head approval **5305696191** at 14:19:26Z on `2bb08fb5`, zero findings; CI **36011054153** with 52/52 E2E, no retries) and is promoted through #1072 (`92e0fd4afc9f8b3415d5baf79b7f796a1661d964`, merged **14:36:50Z**) after the executable provenance proof and promotion CI **36012724152** (52/52 E2E, no retries). Main CI **36014043746** passed `test` at **14:48:51Z**. Vercel was Ready at **14:38:10.573Z**, observed STAGED without a production alias from **14:38:13Z** through **14:48:31Z**, then assigned production at **14:48:53.891Z**. Both trees equal `fa03acd3f5f0bb104f3cfacd7da1c8afb32d3591`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **14:49:36Z**.

### Reconciliation fake migration disposition (2026-09-24 UTC)

`src/adapters/gateways/stripe/stripe-checkout-sessions-reconciliation.test.ts` (1 cast, four units) answered the adapter's first listing with nothing and every later listing with caller-chosen Sessions, so the race it describes (another open Session appearing between the create and the reconcile listing) was scripted rather than staged. It now constructs `FakeStripeCheckoutClient` on a mutable frozen clock and stages the race where it can really happen: the only Stripe call between the adapter's create and its reconcile listing is the post-create retrieval, so a one-shot retrieve override creates the racing Session there through the fake's own `create`, from the adapter's own create params, under the clock each case needs. At the same second the fake's tie-break gives the racer the canonical role and the created Session is superseded; created two days later the racer outlives a reconciliation clock that the created Session (24h lifetime) does not; created a day earlier the racer's `expires_at` equals the frozen now and is ignored as inactive. The two expiry-failure cases inject their errors through a new test seam, `setExpireFault`, which throws a caller-supplied error after the expiry call is recorded and before the Session is marked expired, mirroring `setCreateFault`; it models no Stripe fault shape, is documented in the register as a seam, and has its own case in the fake's unit suite. Expiries are read from the fake's `expireCalls`; session ids and urls become the fake's `cs_fake_N` scheme. **Review correction (2026-09-24 UTC):** the already-terminal case's fault now marks the superseded Session expired before it throws and the case asserts that live terminal state, because Stripe reports an already-terminal error only for a Session that is terminal; the transport-failure case keeps its Session open, as its error implies nothing about state. **Promotion-review follow-through (2026-09-24 UTC, #1080):** the list-hook unit case now asserts, from inside the hook, that the listing was recorded before the hook ran, and the F5 plan and step 5 status no longer list the landed list hook as pending. **Promotion-review follow-through (2026-09-24 UTC, #1078):** the contract's real half now tracks the canceled Subscription until its cancel succeeds, so cleanup can still cancel it if that call fails, and the F5 plan and step 5 status no longer list the landed Subscription seeder as pending. **Promotion-review correction (2026-09-24 UTC, #1076):** the created-expires-first case staged its racer at hour 48 while the reconciliation clock read hour 25, a creation time in the future; it is now staged at hour 12, still open at hour 25 while the created Session has expired, and keeping an inactive created Session as a candidate still fails the case.

Mutation proofs against the production adapter, this clone, **2026-09-24 UTC**: treating no expiry error as already terminal failed the idempotent-success case (**13:25:18Z**); treating every expiry error as already terminal failed the reconcile-failure case (**13:25:19Z**); keeping inactive listed Sessions as candidates failed the inactive-listed case (**13:25:20Z**); keeping an inactive created Session as a candidate failed the created-expires-first case (**13:25:21Z**); with the old stub file restored at its path, the lowered floor failed the ratchet (`1` unknown double cast site above a floor of 0, **13:25:30Z**) and passes with the migrated file. Production was restored byte-identically each time.

Casts fall **66 / 18 files → 65 / 17**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. The fake gains a test seam, not provider behavior, so no contract scenario is added; the register row carries the dated clarification. Sanitized receipts: this clone's `.git/claude-debt-resume/reconciliation/`. The local full gate passed on `6abacfd9` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 5 continues with `stripe-checkout-sessions.test.ts`.

**2026-09-24 UTC #1074 release receipt:** Trial-recovery #1073 merged as `746836b17f2b148adee48d1729f6c2ab8830f614` at **15:25:55Z** (exact-head approval **5306396186** at 15:16:22Z on `491e35a8`, one accepted finding across two heads; CI **36018402874** with 52/52 E2E, no retries) and is promoted through #1074 (`8d6cf8cd26ec50053a302234cb8914f0d6d1879d`, merged **15:39:43Z**) after the executable provenance proof and promotion CI **36020154545** (52/52 E2E, no retries). Main CI **36021803634** passed `test` at **15:51:23Z**. Vercel was Ready at **15:41:30.836Z**, observed STAGED without a production alias from **15:42:00Z** through **15:51:17Z**, then assigned production at **15:51:25.750Z**. Both trees equal `7fb7bd45cc0952f12208401a4dff11aebaedf710`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **15:52:20Z**.

### Base checkout fake migration disposition (2026-09-24 UTC)

`src/adapters/gateways/stripe/stripe-checkout-sessions.test.ts` (2 casts: the option-driven `StripeClient` literal and a second literal built only to prove the `subscriptions.list` binding; eighteen units) answered listing, retrieval, expiry and creation from indexed canned responses. It now constructs `FakeStripeCheckoutClient` on a mutable frozen clock and seeds real state: an existing open Session is created through the fake's own `create` under the clock each case needs (a day back plus a second for the Session that expires just after the frozen now, a day back for the one that expires exactly at it); the boundary-replay cases give the fake a clock that sits a day back for its first create only, so the deterministic key replays a Session expiring at the frozen now and the recovery create lands at it; inspections fail through a one-shot retrieve override; expiry errors come from the expire-fault seam, once or persistently. Two behaviors needed more than seams. The Subscription cases use the fake's new seeded Subscriptions: `subscriptions.list` reads `this`, as the SDK method does, so the binding case now fails whenever the adapter drops its `.bind`, and an invalid seeded status is an uncontracted injection. The two missing-URL cases use a new create-response override that bends only what `create` returns, paired with the retrieve override, because the fake always assigns a url and the adapter keeps the created url when the live one is missing.

The seeded Subscriptions are modelled provider behavior, so the shared fake↔Stripe contract gains its fifth scenario, `lists a customer's Subscriptions by id and status and retrieves them by id`: the real half attaches the test card to a fresh customer, creates an active Subscription on the TEST price with `metadata.user_id`, and both halves prove that `subscriptions.list({ customer, status: 'all', limit })` returns that Subscription with its id and status (and nothing under a status it does not have) and that `subscriptions.retrieve(id)` echoes id, customer, status, the metadata and the price. The real half ran in TEST mode on this clone (**15:02:45Z**, five scenarios, fresh customer and Subscription cancelled and deleted in cleanup); its first two runs exposed that Stripe lists full Subscription objects with list metadata, so the scenario asserts the port-visible fields by containment and the fake lists clones of the seeded objects. The register row carries the dated clarification. The fail-closed scheduled Stripe proof pins its case list, so the preview gate failed until the fifth scenario was registered there: the runner's unit test and its captured passed and skipped reporter fixtures now carry five Checkout cases (seven with the two trial-clock cases), the live testing and register docs say five scenarios and seven cases, and the real scheduled runner `pnpm test:stripe-provider` passed locally in TEST mode with `PASS executed=7 passed=7 skipped=0` (**16:53:58Z**). The created-expires-first reconciliation fixture correction from the #1076 promotion review lands in this increment too. **Review correction (2026-09-24 UTC):** three more live descriptions still carried the old contract — the integration guide's skipped-and-requested case count now says seven, the testing-infrastructure network-ownership row adds Subscription list/retrieve, and the register's contracted-scope column adds Subscription listing and retrieval (setup and cleanup calls such as the test-card attach and Subscription create/cancel stay out of the fake's row) — and the runner's timing comment names five Checkout cases. **Review correction (2026-09-24 UTC, outside-diff):** the create-response override's unit case now also covers the idempotent replay with a mutating override and proves the saved replay and the stored Session stay untouched; a replay that bypassed the override failed it (**17:42:39Z**), as did handing the saved response itself to the override (**17:42:40Z**). **Review correction (2026-09-24 UTC, fidelity):** the fake listed canceled Subscriptions when no status was given, but Stripe excludes them unless the status is `all`; the fake now does the same, its unit case failed before the fix (**18:53:48Z**) and passes after it (**18:53:49Z**), and the fifth contract scenario seeds a second Subscription that it cancels, then proves `all`, the default listing and `canceled` against both halves. The real half passed in Stripe TEST mode (**18:54:40Z**) and the scheduled runner still reports `PASS executed=7 passed=7 skipped=0` (**18:55:17Z**).

Mutation proofs against the production adapter, this clone, **2026-09-24 UTC**: dropping the `subscriptions.list` bind failed the binding case and every case that lists Subscriptions (**15:13:50Z**); accepting an invalid Subscription status failed the fail-closed case (**15:13:51Z**); skipping the missing-URL throw failed both missing-URL cases (**15:13:52Z**); treating any `rawType` as already terminal failed the `api_error` case (**15:13:53Z**); with the old stub file restored at its path, the lowered floor failed the ratchet (`2` unknown double cast sites above a floor of 0, **15:14:02Z**) and passes with the migrated file. Production was restored byte-identically each time.

Casts fall **65 / 17 files → 63 / 16**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. Sanitized receipts: this clone's `.git/claude-debt-resume/base/` and `.git/claude-debt-resume/subscriptions/`. The local full gate passed on `1f029688` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 5 continues with `-concurrency`.

**2026-09-24 UTC #1076 release receipt:** Reconciliation #1075 merged as `5e819c50cdffeaec5014c3d1f8c3f5cb91d7cf4a` at **16:34:48Z** (exact-head approval **5307227845** at 16:27:49Z on `a2bc3030`, one accepted finding across two heads; CI **36026737810** with 52/52 E2E, no retries) and is promoted through #1076 (`79ab7f5524b996887be31d1e590fe6fc2d984786`, merged **16:49:41Z**) after the executable provenance proof and promotion CI **36028406147** (52/52 E2E, no retries). Main CI **36030064054** passed `test` at **16:59:53Z**. Vercel was Ready at **16:51:12.088Z**, observed STAGED without a production alias from **16:51:59Z** through **16:59:12Z**, then assigned production at **16:59:55.571Z**. Both trees equal `c059bb777fe0504a3b9024eae4b760dece86b5c1`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **17:00:16Z**.

### Concurrency fake migration disposition (2026-09-24 UTC)

`src/adapters/gateways/stripe/stripe-checkout-sessions-concurrency.test.ts` (1 cast, two units) carried a stateful stub of its own — idempotent replay, sequential ids, an expiry that mutated the stub's map — plus a barrier that held both callers' first two listings until the second arrived. It now constructs `FakeStripeCheckoutClient`, whose replay, ids, listing and expiry are the contracted ones, and stages the same interleaving through a new sequencing seam, `setListHook`: the hook runs after a listing is recorded and before it is answered, and the suite awaits inside it only for the adapter's preflight listings (the only `status: 'open', limit: 1` listings it makes) until both callers have arrived, so neither can see the other's Session before both have created. The same-plan case then proves the second primary create replays the first (two recorded creates, one open Session); the different-plan case proves the post-create reconciliation keeps the later canonical Session open and expires the superseded one, read back through the fake's own filtered listings. The hook models no Stripe behavior and is documented as a seam, with a unit case proving it is awaited before a listing is answered. The #1078 promotion review's follow-through lands in this increment too: the contract's real half tracks the canceled Subscription until its cancel succeeds, and the F5 plan and step 5 status no longer list the landed Subscription seeder as pending.

Mutation proofs against the production adapter, this clone, **2026-09-24 UTC**: making the deterministic fallback key unique per call failed both cases (**15:17:36Z**); skipping the reconciliation expiry of superseded Sessions failed the different-plan case (**15:17:37Z**); with the old stub file restored at its path, the lowered floor failed the ratchet (`1` unknown double cast site above a floor of 0, **15:17:46Z**) and passes with the migrated file. Production was restored byte-identically each time.

Casts fall **63 / 16 files → 62 / 15**; own-code factories stay **0 / 0** and maintained-port doubles **44 / 17**. The fake gains a sequencing seam, not provider behavior, so no contract scenario is added; the register row carries the dated clarification. Sanitized receipts: this clone's `.git/claude-debt-resume/concurrency/`. The local full gate passed on `99da0aa9` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. Step 5 continues with `stripe-webhook-processor.test.ts`, the last file in its order.

**2026-09-24 UTC #1078 release receipt:** Base checkout #1077 merged as `122bc48c7e3164f3092a2f1605e0dfe7938ab147` at **19:17:16Z** (exact-head approval **5308948916** at 19:08:22Z on `39cfebbe`, three accepted findings and one accepted outside-diff claim across four heads; CI **36045558378** with 52/52 E2E, no retries) and is promoted through #1078 (`82e82c84838fdf79403cdf22dab5b14144da06f8`, merged **19:33:04Z**) after the executable provenance proof and promotion CI **36047132772** (52/52 E2E, no retries). Main CI **36048913884** passed `test` at **19:44:45Z**. Vercel was Ready at **19:34:27.780Z**, observed STAGED without a production alias from **19:35:19Z** through **19:44:35Z**, then assigned production at **19:44:48.961Z**. Both trees equal `39a1406fa212321eeea2f95967ebca5220f052fd`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **19:46:40Z**.

### Webhook-processor fake migration disposition (2026-09-24 UTC)

`src/adapters/gateways/stripe/stripe-webhook-processor.test.ts` (one cast, the signature-failure stub, and one hand-rolled port double, the typed `StripeClient` literal its helper returned; eighteen units) was mostly typed test data: hand-built event payloads handed to the processor through a `constructEvent` that ignored body, signature and secret, a `subscriptions.retrieve` that returned the same fixture whatever id was asked for, and per-case `setupIntents` assignments. It now constructs `FakeStripeCheckoutClient` and hands each case's event through three seams: `setWebhookEvent` injects the event and records the verification call (body, signature and secret, so the adapter's plumbing is now observable), `seedSetupIntent` serves the setup-intent retrieval by id with the calls recorded, and the seeded Subscriptions from the base-checkout increment serve `subscriptions.retrieve` by id with the calls recorded. Two things changed in what the cases prove. The nested-invoice cases now seed the Subscription under the id the invoice references and expect that id back, as a live retrieval would; the retired stub returned `sub_123` for any requested id, an echo Stripe cannot produce. The invalid-signature setup case now uses a well-formed but wrong signature, because the retired `invalid` fixture was rejected by the metadata schema's shape before the server-side HMAC check ran, so that check was unobservable. The signature-failure case needs no injection: the fake's `constructEvent` throws when no event is injected, so the case (renamed "maps a constructEvent failure to INVALID_WEBHOOK_SIGNATURE") pins the processor's error mapping, and its `webhookCalls` assertion pins argument forwarding only. Neither is Stripe signature verification, which the fake does not model; rejection of real bad signatures is proven with Stripe-generated signatures in `tests/integration/webhook-signature-ingress.integration.test.ts` (#1081 review correction, 2026-09-24 UTC). Three near-duplicate consent-warning cases and the two nested-invoice cases become `it.each` variants; the file drops from 860 to under the 800-line limit, so its DEBT-469 length suppression goes with it. Event injection and the setup-intent seeder model no Stripe behavior and are documented in the register as seams. The #1080 promotion review's follow-through lands here too: the list-hook unit case now proves a listing is recorded before its hook runs, and the landed list hook leaves the F5 and step 5 pending lists.

Mutation proofs against the production processor and schemas, this clone, **2026-09-24 UTC**: swapping the signature and secret handed to `constructEvent` failed the setup-completion and signature-failure cases (**15:23:03Z**); ignoring the nested invoice reference failed both nested-invoice cases and the preference case (**15:23:04Z**); skipping the consent identity check on the user id failed the identity-mismatch case (**15:23:05Z**); skipping the setup consent-state signature check failed the invalid-signature case once that case carried a well-formed wrong signature (**15:25:06Z**; with the retired fixture the same mutation was unobservable at both signature checks, recorded in the receipts); with the old file restored at its path, the lowered floors failed the ratchet (`1` unknown double cast site and `1` hand-rolled maintained-port double site above floors of 0, **15:23:58Z**) and pass with the migrated file. Production was restored byte-identically each time.

Casts fall **62 / 15 files → 61 / 14**; maintained-port doubles fall **44 / 17 → 43 / 16**; own-code factories stay **0 / 0**. Sanitized receipts: this clone's `.git/claude-debt-resume/webhook/`. The local full gate passed on `99eec09d` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending. With this file step 5's eight-file order is complete; the record's remaining open work is step 4's named residue.

**2026-09-24 UTC #1080 release receipt:** Concurrency #1079 merged as `1bea92c6bddfed2f86d88d4e91ce381b0ac4683b` at **20:08:43Z** (exact-head approval **5309584858** at 20:03:37Z on `e32ea29b`, zero findings; CI **36051394041** with 52/52 E2E, no retries) and is promoted through #1080 (`5e35569ca8a6a8f71c3a717b85db6da1f5d68b19`, merged **20:23:09Z**) after the executable provenance proof and promotion CI **36052897832** (52/52 E2E, no retries). Main CI **36054514130** passed `test` at **20:34:37Z**. Vercel was Ready at **20:24:35.012Z**, observed STAGED without a production alias from **20:25:23Z** through **20:34:37Z**, then assigned production at **20:34:38.796Z**. Both trees equal `416c4268af0b3c3bd800464b807a42bc9307a726`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **20:35:40Z**.

**2026-09-24 UTC #1082 release receipt:** Webhook-processor #1081 merged as `6a5f5bf42933e47faeea4c58740876637b2e1700` at **22:06:19Z** (exact-head approval **5310762889** at 22:03:14Z on `e6589646`, one accepted finding; CI **36059065496** with 52/52 E2E, no retries) and is promoted through #1082 (`0673451d61b91910843bd5db15393c782d111e23`, merged **22:18:57Z**) after the executable provenance proof and promotion CI **36065580706** (52/52 E2E, no retries). Main CI **36066727475** passed `test` at **22:30:00Z**. Vercel was Ready at **22:20:15.632Z**, observed STAGED without a production alias from **22:21:14Z** through **22:29:27Z**, then assigned production at **22:30:02.654Z**. Both trees equal `20fab29ef7660585e09bdd0cdf203fbaf69010ba`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **22:30:30Z**. Step 5's file order is released.

### Subscription-cancel contract and reconcile version-fence disposition (2026-09-25 UTC)

A DEBT-468 Part 3 (a) follow-on under this record's contract discipline. `src/adapters/jobs/reconcile-stripe-subscriptions-version-fence.test.ts` (two units, one hand-rolled port double: a full `StripeClient` literal whose Checkout, portal, customer and webhook members all threw, whose `subscriptions.list` answered an empty page whatever customer was asked for, and whose `cancel` returned the active Subscription) could not move onto `FakeStripeCheckoutClient` as a zero-extension proof: the reconcile job binds `subscriptions.list` and `subscriptions.cancel` before it touches any row and fails closed when either is missing, and the fake had no `cancel`. The migration's first run failed on exactly that guard, so the fake now models cancel.

What cancel models was settled against Stripe before any fake code: the [Cancel a subscription](https://docs.stripe.com/api/subscriptions/cancel) reference documents only the success response, and the `resource_missing` error-code entry says the resource does not exist. A direct TEST-mode probe on 2026-09-24 UTC (a throwaway customer and trialing Subscription, deleted afterwards) observed a first cancel returning the canceled Subscription, a retrieve still returning it as canceled, and a second cancel failing with HTTP 404, `invalid_request_error`, `resource_missing`, `param: id` and the message `No such subscription: '<id>'`, while the default listing excluded it and `status=all` kept it. The documentation gap was reported to Stripe. The fake mirrors exactly that: `subscriptions.cancel` marks a seeded Subscription canceled once and returns it, a repeat or unknown id throws the same 404 shape, and `cancelCalls` records each id and its options. It ignores idempotency keys, as Stripe does for DELETE requests (**2026-09-25 UTC #1086 review correction:** this sentence first claimed Stripe replays a reused key's first result; Stripe's [Idempotent requests](https://docs.stripe.com/api/idempotent_requests) page says keys have no effect on GET and DELETE, a second TEST-mode probe got the same 404 for a same-key repeat cancel, and the sixth scenario now reuses the first cancel's key to pin that). The reconcile job passes an `idempotencyKey` to `subscriptions.cancel`; by the same documentation it has no effect there, and the job's `resource_missing` handling is what makes a repeat safe.

The behavior is modelled, so the shared contract gains its sixth scenario, `cancels a Subscription once and rejects a repeat cancel as resource_missing`. The real half wraps `stripe.subscriptions.cancel` and untracks a Subscription as soon as its cancel succeeds, because cleanup's own cancel would otherwise get the same 404. Written first, the fake unit case, the fake's sixth scenario and the registration pin failed (**00:12:46Z**); after the fake change `pnpm test:stripe-provider` passed against Stripe TEST mode with `PASS executed=8 passed=8 skipped=0` (**00:13:39Z–00:14:19Z**), and the scheduled runner's pinned counts, report fixtures and timing comment move from seven to eight cases.

The version-fence suite then seeds its one Subscription on the fake and reads the recorded calls instead of two counters; the listing assertion now pins the customer and `status: 'all'`, which the counters could not. Mutation proofs against the production job and the shared observation loop, this clone: reading the version first instead of using the row's (**00:15:45Z**) and skipping the re-read after a conflict (**00:15:46Z**) each fail the convergence case; reusing the first observation instead of re-retrieving fails both cases (**00:15:48Z**); listing without `status: 'all'` fails the convergence case on the new listing assertion (**00:15:58Z**). Production restored byte-identically each time. With main's suite restored and its floor entry removed, `pnpm lint:doubles` fails on the `StripeClient` literal (**00:16:37Z**).

Maintained-port doubles fall **43 / 16 → 42 / 15**; casts stay **61 / 14** and own-code factories **0 / 0**. The main reconcile suite, whose stub answers every listing regardless of customer or status, is the next consumer. Sanitized receipts: this clone's `.git/claude-debt-resume/cancel/proofs.log`. The local full gate passed on `096a5859` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

### Reconcile suite fake migration and concern split disposition (2026-09-25 UTC)

The DEBT-468 Part 3 (a) follow-on to the cancel contract, and a DEBT-469 step 5 split. `src/adapters/jobs/reconcile-stripe-subscriptions.test.ts` (1,799 lines, 37 cases, one hand-rolled port double) drove the job through three stub builders — 28 fixture-backed stubs, eight bare ones and three hand-assembled overrides — whose `list` answered a caller-chosen page whatever customer or status was asked for, whose `retrieve` looked fixtures up by a key that could differ from the id they carried, and whose `cancel` returned a canned object; its API-unavailable case cast an object literal to the port. It now runs on `FakeStripeCheckoutClient`, whose Subscription listing is contracted by customer and status and whose cancel is contracted against Stripe, and it splits by concern: batch processing, row failures and persistence stay at the original path (15 cases, 500 lines), canonical selection moves to `reconcile-stripe-subscriptions-canonical.test.ts` (14 cases, 430 lines) and duplicate cancellation to `reconcile-stripe-subscriptions-duplicates.test.ts` (8 cases, 314 lines), over `src/adapters/jobs/test-helpers/reconcile-stripe-subscriptions-harness.ts` (189 lines). All 37 case titles are unchanged (the sorted before/after lists are identical).

Three test seams carry what the stubs scripted, each written red first (**02:47:54Z**) and green after (**02:49:05Z**): a Subscription retrieve override holds retrievals open for the bounded-concurrency case and returns the two responses Stripe cannot send (an empty or foreign id) for the defensive canonical checks; a cancel hook injects the authentication and transport faults, and stages a Subscription canceled elsewhere through `markSubscriptionCanceled`, so the three already-canceled cases now meet the contracted 404 instead of a hand-built error object. The API-unavailable case passes the fake without its Subscriptions member, so no cast remains. Where the fake allows it, cases also assert the provider side: canceled duplicates are canceled in the fake's listing, not just requested.

Mutation proofs against the production job and the canonical comparator, this clone: a default concurrency of 11 fails the concurrency case (**02:53:44Z**); breaking ties by the largest id fails both tie cases (**02:53:45Z**); treating already-canceled cancel errors as failures fails the three already-canceled cases (**02:53:47Z**); canceling in dry-run fails the three dry-run cases (**02:53:48Z**); a non-authoritative customer-mapping write fails the remap case (**02:53:51Z**). A blocking filter that let canceled listings through survived both the new suites (**02:53:50Z**) and main's stub suite (**02:54:36Z**), because no case listed a canceled Subscription beside live ones; the duplicate scenario now includes an old canceled Subscription for the same customer, as real customers have, and the same mutation fails four duplicate cases (**02:54:25Z**). Production restored byte-identically each time. Without its suppression main's suite fails `biome lint --error-on-warnings` at 1,798 lines (**02:55:00Z**) while the three suites and the harness pass unsuppressed; with main's suite restored and its floor entry removed, `pnpm lint:doubles` fails on the `StripeClient` literal (**02:55:27Z**).

The #1086 promotion-review follow-through lands first: the cancel comment, the register and the cancel disposition no longer claim Stripe replays a reused key on cancel, and the sixth contract scenario's repeat cancel reuses the first cancel's key (real TEST run `PASS executed=8 passed=8 skipped=0`, **02:46:37Z–02:47:17Z**).

Maintained-port doubles fall **42 / 15 → 41 / 14**; casts stay **61 / 14** and own-code factories **0 / 0**; DEBT-469 suppressions fall **23 → 22**. Sanitized receipts: this clone's `.git/claude-debt-resume/reconcile/proofs.log`. The local full gate passed on `6603deb3` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

**2026-09-25 UTC #1086 release receipt:** Cancel contract #1085 merged as `53425f8ad0060dc5405005551eb2cc1d29d07814` at **02:30:41Z** (exact-head approval **5312577617** at 02:28:15Z on `db5ba571`, two accepted review findings and one coverage correction; CI **36082421631** with 52/52 E2E, no retries) and is promoted through #1086 (`8c6f2df9e424c9a68bb4bf9b2e194ae6b5f0fd16`, merged **02:45:41Z**) after the executable provenance proof and promotion CI **36086585359** (52/52 E2E, no retries). Main CI **36087592643** passed `test` at **02:54:35Z**. Vercel was Ready at **02:46:53.782Z**, observed STAGED without a production alias from **02:47:55Z** through **02:54:03Z**, then assigned production at **02:54:37.525Z**. Both trees equal `e20a94d15c0501b168b52dffbe00280dab5163d8`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **02:55:07Z**.

**2026-09-25 UTC #1088 release receipt:** Reconcile suite #1087 merged as `bfcd9ffd55e13c9562f138bf614e57e6766cf914` at **03:26:01Z** (exact-head approval **5312895594** at 03:17:18Z on `faef20a5`, zero findings; CI **36089430963** with 52/52 E2E, no retries) and is promoted through #1088 (`69ad05c4a99c7240cc3fccb9ffb00d34a27be50a`, merged **03:39:29Z**) after the executable provenance proof and promotion CI **36090362397** (52/52 E2E, no retries; CodeRabbit approved with no actionable comments). Main CI **36091244502** passed `test` at **03:50:51Z**. Vercel was Ready at **03:40:46.201Z**, observed STAGED without a production alias from **03:41:45Z** through **03:49:57Z**, then assigned production at **03:50:53.312Z**. Both trees equal `7d81494d44b83502e536c14fe357a8c315552126`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **03:51:00Z**.

### Proxy real-request disposition (2026-09-25 UTC)

Step 4's named residue, together with a DEBT-469 step 5 split. `proxy.test.ts` (890 lines, 33 cases) invoked the Next proxy 14 times with an empty object cast to `NextRequest` and another cast to `NextFetchEvent` (four requests carried only a `url`), which the ratchet counted as 28 casts. It now passes real objects through `tests/shared/next-proxy-invocation.ts`: a `NextRequest` built from a URL, defaulting to a protected app route, and a `NextFetchEvent` built with its public constructor. `next/server` exports `NextFetchEvent` only as a type, so the helper takes the class from Next's published `fetch-event` module; the helper is unit-tested and was written red first. The suite splits by concern: routing, Clerk protection and checkout-bounce logging stay at the original path (`proxy.test.ts`), content-security-policy assembly moves to `proxy-csp.test.ts` (8 cases) and the pure Sentry DSN parsers to `proxy-sentry-endpoints.test.ts` (15 cases). All 33 case titles are unchanged (**05:50:26Z**).

Real requests also expose a gap: the proxy passes its exact public resource paths (`/.well-known/security.txt`, `/robots.txt`, `/sitemap.xml`, the social image) through before Clerk starts, reading `request.nextUrl`, and no unit case covered that bypass, because the cast `{}` requests had no `nextUrl`. Four cases now cover it, one per path; they assert a pass-through response and that the Clerk middleware is never created. Removing the bypass fails all four (**05:50:52Z**) and passes main's whole suite (**05:51:09Z**).

Further mutations against the production proxy: dropping the Clerk middleware cache fails the caching case (**05:50:53Z**); flipping the CSP to enforcing fails four CSP cases (**05:50:54Z**); dropping the checkout-bounce log fails the three bounce cases (**05:50:55Z**); letting the Sentry origin parser accept non-HTTP schemes fails three parser and CSP cases (**05:51:08Z**). **#1091 review corrections:** the CSP cases share one Clerk-mock helper that also returns the proxy's response, and the invalid-DSN case now asserts that no Sentry reporting (`reportTo` or `report-uri`) is emitted; a caller that always sets `reportTo` fails it and the unset-DSN case (**06:16:54Z**), and the enforcing-CSP mutation still fails the four CSP cases after the consolidation (**06:16:39Z**). Production restored byte-identically each time. Without its suppression main's suite fails `biome lint --error-on-warnings` at 889 lines (**05:51:21Z**) while the three files pass unsuppressed; with main's suite restored and its floor entry removed, `pnpm lint:doubles` fails on the 28 casts (**05:51:44Z**).

Casts fall **61 / 14 → 33 / 13**; maintained-port doubles stay **41 / 14** and own-code factories **0 / 0**; DEBT-469 suppressions fall **21 → 20**. Step 4's remaining residue is the three Search-behavior cases and the three `db/schema.test.ts` casts. Sanitized receipts: this clone's `.git/claude-debt-resume/proxy/proofs.log`. The local full gate passed on `ada63015` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

### Schema public-config disposition (2026-09-25 UTC)

Step 4's other named cast residue. `db/schema.test.ts` (8 cases) cast three Drizzle tables to `Record<symbol, unknown>` to reach Drizzle's private `ExtraConfigBuilder`/`ExtraConfigColumns` symbols, called the builder to recover the declared indexes and checks, and then walked a check's internal query chunks to find its SQL text. It now reads the same declarations through Drizzle's public API: `getTableConfig` from `drizzle-orm/pg-core` for indexes and checks, and `PgDialect.sqlToQuery` to render a check's SQL and parameters. All 8 case titles are unchanged (**07:00:22Z**), no cast or private symbol remains, and the pins are stronger. The partial unique index now asserts its uniqueness and its partial predicate, not only its name. The day-bound check now asserts that the bound is rendered into the SQL text and absent from the bind parameters, which a `CHECK` constraint cannot take.

Mutations against `db/schema.ts`: renaming the started-at index (**07:00:32Z**), declaring the incomplete-session index non-unique (**07:00:34Z**), dropping its `ended_at IS NULL` predicate (**07:00:35Z**) and passing the day bound as a bind parameter (**07:00:36Z**) each fail their case. The non-unique and predicate mutations pass main's name-only suite (**07:00:48Z**, **07:00:49Z**). **#1093 review corrections:** the partial index now asserts its rendered predicate is `ended_at IS NULL` (inverting it fails the case, **07:30:59Z**), and the day-bound check pins the exact rendered SQL with an empty parameter list. The suggested `toContain(`BETWEEN 0 AND ${DAY_MS}`)` would still pass a ten-day bound, whose digits begin with DAY_MS's (**07:31:00Z**); the exact-text assertion fails it (**07:31:17Z**). **#1094 promotion-review follow-through:** the predicate assertion was still a containment check, which a predicate that disables the index (`ended_at IS NULL AND false`) passes (**07:59:58Z**); it now pins the exact text, which fails that mutation (**08:00:00Z**) and the inversion (**08:00:01Z**). Production restored byte-identically. With main's suite restored and its floor entry removed, `pnpm lint:doubles` fails on the three casts (**07:01:12Z**).

Casts fall **33 / 13 → 30 / 12**; maintained-port doubles stay **41 / 14** and own-code factories **0 / 0**. Step 4's only remaining residue is the three `stripe-customers` Search-behavior cases. Sanitized receipts: this clone's `.git/claude-debt-resume/schema/proofs.log`. The local full gate passed on `09019bb4` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

**2026-09-25 UTC #1092 release receipt:** Proxy #1091 merged as `7c3bd56263f89104af9f501ef7069effcab50e83` at **06:46:07Z** (exact-head approval **5314285172** at 06:29:01Z on `b22ae1b1`, two accepted findings; CI attempt 1 failed on a Google Fonts fetch in Build, documented, attempt 2 green; CI **36102822545** attempt 2 with 52/52 E2E, no retries) and is promoted through #1092 (`b11fd55cebc0b578bbecc8f2041a7cf13db03ac4`, merged **06:59:38Z**) after the executable provenance proof and promotion CI **36104403016** (52/52 E2E, no retries; CodeRabbit approved with no findings). Main CI **36105424853** passed `test` at **07:11:09Z**. Vercel was Ready at **07:00:57.214Z**, observed STAGED without a production alias from **07:01:55Z** through **07:11:09Z**, then assigned production at **07:11:11.498Z**. Both trees equal `01cc86613f1e3867142f26726659410e81de4eb2`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **07:12:13Z**.

### Customer Search contract disposition (2026-09-25 UTC)

This is step 4's last residue. `stripe-customers.test.ts` held three Search-behavior cases (matching, absent and duplicate customers) on hand-built `customers` objects cast to the client type, and four other cases replaced the fake's Search with a local empty answer. The [2026-09-23 attempt](#customer-search-replacement-attempt-2026-09-23-utc) tried provider cases instead and stopped when a mutation failed for the wrong reason.

**Ground truth first.** Four TEST-mode probes measured what a fake would have to model. They printed counts only, and every probe Customer was deleted.
- A new Customer first appeared in metadata Search after 10–29 seconds.
- After it appeared, later reads could briefly return nothing. In one run the once-a-second reads after first visibility were `2222022222020222…`: four empty reads in 25.
- Matching is on the whole value. In 20 reads, values that extend the searched one by `-other` or `_other` never matched.
- Matching is case-insensitive, as [Stripe's search query language](https://docs.stripe.com/search) documents for the `:` operator.
- An unknown value returns an empty page.

**The fake.** `FakeStripeCheckoutClient` now answers `customers.search` from Customers seeded with `seedCustomer(metadata)`.
- It accepts a single `metadata['key']:'value'` clause and matches the whole value, ignoring case.
- It caps results at the limit, which defaults to Stripe's 10.
- Like the SDK method, it needs its receiver.
- Any other query throws instead of answering empty.
- `searchCalls` records each call.
- It is immediately consistent. Stripe's indexing delay and read regressions are documented divergences in the register.
- `customers.create` stays a canned id.

**The contract.** The seventh shared scenario is `finds Customers by whole, case-insensitive metadata value through Search once indexed`. It first seeds a Customer whose value extends a fresh user id, then two Customers for the user id itself. It checks the extended value alone, the exact pair (which excludes the extension), the upper-cased query, a limit of one, and an empty page for an unknown value.

The harness's `searchUntil(params, minimum)` returns the first read with at least `minimum` Customers. The real half polls once a second, and one 60-second bound covers all of the case's reads; the fake reads once. Each read is then asserted in full, comparing ids without printing them. The case registers a 120-second budget, and the provider lane's other cases keep 20 seconds.

Four fake mutations each fail this scenario (**07:54:28–07:54:30Z**): prefix matching, case-sensitive matching, an ignored limit, and matching every Customer with the key. The provider proof now requires nine cases. It passed twice in TEST mode with `PASS executed=9 passed=9 skipped=0`, at **07:56:32–07:57:33Z** and **07:57:51–07:59:01Z**. **#1095 review corrections:** the first version gave each read its own 60-second bound, so four sequential reads could outrun the 120-second case budget. One bound now covers every read in the case. The budget was not raised to the suggested 300 seconds, because that equals the provider run's whole five-minute process bound. It passed again at **08:38:19–08:39:17Z**. The integration guide's receipt paragraph also now names the nine-case runs.

**The migration.** The matching, absent and duplicate cases seed Customers in the fake, and the four create-path cases drop their local Search stubs. All seven titles are unchanged (**07:58:21Z**). Six production mutations each fail the migrated suite (**07:59:15–07:59:19Z**):
- calling Search unbound (six cases fail);
- raising the duplicate threshold past two;
- searching the wrong metadata key;
- ignoring an existing match;
- a limit of one;
- dropping the id validation.

Main's suite also caught all six, through its stubs' call assertions. The migrated suite catches the limit-of-one mutation through the fake's behavior as well, because a one-result page hides the duplicate. Production was restored byte-identically. With main's suite restored and its floor entry removed, `pnpm lint:doubles` fails on the three casts (**08:01:24Z**).

**Production observation, not a defect claim.** The adapter's single Search read is a recovery path. The checkout use case reads its own Customer mapping first and calls Stripe only when the mapping is missing. Customer creation also carries the per-user idempotency key. A regressed read therefore matters only for a Customer Stripe holds but the mapping lacks, after that key has expired. No production change is made.

**Closeout audit.** With every Resolution step done, the [Verification](#verification) section was checked again against this branch.
- F1/F2, F3, F4, F6/F7, F8 and the whole-debt ratchet hold.
- **F5 does not hold.** Two suites still hand-build Stripe clients: `stripe-payment-gateway.test.ts` (1,537 lines) and `stripe-webhook-processor-setup-expiration.test.ts`. Their `subscriptions.retrieve` defaults return `{}` (`stripe-payment-gateway.test.ts:144`, `stripe-webhook-processor-setup-expiration.test.ts:29`). Their Session listings omit `has_more` (`:86-87` and `:23`), and the payment-gateway overrides list Sessions without `created`.

The record therefore stays Active until those suites move onto the fake. The smaller setup-expiration suite goes first; the payment-gateway suite follows together with its DEBT-469 concern split.

Casts fall **30 / 12 → 27 / 11**; maintained-port doubles stay **41 / 14** and own-code factories **0 / 0**. **Step 4 is complete.** Sanitized receipts: this clone's `.git/claude-debt-resume/search/proofs.log`. The local full gate passed on `189bb671` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

**2026-09-25 UTC #1094 release receipt:** Schema #1093 merged as `9265bcc9fbb5cb1449443fc7152941b7a04c2b36` at **07:51:18Z** (exact-head approval **5314968283** at 07:41:48Z on `b4266867`, two accepted findings, the suggested containment check strengthened to exact text; CI **36108741246** attempt 1 with 52/52 E2E, no retries) and is promoted through #1094 (`148e440d6968c00d02559c8e783f0fc80085ddd0`, merged **08:03:47Z**) after the executable provenance proof and promotion CI **36109834809** (52/52 E2E, no retries). CodeRabbit's one promotion finding, to pin the partial-index predicate exactly, is accepted as follow-through in this increment's first commit (see [the schema disposition](#schema-public-config-disposition-2026-09-25-utc)). Main CI **36110882154** passed `test` at **08:13:38Z**. Vercel was Ready at **08:05:06.568Z**, observed STAGED without a production alias from **08:06:05Z** through **08:13:17Z**, then assigned production at **08:13:40.580Z**. Both trees equal `86b11af7dfa858db231b8cfcd232cba6fcf6c3b6`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **08:15:49Z**.

### Setup-expiration fake migration disposition (2026-09-25 UTC)

This is the smaller of Verification F5's two counterexamples from the [closeout audit](#customer-search-contract-disposition-2026-09-25-utc). `stripe-webhook-processor-setup-expiration.test.ts` (5 cases) built a typed `StripeClient` literal. Its `subscriptions.retrieve` answered `{}`, its Session listing omitted `has_more`, and two cases passed `vi.fn()` retrievals to prove none happened.

The suite now runs on `FakeStripeCheckoutClient` with no new seam. The injected webhook event (`setWebhookEvent`) supplies each expired Session. The fake's recorded `setupIntents.retrieveCalls` and `subscriptions.retrieveCalls` prove that the signed-state path retrieves nothing. All five titles are unchanged (**08:59:00Z**).

Four mutations against `stripe-webhook-processor.ts` each fail their case:
- retrieving a Subscription during expiration normalization (**08:59:33Z**);
- normalizing without the dedicated secret (**08:59:35Z**);
- treating any expired Session as a setup Session (**08:59:36Z**);
- accepting a forged state signature (**08:59:51Z**).

The same four also fail main's suite (**08:59:37–08:59:52Z**), so the migration preserves detection rather than adding it. Production was restored byte-identically. With main's suite restored and its floor entry removed, `pnpm lint:doubles` fails on the literal (**09:00:18Z**).

Maintained-port doubles fall **41 / 14 → 40 / 13**; casts stay **27 / 11** and own-code factories **0 / 0**. F5's one remaining counterexample is `stripe-payment-gateway.test.ts`. Sanitized receipts: this clone's `.git/claude-debt-resume/setup-expiration/proofs.log`. The local full gate passed on `48a78ffd` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

**2026-09-25 UTC #1096 release receipt:** Customer Search #1095 merged as `5a3e8c86c5a08b53086de4cdcfde5ebf0538b5cb` at **08:58:04Z** (exact-head approval **5315658154** at 08:55:15Z on `52ab3ec6`, two accepted findings, the timeout fixed with a shared per-case bound instead of the suggested 300 seconds; CI **36114847300** attempt 1 with 52/52 E2E, no retries) and is promoted through #1096 (`a257dfe34f2448f32e62252905e6d0930c5e667c`, merged **09:11:56Z**) after the executable provenance proof and promotion CI **36115851395** (52/52 E2E, no retries). Main CI **36117093141** passed `test` at **09:23:18Z**. Vercel was Ready at **09:13:11.725Z**, observed STAGED without a production alias from **09:13:20Z** through **09:22:35Z**, then assigned production at **09:23:19.890Z**. Both trees equal `9573630eb1b77bac7246b9a22742dae7d33b8c83`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **09:23:38Z**.

### Payment-gateway webhook section disposition (2026-09-25 UTC)

This is the first of three increments on F5's remaining counterexample, `stripe-payment-gateway.test.ts`. That suite tests the `StripePaymentGateway` facade, but 23 of its 44 titles (26 of 52 runtime cases) re-tested webhook normalization through the facade's hand-built client. A read-only twin map of all 44 cases found three things:
- Several behaviors had no adapter-level case at all: routing for seven subscription event types and `invoice.payment_action_required`, `occurredAt`, the invalid-payload logs, the signature-failure message, and checkout completions with no subscription key.
- Two facade forwards had no case anywhere: the consent-state secret and the E2E owner.
- The rest were duplicates.

**Twins first.** `stripe-webhook-processor.test.ts` gains cases on the fake:
- every subscription event type and every root-referenced event, each with its `occurredAt`;
- malformed references, and retrieved Subscriptions that fail the schema, each with its error log (the fake's retrieve override supplies an empty item list);
- `metadata.user_id` missing on the updated, created and checkout paths;
- a checkout completion without a subscription key;
- the signature-failure message, added to the existing case.

The suite grows from 18 to 38 runtime cases (715 lines, under the file limit).

Nine mutations against the processor, schemas and normalizer show the effect (**09:37:49–09:38:35Z**):
- Main's processor suite let all nine pass.
- The new processor suite fails every one.
- Main's gateway suite caught eight of them (**09:34:55–09:35:21Z**, **09:38:21–09:38:35Z**) but not the dropped `occurredAt` on root-referenced events.

The normalizer's existing missing-client case fails when its guard is removed (**09:39:08Z**).

**Then retirement.** The facade's 21 duplicated webhook titles retire, each behind a named processor or normalizer twin; the twin list is in the proofs log. Four facade cases remain, all on the fake:
- the webhook secret and price ids reach verification and normalization;
- the logger receives verification failures;
- a correctly signed expired setup Session normalizes, which proves the consent-state secret is forwarded;
- a Subscription owned by another E2E runner is rejected, which proves the E2E owner is forwarded.

Dropping any of the five forwards fails the new facade cases (**09:38:19–09:38:31Z**). Main's suite let the consent-secret and E2E-owner forwards be dropped (**09:38:23Z**, **09:38:26Z**).

The gateway suite falls from 1,537 to 991 lines and from 52 to 30 runtime cases. Its port-double floor stays **8**: the base client literals and the six payment-method literals remain, and so does its DEBT-469 suppression. The next increment takes the Checkout, customer and portal section; the last adds `paymentMethods` and `subscriptions.update` to the fake under a contract scenario. Sanitized receipts: this clone's `.git/claude-debt-resume/gateway-webhooks/proofs.log`. The local full gate passed on `054cc1d3` and is re-run on every later head before it is pushed; hosted CI, exact-head approval, merge and promotion remain pending.

**2026-09-25 UTC #1098 release receipt:** Setup-expiration #1097 merged as `238fc03242bda1e048fcff5a1355f01c8127f2b5` at **09:44:49Z** (exact-head approval **5316030148** at 09:37:20Z on `48141759`, no findings; CI **36118954054** attempt 1 with 52/52 E2E, no retries) and is promoted through #1098 (`9ef820192377b1a2b8c9e8f2f78da0121cb5e4aa`, merged **09:58:15Z**) after the executable provenance proof and promotion CI **36120189081** (52/52 E2E, no retries). Main CI **36121393331** passed `test` at **10:09:44Z**. Vercel was Ready at **09:59:34.220Z**, observed STAGED without a production alias from **10:00:27Z** through **10:09:42Z**, then assigned production at **10:09:46.566Z**. Both trees equal `1d7803495ea5d97e3cd39c0da8ab6e6e0847a973`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **10:10:45Z**.

## Description

**2026-09-23 UTC promotion pointer:** Quick Practice #1031 and Practice starter #1032 are merged and promoted through #1033 (`21912817`, **13:57:00Z**). The executable promotion proof records source approvals **5291471054** / **5291748690** predating both source merges and zero unresolved threads. Promotion CI **35869147932** passed **5,698 unit / 420 browser / 369 integration plus six opt-in skips / 52 E2E** without retries. Main CI **35870656427** and the production gate/health verification are still pending at this update; the earlier pending summaries above are historical, not claims that the source PRs remain open.

**2026-09-23 UTC #1033 release receipt:** The pending verification above is complete. Main CI **35870656427** passed `test` at **14:08:20Z**. Vercel was Ready at **13:58:19.434Z** and assigned the production domains at **14:08:22.714Z**, after that check (`readySubstate: PROMOTED`, no alias error). Both trees equal `b068b448930fccb943d0e2e6a4dcb2ac364ec1e4`; production `/` and `/api/health` returned 200 with `{"ok":true,"db":true}` at **14:41Z**. An independent re-audit of every merge since #1008 found each feature head approved by CodeRabbit before its merge and each promotion mapped to approved sources; #1012 merged with only the promotion-exempt CodeRabbit status still pending and no posted findings. The three root-layout mutations (Providers nonce, ThemeProvider nonce, `forcedTheme` light) were reproduced against `7d7e9f81` at **14:47Z**, each failing exactly one preserved case before byte-identical restoration.

**2026-09-23 UTC Quick Practice review correction (#1031):** The browser twin's role/name lookup did not preserve the deleted unit's explicit `fieldset` and `legend` assertions. Both are now restored: a temporary `div role="group"` mutation failed on `DIV` versus `FIELDSET` at **12:58:32Z**; a separately labelled fieldset with a span instead of a legend failed on the missing legend at **12:59:18Z**. Production is restored byte-for-byte; no UI change ships. The register's Quick Practice update moves from the issue cell to its description, and the unit retains its original alias-import convention. The earlier `/private/tmp` logs were removed by the machine restart; their dated summaries and hosted receipts remain, but the raw local files are no longer available. New sanitized receipts are kept under this clone's `.git/codex-debt-resume.dnZFSM/`.

"Fakes over mocks" is the right principle. The repository adopted half of it.

**Review-proof bootstrap note (2026-09-23 UTC):** The restored-source focused browser invocation at **12:59:55Z** executed no cases and reported Vitest's `Orchestrator not found` startup error. The single DEBT-469 W5 retry passed all six cases at **13:00:16Z**; all nine retained units then passed. This is the documented bootstrap exception, not an E2E retry or a product-test failure. The complete pre-push gate remains independently required.

The canon (Meszaros via Fowler; *Software Engineering at Google* ch. 13) has two clauses. First: prefer a **fake** — "objects [that] actually have working implementations, but usually take some shortcut which makes them not suitable for production" — over a **stub** that "provide[s] canned answers" or a **spy** that is a stub that "also record[s] some information" ([Fowler, *Mocks Aren't Stubs*](https://martinfowler.com/articles/mocksArentStubs.html)). Second, and load-bearing: "A fake must have its own tests to ensure that it conforms to the API of its corresponding real implementation," achieved by "writing tests against the API's public interface and running those tests against both the real implementation and the fake" — contract tests ([*Software Engineering at Google*, ch. 13 "Test Doubles" → "Fakes Should Be Tested"](https://abseil.io/resources/swe-book/html/ch13.html)). Without the second clause a fake is an elaborate mock: it can diverge from reality and nothing detects it.

The live rules encode only the first clause, and encode it with an exception that swallows every external seam. The consequences are measurable and are listed as findings F1–F8 below.

### F1 — The live rule prescribes inline `vi.fn()` doubles for exactly the collaborators whose behavior matters

`.claude/rules/testing.md:25-28` carries the decision tree verbatim:

```text
- Fake exists? Use it.
- External dependency (Drizzle, Clerk, Stripe SDK)? Use `vi.fn()` inline or `vi.mock()`.
- No fake exists for our code? Create one in `fakes/`, then use it.
```

`AGENTS.md:535-542` carries the same tree in long form ("Is it an external dependency (Drizzle db, Clerk, Stripe SDK)? YES → Use vi.fn() inline object OR vi.mock()"). This branch entered in `792beed7` (2026-02-02), the guidance rewrite whose message says the prior ambiguity "created DEBT-051" — archived [DEBT-051](../_archive/debt/debt-051-controller-tests-use-mocks-not-fakes.md) (inline `vi.fn()` for *internal* dependencies) was resolved separately in `64b86385` the same day. Its sanctioned example is captioned `// ✅ OK - Mocking external Drizzle db object (no fake exists)` (`AGENTS.md:565`). The parenthetical was a condition, never re-evaluated once fakes for those seams appeared.

The split the rule draws is *ownership* (our code vs. external). The split that matters is *what the code under test depends on*: a collaborator's **shape** (a narrow typed stub is fine) versus its **stateful behavior** — idempotency-key replay, status transitions, cursor pagination, constraint errors (a stub cannot express these, and a test built on one proves nothing about them). Drizzle and Stripe are both the second kind. Every incident this month — DEBT-466, DEBT-467, DEBT-470 — was production code depending on Stripe behavior that no inline stub modeled.

`.claude/rules/architecture.md:45` already records the cost in a different voice: "Unit tests with `vi.fn()` mocks will NOT catch this — they have no `this` dependency. See BUG-069, BUG-070." Two live rule files; one prescribes the double the other says caused two production bugs; no cross-reference.

### F2 — The same construct is the canonical BAD mock in ADR-003 and the ✅ OK example in AGENTS.md

[ADR-003](../adr/adr-003-testing-strategy.md) lines 161-165:

```ts
// BAD: Mock (couples test to implementation)
const mockRepo = { findById: vi.fn().mockResolvedValue(question) };
```

`AGENTS.md` lines 565-569:

```ts
// ✅ OK - Mocking external Drizzle db object (no fake exists)
const fakeDb = { query: { users: { findFirst: vi.fn().mockResolvedValue(null) } } };
const repo = new DrizzleUserRepository(fakeDb);
```

Same construct; the variable was renamed from `mockRepo` to `fakeDb`. That rename is the whole confusion, and `.coderabbit.yaml:67` ("`vi.fn()` inside fake objects for spying is CORRECT") encodes the permissive reading, so the only PR-time reviewer enforces the wrong side.

Three incompatible definitions of "fake" exist. ADR-003:146-165 — a class implementing the port with real behavior. `AGENTS.md:588-591` — reusable, "real behavior (filtering, sorting, validation)". Archived [DEBT-035](../_archive/debt/debt-035-inconsistent-repo-test-mocking.md):20-29 — "This **is** a fake passed through dependency injection" (line 25), with the table cell "Plain object with spy methods, passed via constructor" (line 29), quoting `db as unknown as RepoDb` (line 20) as its example. Only DEBT-035 classifies a call-chain object as a fake; archived [DEBT-444](../_archive/debt/debt-444-hot-path-prune-contention-and-coverage.md):17,39 names the identical construct "Drizzle call-chain doubles" — the anti-pattern. Both are archived and therefore invisible to agents. ADR-003:167-179 assigns adapters to integration-only testing ("real database… `tests/integration/`") and has no category for unit-level adapter tests; the 23 repository files and 9 Stripe gateway files that hand-roll doubles sit in that uncategorized layer, among roughly 112 adapter unit-test files in total. ADR-003:396 ("NO excessive mocking — if you need >2 mocks, refactor") is restated in no live file. ADR-003:174 also instructs integration tests to "Mock external providers at the boundary (Stripe/Clerk)", while `docs/dev/testing-infrastructure.md:95` now mandates a required real-Stripe provider contract lane (`tests/e2e/checkout-success-provider.spec.ts`, DEBT-471); the two are reconcilable by lane, but ADR-003 was never amended to say so, leaving agents two incompatible instructions about the same seam.

### F3 — Contract tests exist for 2 of 24 fakes; the revenue-boundary fake cannot diverge detectably

Exactly two maintained fake surfaces have fake↔real contract tests. `tests/shared/subscription-observation-version-contract.ts` runs the same scenarios from `src/application/test-helpers/fakes/fake-subscription-repository-contract.test.ts` (11 lines; never imports Drizzle) and `tests/integration/bug-regression-subscription-observation-version-fence.integration.test.ts:48` (real Postgres), covering 3 of the port's 5 methods. `tests/integration/pending-stripe-customer-cleanup-contract.integration.test.ts:76-80` is the exemplar — `describe.each([['fake', …], ['real Postgres', …]])` over one scenario table — and states the doctrine at line 27: "the same scenario table runs against the fake and the real Postgres adapter and must observe identical outcomes, so a fake-backed unit test can never certify anti-production semantics."

Of the other 22 fakes, 21 have colocated isolation tests only and `FakeSha256Hasher` has no isolation test. `FakePaymentGateway` (`src/application/test-helpers/fakes/fake-gateways.ts:89-190`) is, by Fowler's taxonomy, a spy: it records inputs and returns constructor-supplied constants. It has no behavior, so it is structurally incapable of diverging from `StripePaymentGateway` in any way a test could observe. `FakeStripeCheckoutClient` has no contract test against real Stripe; its replay and pagination semantics rest on prose probe receipts in DEBT-466/467/470 (genuine TEST-mode probes, including a recorded red→green where the probe falsified the fake), but its same-key/different-params `idempotency_error` text is reverse-engineered to match the adapter's own matcher (`src/adapters/gateways/stripe/stripe-checkout-sessions.ts:70-78`) — self-consistency, not provider verification. Archived [DEBT-455](../_archive/debt/debt-455-fake-user-repository-fidelity-divergences.md) rejected "a generalized fake contract harness" as disproportionate. No fake carries a known-divergences note; divergences have been recorded only after someone tripped over one (DEBT-007, DEBT-170, DEBT-367, DEBT-455, BUG-003).

The repository's own vocabulary compounds this: [DEBT-468](./debt-468-test-estate-coverage-and-fixture-debt.md):12 says the fakes "have colocated contract coverage except `FakeSha256Hasher`," where "contract" means an isolation test of the fake. Read as fake↔real verification, that overstates coverage by roughly 22 fakes.

### F4 — 187 hand-rolled Drizzle query-builder chains, including five tautological tests and one real coverage hole

`as unknown as RepoDb` appears at 187 sites across 23 repository test files. `RepoDb` is not a shared type; it is a file-local `ConstructorParameters<typeof X>[0]` re-declared in each file, in `drizzle-stripe-customer-repository.test.ts` eight times inside its own `it()` blocks. No shared chain builder exists; `drizzle-idempotency-key-repository.test.ts` re-declares the same four-line `update→set→where→returning` ladder nine times.

Five tests are tautological. `src/adapters/repositories/drizzle-attempt-repository.test.ts:932-1055` — "supports result filter (correct)", "(incorrect)", "source (adhoc)", "(tutor)", "(exam)" — script `finalQueryExecute` to return the one matching row, then assert that row comes back. The filter argument never reaches any predicate the double evaluates; removing the filter from production leaves all five green. Most of the remainder has real-Postgres behavioral twins in `tests/integration/` (the attempt, user, idempotency-key, and question repositories are near-fully duplicated there). The unit-only value worth preserving is error translation that is hard to force on real Postgres: `23505 → CONFLICT`, the `40P01` deadlock cause, corrupt cached JSON.

One gap is real: `DrizzleStripeEventRepository.pruneProcessedBefore` (`src/adapters/repositories/drizzle-stripe-event-repository.ts:85`) has zero integration coverage — the unit call-chain test is its only coverage anywhere. This is the DEBT-444 class ("a SQL-emission or predicate regression… can remain green while expired rows accumulate"), closed for idempotency keys and still open here.

### F5 — The Stripe doubles return shapes Stripe cannot produce, and that steers production into unexercised branches

Fourteen `as unknown as Stripe*` casts across ten files, plus `stripe-customers.test.ts` (seven casts to the port's `Parameters<…>['stripe']` shape) and one typed-literal double (`stripe-webhook-processor-setup-expiration.test.ts:15`). Verified consequences:

- `src/adapters/gateways/stripe/stripe-checkout-sessions.ts:198` returns `logFallback('provider-list-has-more-is-missing')` unless `page.has_more` is a boolean. Only `stripe-checkout-sessions-recovery.test.ts` and the fake's own contract test supply `has_more`. The DEBT-470 tail-scan success path is therefore exercised only through `FakeStripeCheckoutClient` in `stripe-checkout-sessions-tail-jump.test.ts`; every other hand-rolled double silently routes production into the fallback branch and the test passes anyway.
- `stripe-checkout-sessions-trials.test.ts:79-80` returns `id: 'cs_existing'` from `retrieve` for any id the double has not itself seeded as a setup-mode session. Stripe cannot return an id other than the one requested; production's id-mismatch guards (`stripe-checkout-sessions.ts:729,737`) are never exercised by that file.
- `stripe-checkout-sessions-trials.test.ts:456-459` asserts `{ sessionId: 'cs_new', url: … }` — the double's own defaults, passed through `stripe-checkout-sessions.ts:359` unchanged. The assertion cannot fail.
- `subscriptions.retrieve: vi.fn(async () => ({}))` appears in seven of the nine files (eight sites) and in the fake itself (`fake-stripe-checkout-client.ts:165`, now `:167`). Stripe never returns `{}`.
- `stripe-webhook-processor.test.ts:77-79` — `constructEvent` ignores body, signature, and secret, so signature verification is not modeled at unit level (the real-signature integration matrix from DEBT-468 item 2 is the only proof).

`FakeStripeCheckoutClient` currently has two consumers (**2026-09-24 UTC correction:** five unit consumers — `stripe-customers`, `stripe-portal`, `-tail-jump`, `-status-guard` and `-setup-live-retrieve` — plus its own isolation test and the shared contract runner; the eight step-5 files still build inline `StripeClient` stubs, none imports the fake, and only `-live-retrieve` needs no extension). **2026-09-24 UTC census update:** after the live-retrieve migration seven files remained and the fake had six unit consumers; after the recovery migration six remained (`-trials`, `-trial-recovery`, `-reconciliation`, `stripe-checkout-sessions.test.ts`, `-concurrency`, `stripe-webhook-processor.test.ts`) and the fake had seven; after the webhook-processor migration no step-5 file remains and the fake has thirteen unit consumers (the eight step-5 suites plus `stripe-customers`, `stripe-portal`, `-tail-jump`, `-status-guard` and `-setup-live-retrieve`), besides its own isolation and contract tests (#1082 review correction). The extensions that would unblock the rest, in dependency order (**2026-09-24 UTC status:** the `subscriptions` seeder has landed with the contract's fifth scenario; create and expire faults, a list hook, webhook-event injection (`setWebhookEvent`) and a SetupIntent seeder (`seedSetupIntent`) exist as uncontracted test seams; `customers.search`, caller-chosen session ids and list fault injection have not landed): a `subscriptions` seeder (`list` data + `retrieve` payload); create/expire/list fault injection; seeding sessions with caller-chosen ids; `webhooks.constructEvent`, `setupIntents`, `customers.search`; an await barrier in `list` (only `stripe-checkout-sessions-concurrency.test.ts` needs it). `stripe-checkout-sessions-live-retrieve.test.ts` needs none of these and can migrate today. `stripe-portal.test.ts` and `stripe-customers.test.ts` cast to the same `StripeClient` port the fake implements.

### F6 — Audited at `dev` `01fd55b8`: seventeen own-code `vi.mock()` calls survived in `app/` and `components/`; zero in `src/`

At the Part A audit baseline, the then-live rule "NEVER `vi.mock()` for our own code" (`AGENTS.md:76,544`; `.claude/rules/testing.md:21`) was violated by 17 factory-form calls in 12 files, all under `app/` or `components/`, none under `src/` or `lib/`. No estate-wide sweep of this rule had run: DEBT-050 added fakes, and DEBT-368's recorded grep targeted `**/*.browser.spec.tsx` only, so jsdom `*.test.tsx` and route tests were never checked. Five mocked `@/lib/report-client-error` while bypassing the sanctioned `tests/test-helpers/report-client-error-mocks.ts`. Four in `app/api/cron/reconcile-stripe-subscriptions/route.test.ts` carried a written justification, two of them preserving real exports via `importOriginal`. Eight stubbed the unit-under-test's own collaborators: `app/(app)/app/practice/practice-page-client.test.tsx:9,18` mocked both `./components` and `./hooks/use-practice-session-controls`, so it could only assert wiring against its own fixtures; also `history-sessions-tab.test.tsx:57`, `quick-practice-client.test.tsx:26`, `app/layout.test.tsx:25,39`, `layout-shell.test.tsx:19`, `components/marketing/marketing-layout.test.tsx:21`. At that baseline, `docs/dev/react-vitest-testing.md:254` still presented factory-form `vi.mock` as "the current repo pattern," the form DEBT-368 drove from 26 to 0.

### F7 — Audited at `dev` `01fd55b8`: no executable enforcement existed for any test-double rule

At the Part A audit baseline, source-scan tests existed for architecture boundaries, design tokens, fixture UUID shape, Playwright lane policy, and CI workflow shape, and a CI step enforced the E2E skip policy (`.github/workflows/ci.yml:91`). None enforced fakes-over-mocks, own-code module factories, `as unknown as` in tests, or the ">2 mocks" rule. The only mechanism that mentioned doubles was `.coderabbit.yaml` prose — advisory, and encoding the permissive DEBT-035 definition (F2). DEBT-368's gate was a recorded one-time, line-oriented grep; `app/(app)/app/practice/quick/quick-practice-client.browser.spec.tsx:24` carried `// biome-ignore format: keep { spy: true } on this line for the DEBT-368 verification grep` — code shaped to satisfy the checker — while `app/(app)/app/practice/hooks/use-practice-question-feedback.browser.spec.tsx:9-11` and `app/(app)/app/questions/[slug]/hooks/use-question-page-feedback.browser.spec.tsx:8-10` formatted the option across lines and would false-positive.

Resolutions without guards do not hold. Archived [DEBT-271](../_archive/debt/debt-271-structural-ast-test-brittleness.md) (2026-03-03) deleted `collectColumnNamesForTable` and its structural assertions in `576b81c4`, `2d575748`, and `fcd866e3`, and verified them gone. `b420f147` ("Fix BUG-195 through BUG-198", 14:04 the same day) re-introduced the helper deliberately — its message records fixing BUG-195 "with a structural unit test" — but without reference to DEBT-271; `de29bf32` (BUG-257, 2026-06-23) added four more call sites, and `22a24ad2` ([DEBT-429](../_archive/debt/debt-429-duplicated-question-state-mapper-and-test-helpers.md), 2026-07-04) consolidated rather than removed it. It now lives at `src/adapters/repositories/repository-test-helpers.ts:51` with seven call sites and in-file `// Structural assertion:` comments at `drizzle-question-repository.test.ts:415,467`. Each step was locally reasonable; the resolution simply had nothing holding it.

### F8 — The remaining `as unknown as` census, and one prototype-dropping spread

> **2026-09-21 arithmetic and coverage correction:** the historical list below totals **23** hand-rolled doubles (12 + 2 + 7 + 1 + 1), not 25; with the five type lies, **28**, not thirty. The three pre-existing rate-limiter integration cases do not replace every unit below: automatic cleanup, invalid inputs and cleanup-failure behavior need additional evidence. Published WIP `9446b459` supplies 14 such cases, now independently green and mutation-proven on current Postgres, but is **not landed**. Its candidate 228-cast floor is not the live 240 floor. Refresh its stale aggregate counts before a separate PR. [Inventory and per-behavior proof](./assets/inventory-2026-09-21/verification.md#published-rate-limiter-wip--adapt-do-not-land-in-this-pass).

325 `as unknown as` sites exist across 60 test files (no multiline form), including 28 in the root-level `proxy.test.ts` and 3 in `db/schema.test.ts` (both outside the census's classified set and still to be dispositioned). Beyond F4, F5, and those two files: 19 are `typeof import(...)` type annotations for `beforeAll` dynamic imports (benign); 29 are narrow shapes (container placeholders, `NodeJS.ProcessEnv`, `setTimeout` handles, `HTMLElement`, tracked thenables); 11 are intentional-invalid inputs for negative tests; 4 are type plumbing. Thirty warrant fixing:

- 25 hand-rolled doubles outside F4/F5: `src/adapters/gateways/drizzle-rate-limiter.test.ts` (12 sites; redundant — `tests/integration/rate-limiter.integration.test.ts` proves the same behavior on real Postgres), `stripe-portal.test.ts` (2), `stripe-customers.test.ts` (7, re-implementing the SDK's `_makeRequest` pagination plumbing), `scripts/export-question-feedback.test.ts:412`, `lib/container.test.ts:669`.
- 5 type lies. `tests/integration/controllers.integration.test.ts:619` spreads a `DrizzlePracticeSessionRepository` instance to override `recordQuestionAnswer`; object spread copies own enumerable fields only, so the result has no prototype methods at all. It passes solely because `src/application/use-cases/submit-answer.ts:224` is the only `tx.sessions.*` call inside that transaction. `lib/container.test.ts:528,531,560` reach private fields through invented public shapes. `fake-practice-session-repository.test.ts:22` defeats `private` to reset state.

### What the census did not find

The estate is otherwise clean, and this matters for the diagnosis: zero `expect(true)`-style or same-variable tautologies, zero snapshots, zero `as any`, zero `@ts-ignore` or `eslint-disable`, zero `.only`, zero swallowed errors in test bodies, zero always-true skip guards. Thirteen of the 14 `@ts-expect-error` directives are negative type tests (the other, `app/api/webhooks/clerk/route.test.ts:99`, is a documented third-party type-compatibility suppression); all skips are env-driven. The doubles are rule-compliant work, not gate evasion.

## Impact

- **Silent coverage loss at the money seam.** F5's `has_more` omission means the DEBT-470 tail jump — the structural fix for a production-visible saturation defect — has one test file that exercises its success path. The other eight Stripe suites pass while running the fallback.
- **Unfalsifiable tests.** F4's five filter tests and F5's pass-through assertions cannot fail. They add to the count and the coverage percentage while proving nothing, which is the same defect class CodeRabbit caught in `persisted.plan` on PR #816.
- **Undetectable drift at the revenue boundary.** `FakePaymentGateway` is a spy. When `StripePaymentGateway` changes, nothing in the suite can notice.
- **Rule-driven recurrence.** Any agent following `testing.md:27` as written will produce more of F4/F5. DEBT-051, DEBT-109, and DEBT-357 each re-litigated the same question by hand because the rule cannot be checked.
- **False confidence in the register.** "Colocated contract coverage" reads as fake↔real verification; it is not.

## Resolution

Ordered so the rule is fixed before any migration, and so each deletion gets a guard.

1. **[x] Correct the rule text (Part A, 2026-08-23).** `.claude/rules/testing.md` now owns the one Meszaros/Fowler vocabulary and shape-vs-behavior decision; `AGENTS.md`, `.coderabbit.yaml`, and `docs/dev/react-vitest-testing.md` reference it instead of duplicating it. The `fakeDb` sanction is gone. The canonical rule shows a narrowly typed shape-only adapter stub without `as unknown as`, requires a maintained fake + fake↔real contract coverage for behavior seams, names adapter-owned `FakeStripeCheckoutClient`, restores ADR-003's ">2 doubles → refactor" and real-infrastructure requirements, and cross-references `.claude/rules/architecture.md:45`. ADR-003 has a dated amendment for error-translation-only adapter units and the required provider-contract E2E category.
2. **[x] Add the enforcement scan before any migration (Part A, 2026-08-23; placement replaced under DEBT-475 on 2026-08-29).** `tests/test-double-fidelity-source-scan.ts` parses TypeScript syntax, while the dash-split `tests/test-double-fidelity-port-double-scan.ts` follows contextual port types back through inferred object variables and helper returns; `tests/test-double-fidelity-live-scan.ts` executes the full-estate scan outside the parent V8 coverage collector and fails closed through `pnpm lint:doubles`; and `tests/test-double-fidelity-ratchet-floors.ts` records growth-only per-file floors. Local `pnpm lint` and required CI invoke the command, with CI running it before unit coverage. The former bounded Vitest `globalSetup` provider and its three injected unit assertions were removed only after command, CI-order, config-absence, and forbidden-fixture contracts failed red; a real temporary forbidden fixture then made `pnpm lint:doubles` exit 1. The three-run `pnpm test --run` median moved from 38.07 to 29.43 seconds without changing a scanner policy or floor. This replaces the original setup seam whose coverage-mode and hosted-runner contention required first 14- and then 30-second child bounds; those bounds disappeared with the setup subprocess rather than being weakened. Within the scanner's original `*.test.*` / `*.spec.*` scope, the live floors are 22 own-code module factories / 13 files (17 `vi.mock` plus 5 `vi.doMock`), 289 non-allowlisted unknown-double casts / 50 files, and 45 object-literal doubles for maintained fake ports / 18 files. The raw cast census remains 325 / 60; 36 sites match six documented shape-only or intentional-invalid categories. The fake-port set is derived from the application barrel plus `FakeStripeCheckoutClient`, so new implemented fake exports enter the scan automatically. Function-valued `vi.mock`/`vi.doMock` factories count whether inline or referenced through a local variable, alias, function declaration, or the latest assignment across the binding's enclosing execution scopes before the call; later assignments and assignments in unrelated nested execution scopes do not affect classification. Lexical parameter shadowing, unresolved bindings, and alias cycles stop resolution. Automock and `{ spy: true }` forms do not count. Multiline `{ spy: true }` is parsed correctly and the DEBT-368 format suppression is deleted.
3. **[x] Contract-test mandate (Part B, 2026-08-23; hosted activation 2026-08-27).** `docs/dev/test-double-contract-register.md` is the executable 24-surface census: every maintained behavior fake has either a named fake↔real scenario file or a dated written waiver, and every row states known divergences. `FakePaymentGateway` keeps its truthful option-(b) classification as a canned-answer spy; the waiver names the adapter, signed-webhook, required provider-E2E, and Stripe-client contracts that replace a meaningless constructor-constant parity table. `FakeStripeCheckoutClient` now runs the same four cases against the fake and Stripe TEST mode for frozen replay/live retrieval, reverse-chronological cursor pagination with `has_more`, terminal visibility, and same-key/different-parameter `idempotency_error`; the mismatch case also verifies the adapter-consumed `same parameters` provider message phrase, eliminating the prior text self-consistency. The real half is opt-in and required by the scheduled Stripe runner under a 20-second per-case and five-minute process-tree bound. Hosted run `33038731445` at `main` head `3162a7be91e57eb5c66f0575d675414c91646991` reported `PASS executed=6 passed=6 skipped=0`, and the step-1 gate ancestry check from `934e57a319ae45e0aec807a4e903368f13c364af` exited 0. The shared subscription table now names and executes all five port methods against both fake and Drizzle. The register enumerates the exact 17 repository fakes covered by ADR-003's validation license. DEBT-455's universal-harness rejection remains correct: unrelated ports keep port-owned scenarios, while an AST-backed census guard—not a cross-port assertion DSL—makes every contract or waiver explicit and self-updating. Any behavior change invalidates its dated waiver.
4. **[x] Census dispositions (completed 2026-09-25 UTC; see the dated status below).** Delete F4's five tautological tests. Add the `pruneProcessedBefore` integration case (and `peek` for the Stripe event repository). Migrate the 23 F8 doubles (arithmetic corrected 2026-09-21 above) and the F6 `vi.mock` sites; fix the five type lies (`controllers.integration.test.ts:619` becomes a subclass overriding `recordQuestionAnswer`). For the 23 `RepoDb` files, keep only error-translation cases that integration cannot force; delete chain-shape assertions that have a real-Postgres twin; move the rest to integration. **Dated scope addition (2026-08-24, DEBT-475 F5):** the scanner's original `*.test.*` / `*.spec.*` walk omitted `app/(app)/app/practice/[sessionId]/hooks/practice-session-page-model.browser.setup.ts`; its three own-code factory-form `vi.mock` sites join this step's migration set before the widened scan lands. They are not added to or allowed by the 22 / 13 floor.
   **Status (2026-09-24 UTC):** [x] F4's five tautological tests are deleted and `pruneProcessedBefore`/`peek` are twinned (attempt and Stripe-event dispositions, 2026-09-19). [x] The 23 F8 doubles and the five type lies are dispositioned in the 2026-09-23 sections above (rate limiter, portal, customers, feedback export, container wiring and transaction, private-state fixture, controller subclass), except the three `stripe-customers.test.ts` Search-behavior cases whose replacement remains an unlanded candidate. [x] The F6 own-code `vi.mock` sites and the three browser-setup sites are retired or on the sanctioned `{ spy: true }` form; the own-code factory floor is empty. [x] The 23 `RepoDb` files are closed by the repository dispositions above, from the user repository through the last chain doubles: every retained unit case is error translation on the shared `drizzle.mock` boundary, `RepoDb` casts are 0 and no repository chain double remains. **Open:** the three Search-behavior cases, and the `proxy.test.ts` (28 sites) and `db/schema.test.ts` (3 sites) casts that F8 left unclassified; both are the whole remainder of this step. **2026-09-25 UTC:** [x] the `proxy.test.ts` casts are gone (see [the proxy disposition](#proxy-real-request-disposition-2026-09-25-utc)). [x] the `db/schema.test.ts` casts are gone (see [the schema disposition](#schema-public-config-disposition-2026-09-25-utc)). Only the three Search-behavior cases remain. [x] the three Search-behavior cases are on the fake, whose Search is contracted against Stripe TEST mode (see [the customer Search disposition](#customer-search-contract-disposition-2026-09-25-utc)). **Step 4 is complete (2026-09-25 UTC).**
5. **[x] Re-scope DEBT-468 Parts 2–4 (completed 2026-09-24 UTC; see the dated status below)** onto F5's fake-extension order, migrating `stripe-checkout-sessions-live-retrieve.test.ts` first as the zero-cost proof, then `-recovery`, `-trials`, `-trial-recovery`, `-reconciliation`, `stripe-checkout-sessions.test.ts`, `-concurrency`, and `stripe-webhook-processor.test.ts` last.
   **Status (2026-09-24 UTC):** [x] `stripe-checkout-sessions-live-retrieve.test.ts` is on `FakeStripeCheckoutClient` with no fake extension (the zero-cost proof; see [the live-retrieve disposition](#live-retrieve-fake-migration-disposition-2026-09-24-utc)). [x] `-recovery` is on the fake with one test seam, the create fault (see [the recovery disposition](#recovery-fake-migration-disposition-2026-09-24-utc)). [x] `-trials` is on the fake with two recording seams (see [the trials disposition](#trials-fake-migration-disposition-2026-09-24-utc)). [x] `-trial-recovery` is on the fake with no new seam (see [the trial-recovery disposition](#trial-recovery-fake-migration-disposition-2026-09-24-utc)). [x] `-reconciliation` is on the fake with the expire-fault seam (see [the reconciliation disposition](#reconciliation-fake-migration-disposition-2026-09-24-utc)). [x] `stripe-checkout-sessions.test.ts` is on the fake with contracted seeded Subscriptions (see [the base checkout disposition](#base-checkout-fake-migration-disposition-2026-09-24-utc)). [x] `-concurrency` is on the fake with the list hook (see [the concurrency disposition](#concurrency-fake-migration-disposition-2026-09-24-utc)). [x] `stripe-webhook-processor.test.ts` is on the fake with an injected event and seeded SetupIntents (see [the webhook-processor disposition](#webhook-processor-fake-migration-disposition-2026-09-24-utc)). **All eight files are on the fake; the migration order is complete (2026-09-24 UTC).** Historical note (#1082 review correction): only the seeded Subscriptions landed as modelled behavior with a fake↔real contract scenario (the fifth). The create and expire faults, the list hook, the recording seams, webhook-event injection and the SetupIntent seeder landed as uncontracted test seams, documented in the register. Caller-chosen session ids and `customers.search` were not needed for step 5 and did not land; step 4 keeps the three Search cases open.
6. **[x] Record the process finding (2026-09-24 UTC).** The deletion/guard convention already exists in `docs/debt/index.md` § Resolution Evidence Conventions (line 1037 at this head; the line moves as the index grows), so nothing is added there; the stale `:901` citation in the re-audit table is corrected. The DEBT-035 versus DEBT-444 adjudication now lives in the canonical rule, `.claude/rules/testing.md` (Test-Double Fidelity), with a dated ADR-003 amendment and a supersession pointer on the archived DEBT-035 record; see [the step 6 disposition](#process-finding-and-call-chain-adjudication-disposition-2026-09-24-utc).

Part A's TDD receipt is explicit: the new suite first failed because its scanner module did not exist, then all four required synthetic checks failed independently against compile-only collectors (4/4 red). Two later red cases proved inferred helper returns and object variables initially escaped the maintained-port scan; the corrected dataflow found three additional existing offenders and raised the measured floor from the preliminary 42 / 15 to 45 / 18. Review then produced four more red cases (4 failed / 21 total) for automock and spy-config false positives, the `vi.doMock` bypass, and substring-based additional-fake discovery. Exact-head CI supplied the timeout red; the bound contract failed 1/1, then the injectable child-process contract failed 1/2 before both passed. Coverage itself supplied another red at 219 / 254 changed executable lines (86.22%); repository-reader, fail-closed, AST, origin-tracing, and sorting cases raised that upload-shaped result to 241 / 248 (97.17%), above the 95.14% Codecov patch target without an ignore. A later exact-head review exposed identifier-referenced factories: the local-variable `vi.mock` and function-declaration `vi.doMock` probes first failed 2/2, and the first resolver then failed a parameter-shadow case 1/26. The lexical resolver closed those bypasses without changing the live 22 / 13 floor. A subsequent exact-head review exposed assignment-referenced factories and the `page as never` escape hatch in the new E2E-helper regression: the assigned-`vi.doMock` probe failed 1/29, and removing all five casts made `pnpm typecheck` fail at each call. The resolver followed the latest preceding assignment in the same execution scope, while `startSession` accepted an exported narrow page/locator seam implemented by both Playwright `Page` and the fake. The next exact-head review exposed the remaining outer-scope case: a module factory assigned before `beforeEach` and consumed inside its callback failed 1/34. The resolver now walks from the declaration's execution scope inward to the reference callback, retaining the latest preceding assignment while pruning unrelated nested functions. The three focused scanner/global-setup suites pass 49/49 at 307 / 315 executable lines (97.46%); the primary source scan measures 99.00% line coverage. The mandatory gate also caught a retry-recovered `startSession` no-op: the helper redundantly reselected an already-active mode, rotating the request key before the subsequent Start click. A fake-page regression reproduced the exact unchanged-URL failure 1/5 before the helper skipped that no-op reselection; it then passed 5/5, and the failed E2E case passed 2/2 with Playwright retries disabled. No double was migrated, no existing test was deleted, and `FakePaymentGateway` was not touched. This document remains **Active** for steps 3–6.

Part B's TDD receipt is likewise explicit. The fake Checkout invocation first failed because the shared contract module did not exist; its four fake scenarios then passed. The scheduled-runner test failed because the Checkout contract file and validated Price forwarding were absent before the bounded runner required both files and all six cases. A TEST-mode live run passed the four provider cases in 11.1 seconds, including Stripe's real `StripeIdempotencyError` / `idempotency_error` / 400 shape and `same parameters` message phrase. The first combined run then caught the card-present trial-clock case at 10.28 seconds against the inherited 10-second integration timeout; the invocation contract failed red before a 20-second scheduled-only CLI bound preserved the ordinary lane's global policy. The next combined run exited green but the outer proof failed because Vitest's JSON reporter truncated object-parameterized titles; literal per-scenario registration made all four names reporter-stable, and a hermetic registration probe now pins them. The exact fail-closed wrapper then passed 6/6 and cleanup left zero matching recent fixture customers or clocks. The subscription coverage test first received `undefined`; the shared table then widened from three to all five port methods, with a compile-time omission check, and the resolver-aware integration lane passed 258 tests with six expected provider skips. The register suite first failed for a missing source module, then for the missing live register, then for the missing canonical-rule link; it now derives and verifies exactly 24 rows. The three combined Part-A edge fixtures identified in PR #823 are split into six single-behavior cases without changing the scanner. No hand-rolled double was migrated, no existing test was deleted, and no ratchet floor changed. This document remains **Active** for steps 4–6.

**Not in scope.** Coverage thresholds or any metric gate (ADR-019 requires a new ADR). Rewriting the domain or application test layers, which the census found exemplary. The Clerk boundary, which DEBT-468 correctly records as already fake-based.

## Verification

- F1/F2: the live rules contain no own-vs-external branch; one definition of fake/stub/spy/contract test appears in `.claude/rules/testing.md` and is referenced, not duplicated, by `AGENTS.md` and `.coderabbit.yaml`; ADR-003 names the unit-adapter and provider-contract categories.
- F3: every fake in the barrel and in `src/adapters/**/test-helpers/` is listed in a contract-test census with a fake↔real scenario file or a dated written waiver; `FakePaymentGateway` is no longer a pure spy or has a recorded waiver; the Stripe executable contract exists and is credential-gated.
- F4: the five named tests at `drizzle-attempt-repository.test.ts:932-1055` are gone; `pruneProcessedBefore` and `peek` have integration cases; the `RepoDb` count is below the ratchet floor and falling.
- F5: `has_more` and `created` are supplied by every remaining Stripe double or the double is gone; the tail-scan success path is exercised by more than one file; `subscriptions.retrieve` never returns `{}` in any double or fake.
- F6/F7: the scan exists, runs through `pnpm lint:doubles` from local lint and required CI, and fails on synthetic cases for own-code factory-form `vi.mock` and `vi.doMock`, an `as unknown as` outside the allowlist, a hand-rolled object shape for a port that has a barrel fake or `FakeStripeCheckoutClient`, and any growth of a counted pattern above its recorded ratchet floor; the DEBT-368 format comment is deleted; the scan parses the multiline `{ spy: true }` form without classifying it or automock as a factory.
- F8: `controllers.integration.test.ts:619` uses a subclass; `container.test.ts` asserts behavior rather than private fields.
- Whole-debt: the number of `as unknown as` sites in test files is recorded as a ratchet and cannot grow in CI.

## Related

- Rule origin: `792beed7` (2026-02-02); live rule files `AGENTS.md`, `.claude/rules/testing.md`, `.claude/rules/architecture.md`, `.coderabbit.yaml`
- Prior adjudications, all archived: [DEBT-035](../_archive/debt/debt-035-inconsistent-repo-test-mocking.md) (ruled the `RepoDb` pattern a fake), [DEBT-051](../_archive/debt/debt-051-controller-tests-use-mocks-not-fakes.md), [DEBT-109](../_archive/debt/debt-109-inline-vi-fn-logger-mocks.md), [DEBT-271](../_archive/debt/debt-271-structural-ast-test-brittleness.md) (reversed same day), [DEBT-357](../_archive/debt/debt-357-test-double-discipline-drift.md), [DEBT-368](../_archive/debt/debt-368-browser-spec-vi-mock-missing-spy-true.md), [DEBT-444](../_archive/debt/debt-444-hot-path-prune-contention-and-coverage.md) (named the anti-pattern), [DEBT-455](../_archive/debt/debt-455-fake-user-repository-fidelity-divergences.md) (rejected a general harness), [DEBT-007](../_archive/debt/debt-007-fake-repos-no-validation.md) (licensed permissive fakes)
- Open work this re-scopes: [DEBT-468](./debt-468-test-estate-coverage-and-fixture-debt.md) Parts 2–4; coordinates with [DEBT-469](./debt-469-toolchain-warning-debt.md) splits and [DEBT-465](./debt-465-test-quality-practices-adoption.md) Part 2 (mutation testing will expose the unfalsifiable tests in F4/F5 directly)
- Follow-on: [DEBT-473](../_archive/debt/debt-473-green-without-evidence.md) files the suite-level fail-open in both scheduled provider suites, the grep-only skip policy noted under F4, and the missing hosted `executed=6` receipt for the Part B runner; its F9 extends step 6's convention.
- Exemplars to copy: `tests/integration/pending-stripe-customer-cleanup-contract.integration.test.ts`, `tests/shared/subscription-observation-version-contract.ts`, `tests/architecture-boundary-source-scan.ts`
- Canon: [Fowler, *Mocks Aren't Stubs*](https://martinfowler.com/articles/mocksArentStubs.html) (Meszaros taxonomy; classical vs. mockist coupling); [*Software Engineering at Google*, ch. 13 "Test Doubles"](https://abseil.io/resources/swe-book/html/ch13.html) ("Fakes Should Be Tested"; fidelity); [Google Testing Blog, *Don't Mock Types You Don't Own*](https://testing.googleblog.com/2020/07/testing-on-toilet-dont-mock-types-you.html) (2020-07-16: wrap the third-party type, double the wrapper — the `StripeClient` port is that wrapper, and these doubles stub it instead of faking it)
