# Adversarial review — DEBT-469/468 debt-paydown campaign (#1111–#1130)

**Date:** 2026-09-26 UTC. **Reviewed tree:** `main` and `dev` at `656d98ad` / `906c9279`, both tree `34ab857184cd9f0c48af166f52d0e3ec5f6e305d`.

**Scope:** the campaign's own report (ten feature PRs and ten promotions), the live [DEBT-468](../../debt-468-test-estate-coverage-and-fixture-debt.md) and [DEBT-469](../../debt-469-toolchain-warning-debt.md) records, and the [debt index](../../index.md).

**Method:** every claim was treated as unproven and re-derived from four sources: GitHub (reviews, threads, body-edit history, runs, attempts, job logs), the Vercel v13 deployment API, git, and local re-runs in this clone. Local re-runs used the unit, browser and integration lanes, coverage, mutation and `tsc`. The campaign's proof logs were used only as leads. Every mutation was reverted byte-identically, and `git diff --exit-code` was checked after each run. Severity runs from P0 (production or security harm) to P3 (record accuracy or hygiene). No P0 or P1 finding survived.

## Findings, most severe first

| ID | Claim | Verdict | Sev | Evidence | Fix |
| --- | --- | --- | --- | --- | --- |
| F1 | The last suppression, the lock-order suite, is blocked until its hand-built Stripe literal migrates to `FakeStripeCheckoutClient`. | **REFUTED** (reasoning) | P2 | Harness lines 40–628 were extracted into a scratch module, with the cases importing it, and run three times through `pnpm test:integration` with `--coverage.include` on the module. Each run gave **129/153** statements and **48/61** functions, with the same misses every time. The 24 unrun statements split four ways. **9** are the literal's stubs (lines 287–315). **11** are fixture and failure-only guards (93, 190, 252–258, 415, 447, 494–495). **2** are callbacks the writers never invoke (467 `now`, 530 `getClerkUserById`). **2** are lock-state branches (199, 240), which did not vary across the runs. None of the 6 partial branch sites (92, 189, 199, 240, 251, 253) is in the literal. The migration retires 9/24 of the statements and 0/6 of the partial sites. Scratch files were deleted. | DEBT-469 correction paragraph and index pointer. |
| F2 | The DEBT-472 ratchet floors are sound after the campaign's two edits. | **REFUTED** (new defect) | P2 | `HAND_ROLLED_PORT_DOUBLE_FLOORS` kept `drizzle-practice-session-repository-corrupt-list-reads.test.ts` at **1**, but the live count has been **0** since `a4eaf21e` (2026-09-24). Red: a regrown `Logger` literal in that suite still gave `PASS test-double-fidelity issues=0`. With the entry removed, the same literal fails with "above its ratchet floor of 0". The campaign edited this map twice (#1119, #1129) without noticing. | `3c7e14de`. Floors now equal the live counts: module mocks 0/0, casts 25 sites / 10 files, port doubles 31 / 12. |
| F3 | Each promotion merged only after `git fetch` + `verify-promotion` passed in the same chain, and its body carries the output. | **PARTIAL** | P2 | The gate held: all ten `verify-11NN.log` files in the campaign's session scratch directory were written **2–3 s before** each merge, and each ends with the tool's success sentence. But GitHub's body-edit history shows the verify block first appearing **9–14 s after** each `mergedAt`. The bodies had promised it would be "appended below once promotion CI is green". AGENTS.md orders the body edit (step 1) before the merge (step 4). GitHub alone therefore cannot show verify-before-merge, and the only proof sits in an ephemeral local directory. | Not retroactively fixable. Recommendation: `verify > log && gh pr edit --body-file … && gh pr merge --match-head-commit …`. |
| F4 | `question-page-client-test-helpers.test.ts` is justified. | **REFUTED** (coverage theater) | P3 | It asserted only that four no-op callbacks return `undefined`, a property no suite relies on. Without it the helper was 4/8 statements and 2/6 functions (measured with the three QuestionView suites alone). | `45d89384`: the callbacks become `vi.fn()` stubs (the sibling review-stage helper's idiom) and the test is removed. The helper is 4/4 statements and 2/2 functions, and 49/49 cases pass. |
| F5 | The two `createQuestionOutput` copies stay below the rule of three. | **PARTIAL** | P3 | The copy is verbatim, which is right for a move. But the same directory already builds `GetQuestionBySlugOutput` fixtures in the bookmarks, feedback and previous-attempt hook specs, so these copies are the fourth and fifth. | Inline DEBT-469 correction; the family belongs with DEBT-468 Part 3 (e). |
| F6 | DEBT-469's post-split line counts are accurate. | **REFUTED** (four) | P3 | `wc -l` at the merge commits and on `main` gives 426/636 for the two checkout suites (doc: 425/635), and 472/525 for the two pricing suites (doc: 471/526). The PR bodies had three more wrong counts: #1111's 545/633/435 against 543/632/442. | Inline DEBT-469 corrections. PR bodies are left as history. |
| F7 | cached-reads rose from 19/37 statements and 0/2 branches to 37/37 and 2/2. | **PARTIAL** | P3 | The statement counts reproduce. Vitest's v8 provider counts branch arms: **1/4** before (the filing's 25%) and **4/4** now. "0/2 → 2/2" counted `if` sites taken both ways. | Inline DEBT-468 correction. |
| F8 | `lib/container-practice-session-state-transactions.test.ts` (2026-07-21) refutes the composition-wiring gap. | **CONFIRMED**, wrong date | P3 | The suite was added on 2026-07-02 (`9711ca48`), and the cited cases landed between 07-02 and 07-13; 07-21 is a later edit. Mutations in `lib/container/use-cases.ts`: dropping 40P01 from the retry allowlist fails 3 cases, and disabling cancellation classification fails 2. | Inline DEBT-468 correction. |
| F9 | Making `paymentMethods`, `detach` and `subscriptions.update` required yields exactly four TS2741 errors in three integration literals. | **PARTIAL** | P3 | The trial as stated reproduces exactly four errors, in `bug-regression-subscription-observation-version-fence`, `renewal-consent-records` (×2) and `stripe-subscription-writer-lock-order`. But guard 3 reads `subscriptions?.update`, so retiring it needs `subscriptions` required too. That adds a fifth error, `src/adapters/jobs/reconcile-stripe-subscriptions.test.ts:139`, whose intentional-invalid case removes `subscriptions`. The expanded-customer branch is forced by the port's `string \| { id } \| null`, which mirrors the SDK (**CONFIRMED**). | Inline DEBT-468 correction. |
| F10 | "A Playwright mobile project would run the whole E2E suite a second time"; the sheet adds "roughly double billed E2E minutes". | **PARTIAL** | P3 | That holds only for an unscoped project. In the hosted `test` job, the E2E step took 271, 196 and 201 s of 850, 684 and 686 s on runs 36205706009, 36206671836 and 36207474184 (≈29–32%). E2E minutes would double, but the billed job grows about 30%. | Inline DEBT-468 correction. It stays an owner decision. |
| F11 | DEBT-468 Part 3's live counts are current, and the three new `*-test-helpers.ts` modules respect its consistency note. | **REFUTED** | P3 | The text read "still records 45 sites", but the ratchet holds 31 sites across 12 files. The note says "ten" helper files outside `test-helpers/`, but there are **11**, with #1119, #1121 and #1127 adding three. Adding them contradicts the note's letter. But folding them without the `-test-helpers` suffix would drop them out of the ratchet, which selects helper files by suffix, and out of Biome's filename-based raw-`<button>` exclusion under `app/` and `components/`. | Live text rewritten, with the suffix guidance. |
| F12 | The ratchet file's dated notes describe the maps they sit above. | **REFUTED** | P3 | The pricing note (a port-double change) and the datetime note (a cast change) both sat above `OWN_CODE_MODULE_MOCK_FLOORS`. | `8f91c160` moves them unchanged. |
| F13 | The #1130 receipt (claim 22). | **CONFIRMED**, receipt missing | P3 | Every claimed identifier and time matches GitHub and Vercel v13 (table below). Two items were operator observations that cannot be re-derived: the STAGED window start and health at 01:22:16Z. | Receipt written after DEBT-469's scanner paragraph. It uses Vercel's own status times and a fresh 17:47:10Z health check. |
| F14 | The debt index is current. | **REFUTED** | P3 | `**Last Updated:** 2026-09-23 UTC` was maintained daily until 09-23, then went stale through three days of edits. The Latest stanza still ended "hosted CI, exact-head approval, merge and promotion remain pending" for #1129. | Updated; new Latest stanza, prior one demoted. |
| F15 | Each feature PR was merged with `scripts/merge-reviewed-pr.ts <PR> --merge`. | **UNVERIFIABLE**, consistent | P3 | Every precondition held on each final head: exact-head APPROVED, zero unresolved threads, green `test` and `codecov/patch`, a merge commit, and no commit after approval. The tool prints its receipt only to stdout, and none was posted or saved, though AGENTS.md says to keep it with the PR. The docs' release receipts do carry the approval ID and head. | Recommendation: post the tool's receipt as a PR comment. |
| F16 | Removing the datetime contract's cast floor after moving the casts out of scan scope is legitimate. | **CONFIRMED**, with caveat | P3 | The two `as unknown as` are Zod-reflection reads, not test doubles, so they are outside the ratchet's threat model, and the header permits removing entries. They are, however, the only such casts in any non-test `tests/**` module, which sets a precedent. The pricing re-key (6 → 3 + 3) is legitimate: the same sites and total moved with their cases, and the scanner tallies 3 + 3. | Recommendation: if the scanner is touched again, read Zod 4's typed `def`/`instanceof` API instead of widening the scan. |
| F17 | (observation) The ratchet scans every test helper. | **LATENT GAP** | P3 | 39 `.ts(x)` files inside `test-helpers/` directories lack the `-test-helpers` suffix, so `readTestSources()` never reads them. None contains `as unknown as` today. | Report only; Guard and Scanner Review Discipline requires an owner decision first. |
| F18 | Promotion findings were adjudicated before merge. | **CONFIRMED**, noted | P3 | #1122 and #1126 merged 14–24 s after CodeRabbit approved, while its latest reply still said the finding stayed open pending a follow-up link. The authors had replied with a deferral and resolved the threads. The fixes landed in #1123 (`a6bdcb05`) and #1127 (`cdb95cb7`) within about 21 minutes, and CodeRabbit confirmed both. A promotion carries reviewed content unchanged, so deferral is the correct adjudication. | None. |
| F19 | Decision sheet: Google Fonts failed hosted Build at 06:25, 11:44, 15:29 and 20:08 UTC. | **PARTIAL** | P3 | Build actually failed at 06:33:19 (run 36102822545, PR #1091), 11:51:35 (run 36131005655, a push-to-main run after #1102 merged), 15:29:00 and 20:08:26. All four logs show `Module not found: Can't resolve '@vercel/turbopack-next/internal/font/google/font'` and contain no explicit network-error string. The three families are SIL OFL (google/fonts `METADATA.pb` `license: "OFL"`), and the weights match `app/layout.tsx`. | The untracked sheet is not edited; its times should be corrected before the owner acts. |

## Clean passes

| Claim | Receipt |
| --- | --- |
| 1: ten features and ten promotions, one PR open at a time; suppressions 15 → 1 | #1111–#1130 were each created after the previous PR closed. No other PR was open between 16:00Z and 01:30Z. PR numbers 1071–1130 are contiguous. |
| 2 (features): exact-head approval | All ten final heads have a CodeRabbit APPROVED review (IDs as recorded in the docs). Every thread is resolved, and every finding has an author reply. |
| 3: gate tallies | Each PR body's unit, browser, integration and E2E tallies equal that head's CI log. A local re-run of unmodified `main` gave 503 files / 5,651 tests. |
| 4: releases | In all ten releases, main `test` passed on attempt 1. Each merge has exactly one READY/PROMOTED production deployment, the alias was assigned 1.9–3.7 s after `test`, and the dev and main trees are equal. |
| 5: Google Fonts | 82 runs on 2026-09-25. Exactly four first attempts failed, all in the Build step. Each was documented on its PR 1–2 s before a single re-run, and every re-run passed. |
| 6: one suppression left | `git ls-files '*.ts' '*.tsx' \| xargs grep -l 'biome-ignore lint/style/noExcessiveLinesPerFile'` lists only the lock-order suite. |
| 7: test full names | Collected with `vitest list --json` (real collection, all three lanes) at each split's base and merge. All 16 suites are identical, with one `it.each` title UUID-normalized in #1115. |
| 8: "pure move" candidates | Each is disclosed in its PR body and makes checks equal or stricter. #1111 compares parsed hrefs. #1119 removes a true duplicate. #1127 now fails `null` and disconnected nodes. #1121's tightened helper types match what every caller already passed. |
| 9: production changes | Consent state: 20,007 generated records sign identically before and after; the vector matches an independent HMAC; the verify side parses a strict all-string schema. The bookmark lookup's only caller passes allowlisted parse output. The count filters use the identical expression. |
| 11: coverage | Reproduced: datetime scanner 237/237 statements, 202/202 branches, 42/42 functions; feedback helpers 37/37 and 19/19; review-stage helpers 23/23, 10/10 and 4/4 in the browser lane. |
| 12: mutations | #1122: with the reset removed, the strengthened case fails (31000 ≠ 1000), while `f353a0c5`'s version passes. #1123: either tx-bound factory on the default db fails the transaction case. QuestionView: 8 wiring mutations each fail exactly one of 8 cases. Feedback: dropping `border-destructive` fails 4/37. |
| 13: disputed adjudications | #1122: a pickaxe over all refs finds no `toMatchObject({ cumulativeMs` ever. #1125: the dated pointer is immediately followed in the same row by its completion pointer. |
| 17: thin integration suites | Tag 3, bookmark 5, Clerk event 7, tombstone 3 cases. |
| 19: Part 2 step 4 and Verification checked | Not premature. Each Part 2 file has direct assertions or a recorded rationale, and the mobile item is an explicit open owner decision, visible while DEBT-468 stays Active. |
| 21: documentation guards | `tests/documentation-archive.test.ts` passes 1,111 cases on `main` (1,112 with this ledger); the archive audit reports 0 broken live links and 43 historical archive links, with exactly one Latest. |

## #1130 receipt derivation

| Item | Claimed | Observed |
| --- | --- | --- |
| #1129 merge | `906c9279` 00:56:06Z | `906c9279be560d0c06a5b8c234883fde530850c4`, 00:56:06Z |
| Approval | 5323842323, 00:43:46Z on `751a3051` | Same; the only review, no findings |
| Feature CI | 36205706009 | Attempt 1, success, 52 passed |
| #1130 merge | `656d98ad` 01:09:43Z | `656d98ad3dd66501abee72a588b4d3a45f38f337`, 01:09:43Z; second parent = head |
| Promotion CI | 36206671836 | Attempt 1, success, 52 passed |
| Main `test` | 36207474184, 01:21:14Z | Same, attempt 1 |
| Vercel Ready | 01:11:12.143Z | Same; "Waiting for checks to complete" status at 01:11:12Z |
| Alias | 01:21:16.346Z | `aliasAssignedAt` 01:21:16.346Z; `deployment-alias` check completed 01:21:16.159Z |
| Trees | `34ab8571` | Both `34ab857184cd9f0c48af166f52d0e3ec5f6e305d` |
| Health | 200 at 01:22:16Z | Not re-derivable; 200 `{"ok":true,"db":true}` at 17:47:10Z |

## Owner decisions

1. **Lock-order split (F1).** Narrow or exercise the harness's 15 non-literal gaps, as was done for the datetime scanner. Alternatively, split so the three cases that run the reconciliation writer share a file and the literal stays out of any shared module. The migration is neither required nor sufficient.
2. **Ratchet exactness (F2).** Should `pnpm lint:doubles` also fail when a floor exceeds its live count? That check would have caught F2. It widens a guard, so it needs approval.
3. **Ratchet scope (F17).** Should the scan also read files in `test-helpers/` directories?
4. **Promotion receipt order (F3).** Reaffirm the documented order: body edit, then merge, in one `&&` chain.
5. **Mobile project (F10).** A tagged smoke subset costs a fraction of the ≈30% a full second E2E pass adds to every hosted run.
6. **Self-hosted fonts (F19).** The licensing and failure premises hold once the sheet's times are corrected.
